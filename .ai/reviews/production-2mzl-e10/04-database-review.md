_OpenCode: модель opencode/gpt-6-astra._

{
  "summary": "Проверены изменения метаданных, фильтрация автовыпусков и серверное заполнение материалов. Обнаружена несовместимость нового фасада с документом-объектом, из-за которой новый сценарий тестирования падает до проверки результатов. Проверки на платформе и обновление базы не запускались.",
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "file": "src/cf/src/Documents/ВыпускПродукции/ManagerModule.bsl",
      "location": "ЗаполнитьМатериалыПоТехкартам, строка 15",
      "problem": "Вызов Выгрузить(, \"Номенклатура, КоличествоУпаковок\") перенесён из формы в метод, принимающий также ДокументОбъект.ВыпускПродукции. У табличной части документа метод Выгрузить() не принимает параметры отбора строк и колонок, в отличие от коллекции данных формы. Поэтому заявленный объектный вариант вызова не работает.",
      "scenario": "Запускается новый сценарий «РучнойВыпуск». ПроверитьОдиночныйРучнойВыпуск создаёт документ-объект и передаёт его в ЗаполнитьМатериалыПоТехкартам. На первой строке фасада возникает ошибка «Слишком много фактических параметров»; заполнение материалов, проведение и проверки остатков не выполняются. Вызов из формы эту проблему не выявляет, поскольку там передаются данные формы.",
      "recommended_fix": "Выгружать табличную часть без аргументов, затем ограничивать колонки полученной таблицы значений: Выпуск.ТЧГотоваяПродукция.Выгрузить().Скопировать(, \"Номенклатура, КоличествоУпаковок\"). Проверить оба заявленных варианта входного значения: документ-объект новым сценарием и данные формы кнопкой заполнения."
    }
  ]
}

## Разбор автором (2026-09-14)

Exit 3 засчитан пройденным гейтом по правилу владельца (модель `opencode/gpt-6-astra`); дополнительно — субагент Claude
(`04-database-review-claude.md`, approve, P3 принята в TECHDEBT).

- **P2 «Выгрузить с параметрами у табличной части документа» — rejected**, та же находка, что в `03-code-review.md`:
  `ТабличнаяЧасть.Выгрузить(, "Колонки")` работает на объекте — `Documents/ТехнологическаяКарта/ObjectModule.bsl:89`
  (`СвёрнутыйСостав`, вызывается из `ОбработкаПроведения` карты, в том числе в пройденных на базе сценариях E1–E9).
