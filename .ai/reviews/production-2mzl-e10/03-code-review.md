_OpenCode: модель opencode/gpt-6-astra._

{
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "file": "src/cf/src/Documents/ВыпускПродукции/ManagerModule.bsl",
      "location": "15",
      "title": "Выгрузка с параметрами не поддерживает табличную часть документа",
      "description": "Процедура объявлена для ДокументОбъект и ДанныеФормыСтруктура, но использует сигнатуру Выгрузить(Строки, Колонки) коллекции данных формы. У табличной части документа метод Выгрузить() не принимает параметры. Поэтому новый объектный путь заполнения материалов не работает.",
      "scenario": "При включённой опции «Производство» запустить сценарий «РучнойВыпуск». НовыйДокумент создаёт ДокументОбъект.ВыпускПродукции; первый вызов ЗаполнитьМатериалыПоТехкартам в ПроверитьОдиночныйРучнойВыпуск завершается ошибкой количества параметров метода Выгрузить, не доходя до проверок материалов и проведения.",
      "recommendation": "Выгружать таблицу без параметров, затем выделять нужные колонки методом Скопировать(, \"Номенклатура, КоличествоУпаковок\") либо явно разделить обработку двух типов. Проверить сценарий «РучнойВыпуск» и заполнение из формы."
    }
  ],
  "summary": "Обнаружена несовместимость нового фасада с объектом документа, используемым добавленным тестом. Остальных подтверждённых дефектов в рассмотренном диффе не выявлено. Проверка статическая: тесты на базе и EDT не запускались."
}

## Разбор автором (2026-09-14)

Exit 3 `GATE_NOT_PASSED` засчитан пройденным гейтом по правилу владельца: отчёт выполнила модель `opencode/gpt-6-astra`.

- **P2 «Выгрузить с параметрами не поддерживает табличную часть» — rejected.** `ТабличнаяЧасть.Выгрузить(<Строки>,
  <Колонки>)` у объекта документа параметры принимает; тот же вызов на табличной части объекта уже работает в
  `Documents/ТехнологическаяКарта/ObjectModule.bsl:89` (`ТЧМатериалы.Выгрузить(, "Номенклатура, …")` в `СвёрнутыйСостав`,
  вызывается из `ОбработкаПроведения` карты; сценарии E1 пройдены на базе), `ПересортицаТоваров/ObjectModule.bsl:10`, `АктРазбора/ObjectModule.bsl:35`. Окончательно подтверждается
  прогоном сценария `РучнойВыпуск` на обновлённой базе.
