_Субагент Claude general-purpose, ai-database (временная замена Codex), 2026-09-14._

# Вердикт: approve

## P3-1 — после смены карты задним числом автовыпуски молча остаются со старыми материалами

**Файл:** `CommonModules/ТехнологическиеКарты/Module.bsl:71-72, 81-82`; вызовы — `Catalogs/Номенклатура/Forms/ФормаЭлемента/Module.bsl:506`,
`Documents/ЗаказПокупателя/ObjectModule.bsl:966`.
**Сценарий:** у P после 01.09 только автовыпуски; новая версия карты с 01.09 — `ЕстьВыпускиПослеДаты` Ложь, предупреждения
нет; автовыпуски сохраняют материалы и себестоимость старой карты до перепроведения реализаций. Решение владельца 4, порчи
данных нет.
**Исправление:** записать в TECHDEBT либо позже — отдельная строка «перепроведите реализации».
**Разбор автором:** принято — пункт в TECHDEBT «Найдено в E10».

## Проверено без находок

1. Константа: имени и uuid нет в `src/cf`, `scripts/`, `docs/hub/` (подсистемы, cmi, формы, СКД, роли, ФО, планы обмена,
   `ОбновлениеПрограммыОбработчики`, начальное заполнение, строковые обращения); реструктуризация удаляет таблицу, падать
   нечему; в `build/drissotex` константы нет; есть в `build/base143` и `build/ahmed` — на maERP не влияет.
2. `ЕстьВыпускиПослеДаты`: условие совпадает с `Производство.ЭтоРеализация`; исторические выпуски (пустой заказ,
   `Неопределено`, пустая реализация) остаются в выборке; производительность как до правки (`ПЕРВЫЕ 1`, отбор по
   `Проведен`, `Дата`, подзапросу).
3. `Документы.ВыпускПродукции.ЗаполнитьМатериалыПоТехкартам`: работает с `ДанныеФормыСтруктура` и `ДокументОбъект`;
   `РассчитатьСтроку`/`ЕстьРеквизит` на сервере принимают строку ТЧ; суммы исторических выпусков меняются только явной командой.
4. `Form.form`: `ДоляСтоимостиПроцент` жила только в форме; `ТЧТоварыСтоимость` с теми же id и путём; 11 удалённых процедур
   сняты вместе с обработчиками, висячих нет; новых повторов id нет.
5. `РучнойВыпуск`: откат транзакции; числа сходятся (M1 10 → P 100; версия M1 3 → 15; мультивыпуск M1 8, M2 8, 180; коды
   1 и 2); отрицательный контроль P2 опирается на `ObjectModule:126-128`; проверки в `АвтовыпускПриОтгрузке` сходятся.
