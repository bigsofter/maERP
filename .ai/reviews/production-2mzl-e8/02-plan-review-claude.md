# E8 — ai-review-plan (обязательный гейт), план редакции 2

**Бэкенд: субагент Claude `architecture-critic` (opus) по `.ai/prompts/plan-critic.md`, не Codex.** Попытка Codex
2026-09-14 06:26Z: `codex exec` exit 1, «You've hit your usage limit … try again at Sep 19th, 2026»
(`.ai/state/production-2mzl-e8/plan-review-20260914T062648Z.jsonl`).

Вердикт: **approve_with_changes**, P0/P1 нет. Все находки приняты — план, раздел 10 (редакция 3).

Подтверждено: явная очистка движений при отмене удаляет то же, что автоудаление (`AutoDelete` у 5 документов,
умолчание `AutoDeleteOnUnpost` у 8), модуль набора есть только у `ТоварыВНаличии` и на пустом наборе ничего не делает;
формула `Начислено − Долг` сходится при зачёте авансов из любых платёжек, возвратах и частичной отгрузке; суммирование
по номенклатуре и характеристике закрывает повторные строки; `ТЧОснования` ПКО и `ТЧЗачётАвансов` реализации принимают
заказ; закрытие находок первой редакции верное, кроме п. 1 и 3 ниже.

| # | Уровень | Находка | Решение |
|---|---|---|---|
| 1 | P2 | РКО и списание безналичных блокируют взаиморасчёты при проведении (`РасходныйКассовыйОрдер/ObjectModule.bsl:124-130`, `СписаниеБезналичныхДенежныхСредств/ObjectModule.bsl:82-88`), план оставлял их без блокировки при отмене | в набор документов с текущими блокировками |
| 2 | P2 | Смена вида операции вместе с отменой проведения (`ЗаказПокупателя/ObjectModule.bsl:429-432`) оставляет продажу в «В производстве»: ветка продаж не считает `ВПроизводстве` записью E8 | признак записи E8 — заполненный `Индикатор`; фильтр по старому и новому виду; шаг 10б |
| 3 | P2 | Сценарий не проверяет зачёт аванса «сам ПКО»; ранний аванс на O1 искажает ожидания шагов 6–7 | аванс на отдельном заказе; зачёт через `ТЧЗачётАвансов` |
| 4 | P3 | `●` оплаты при «Ожидает оплаты», если начислено больше суммы заказа | база `max(Сумма, Начислено)`, `●` только при долге ≤ 0 |
| 5 | P3 | Не определено, какие реализации в долге; помеченная оплаченная реализация выпала бы | долг — по всем реализациям заказа |
| 6 | P3 | «Потребности нет» раньше «выпущено всё» — против решения 6 | порядок переставлен |
| 7 | P3 | Отмена проведения уже привилегирована (`unpostInPrivilegedMode`); подписка `ПослеПроведенияДокументаОплаты` пишет после истории | формулировки и риск |
| 8 | P3 | Скан шапок по неиндексированному `Основание` под блокировками продаж | индекс `Основание` в этой сборке, решение на `ai-database` |
| 9 | P3 | Ключ `РежимЗаписи` в `ДополнительныеСвойства` реализации уже занят (`РеализацияТоваровУслуг/ObjectModule.bsl:78`) | свой ключ модуля |
| 10 | P3 | Экспортная привилегированная очистка движений произвольного документа | только при отметке `ЗапомнитьЗаказыДокумента` с режимом отмены |
| 11 | P3 | Фоновое восстановление последовательности может менять «Пришло» (`docs/TECHDEBT.md:738-740`) | «Найдено в E8» |
