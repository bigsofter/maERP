# ai-review-plan: план E6, редакция 2 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

`.ai/bin/ai-review-plan production-2mzl-e6` 17:27Z: `codex exec` exit 1 — «You've hit your usage limit…
try again at 11:59 PM» (`.ai/state/production-2mzl-e6/plan-review-20260913T172757Z.jsonl`). По
`.ai/README.md` («Цепочка бэкендов») обязательный гейт выполнен субагентом Claude `architecture-critic`
(opus) с промптом `.ai/prompts/plan-critic.md`, не OpenCode.

Вердикт: **approve_with_changes**, P0/P1 нет. Решения — план, редакция 3.

| # | Sev | Находка | Решение |
|---|---|---|---|
| P2-1 | P2 | Шаги 2 и 4 теста проходят при сломанной логике: лишняя регистрация перезаписывает тот же документ дня, число карт и ссылка не меняются | принято: сверка `ВерсияДанных` документа карты до/после (не изменилась в 2, 3, 4; изменилась в 5) |
| P2-2 | P2 | «Известное следствие» уже, чем есть: `Техкарта` любой строки портит любая запись карты продукции в тот же день (карточка, форма карты, заказ); решение «версия не нужна» — без блокировки | принято: формулировка в TECHDEBT/отчёте/обязательствах E7–E9 («`Техкарта` — ссылка на документ дня, не снимок; состав — только `ТЧМатериалы`»); блокировка по всем продукциям заказа с материалами **до** решений; информационное сообщение при перезаписи карты дня с другим составом |
| P3-1 | P3 | Состав из базы должен нормализоваться той же функцией, иначе дубли строк материала вернут откат карты | принято: одна функция нормализации над коллекциями строк, связь по кодам внутри каждого состояния; дубль строки материала в шаге 3 теста |
| P3-2 | P3 | Отказ записи из объекта добавляет платформенное «Не удалось записать…» к сообщениям полей | принято как известное, в ручную проверку |
| P3-3 | P3 | Проверка смены вида после `СогласоватьМатериалы` сначала сообщит об очистке материалов | принято: перед `СогласоватьМатериалы`, функция `ПроверитьСменуВидаОперации` названа |
| P3-4 | P3 | Заголовок «Non reporté» у заказа на производство при выключенной опции | принято: проводимый = «Производство» и опция включена |
| P3-5 | P3 | Упаковка из `Техкарта` строки может вернуть старую упаковку | принято: из действующей карты, из `Техкарта` строки — только без действующей |
| P3-6 | P3 | Ветка упаковки в тесте зависит от константы базы | принято: признак опции — параметр нормализации, тест проверяет обе ветки |

Проверено без замечаний: привилегированный режим формы не переходит на запись; `posting` — единственная
правка `.mdo`; массового перепроведения нет; фикстуры вне транзакции; порядок шага 12; у карты нет
`ПередЗаписью`.
