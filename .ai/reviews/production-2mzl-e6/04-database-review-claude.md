# ai-database: E6 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59 (`.ai/state/production-2mzl-e6/plan-review-20260913T172757Z.jsonl`). По
`.ai/README.md` («Цепочка бэкендов») обязательный гейт выполнен субагентом Claude `general-purpose` с
промптом `.ai/prompts/database.md`.

Вердикт: **approve_with_changes**, P0/P1 нет. Реструктуризации нет: у заказа нет движений, в журналах и
последовательностях он не участвует, `Проведен` есть у любого документа; исторические заказы остаются
непроведёнными. Запись заказов кодом (групповое изменение, обработчик E4, лиды) не ломается.

| # | Sev | Находка | Решение |
|---|---|---|---|
| P2-1 | P2 | Блокировку карт по продукции берёт только заказ: взаимоблокировка с записью карты из карточки номенклатуры / формы карты и две карты на одну дату (срез смешает составы) | **fixed**: `ТехнологическиеКарты.ЗаблокироватьКартыПродукции` (экспортная, описана); `ЗаписатьТехкарту` в своей транзакции берёт блокировку до `ДатаРедактированияТехкарты`/`ДокументДня`; `ТехнологическаяКарта.ПередЗаписью` берёт её до записи документа (один порядок блокировок); заказ использует ту же процедуру. Тест параллельной записи на уровне 3 невозможен (одна сессия) — в ручные проверки |
| P3-1 | P3 | Проведённый заказ, записанный в режиме «Запись» с изменёнными материалами, остаётся проведённым, а перепроведение сочтёт состав неизменным | **fixed**: `ПроверитьМатериалыПроведенногоЗаказа` — отказ «проведите заказ»; шаг теста |
| P3-2 | P3 | Режим загрузки не вызывает `ОбработкаПроведения`: перенос с `Проведен = Истина` даст проведённые заказы в обход проверок | **fixed** (комментарий исправлен); перенос drissotex — в TECHDEBT: ставить `Проведен = Ложь` и сверять запросом перед выкладкой |
| P3-3 | P3 | Сообщения о перезаписи карты дня и о выпусках выводятся и при откате | **fixed**: копятся и выводятся после всех записей, только без отказа |
| P3-4 | P3 | Стандартная обработка «Проведение документов» проведёт непроведённые заказы на производство E4/E5 сегодняшней датой версии | принято: в TECHDEBT и отчёт владельцу (в базах клиентов исторические заказы — продажи) |
