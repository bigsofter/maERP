# ai-review (полный круг): E6 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59. По `.ai/README.md` («Цепочка бэкендов») обязательный гейт выполнен субагентом
Claude `general-purpose` с промптом `.ai/prompts/reviewer.md`. Ревью статическое, по диффу.

Вердикт: **approve_with_changes**, P0/P1 нет.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P2 | В ветке `Исключение` `СтрШаблон` со ссылкой читает представление из испорченной транзакции — вместо причины пользователь получит «В данной транзакции уже происходили ошибки» | **fixed**: представление продукции вычисляется до записи |
| 2 | P3 | Значения блокировки не отсортированы (план 2.3.3.1) — встречные заказы {A,B}/{B,A} | **fixed**: `ЗаблокироватьКартыПродукции` сортирует значения по `XMLСтрока` |
| 3 | P3 | «Что нового»: версия не создаётся и при совпадении с версией, по которой заполнены материалы | **fixed**: формулировка на четырёх языках |
| 4 | P3 | Отменённая карта считается действующей и даёт пустую упаковку базовой версии | **fixed**: базовая — действующая карта с непустым составом, иначе `Техкарта` строки |
| 5 | P3 | При выключенной опции «Производство» проведённый заказ не записать из формы, сообщение не подсказывает отмену проведения | **fixed**: в сообщение добавлено «сначала отмените проведение» |
| 6 | P3 | Команды проведения появятся в таблицах заказов кабинета клиента и списка реализаций | **подтверждено** (автозаполнение панелей таблиц); не исправлялось: таблица реализаций на закладке, кнопка в таблице роняет клиент 8.5 (forms-009) — TECHDEBT, «посмотреть глазами»; проведение продажи отклоняется |
| 7 | P3 | Тест: отказ продаже в `ПередЗаписью` не отличим от `ОбработкаПроведения`; смена вида вместе с отменой проведения не выполнялась; ветка упаковки неизвестна; право в `ПроверитьИзменениеКарты` не проверяется; пользователь прогона без права | **fixed частично**: прямые проверки `ЗаказПроводится`; настоящая отмена проведения со сменой вида в одной записи; ветка упаковки в тексте результата; предусловие права пользователя прогона. Отказ без права — ручная проверка под `Оператор` (TESTING.md) |

Вместе с находками ревью данных и безопасности (`04`, `05`) изменения проверяются кругом `03b`.

## 03b — ai-review --verify (субагент Claude, промпт `reviewer-verify.md`; Codex в лимите)

Вердикт круга: request_changes → исправлено. 11 из 12 находок со статусом fixed закрыты.

| Находка | Статус | Решение |
|---|---|---|
| 05 P2-2 узкий привилегированный режим | partial | принято: у заказа `postInPrivilegedMode = true`, охват режима платформой не проверен; комментарий исправлен — защита держится на `ЕстьПравоМенятьТехкарты` (проверка у явного пользователя в привилегированном режиме) |
| N1 P1 — отказ по дате внутри вложенной транзакции фасада отменял внешнюю: сценарий `ДатыТехкарты` падал на чтении после пойманного отказа | **fixed**: `ЗаписатьТехкарту` фиксирует транзакцию без записи и бросает отказ по дате вне неё; запись вынесена в `ЗаписатьДокументТехкарты` |
| N2 P3 — смена продукции у записанной карты не блокирует прежнюю продукцию | **fixed**: `ПередЗаписью` карты блокирует обе |
| N3 P3 — «Что нового»: запись проведённого заказа с изменёнными материалами отклоняется, а не «не сохраняет» | **fixed** на четырёх языках |

Прогон `doctests` на базе с этим кодом не выполнялся (база не обновлена) — `ДатыТехкарты` и `ПроведениеЗаказа` подтверждаются после обновления `build/ib`.

## 03c — узкий --verify исправлений N1/N2 (субагент Claude; Codex в лимите)

N1 **closed** (отказ по дате не портит внешнюю транзакцию; карточка номенклатуры и обработчик переноса
карт — прежнее поведение; путь исключения из проведения заказа корректен). N2 **closed**. Новых P0–P2 нет.
P3: физическое удаление проведённой карты убирает её записи без блокировки продукции — в TECHDEBT.
