# ai-security: E6 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59. По `.ai/README.md` («Цепочка бэкендов») обязательный гейт выполнен субагентом
Claude `general-purpose` с промптом `.ai/prompts/security.md`.

Вердикт: **request_changes** (P1-1), после исправлений — закрыто; подтверждающий круг — `03b`.
Права ролей сверены субагентом точечно: у `Оператор`, `Клиент`, `ЛичныйКабинет`, `PDV` на карту и
`СоставТехКарты` только Read/View. Экспортные проверки объекта с клиента недоступны и не пишут; ветка
«состав не менялся» карту не пишет; привилегированный режим форм на запись не переходит; секретов нет.

| # | Sev | Находка | Решение |
|---|---|---|---|
| P1-1 | P1 | `ПравоДоступа` без пользователя проверяет права «на момент выполнения строки»: если `ПередЗаписью` проведения выполняется привилегированно (`postInPrivilegedMode`), проверка вернёт Истина и роль без прав перезапишет общую карту | **fixed**: подтверждено справкой 8.5 (`AccessRight`: без пользователя — «на момент выполнения текущей строчки»; `SetPrivilegedMode`: «никакие проверки прав не выполняются»). `ЕстьПравоМенятьТехкарты`: вне привилегированного режима — права текущего пользователя, в привилегированном — `ПравоДоступа(…, ПользователиИнформационнойБазы.ТекущийПользователь())`; проверяются Добавление, Изменение и Проведение |
| P2-2 | P2 | Привилегированный режим включён на всю регистрацию (чтения, сообщения, строки) | **fixed**: только вокруг `ЗаписатьТехкарту`, явное выключение в обеих ветках `Попытка`; у ролей с правом на карты есть чтение выпусков (сверено) |
| P2-3 | P2 | Отказ без права не покрыт автотестом (прогон — администратором) | принято частично: тест сверяет конфигурацию прав ролей; поведение под пользователем `Оператор` — ручная проверка в TESTING.md и отчёте |
| P3-4 | P3 | `ECommerce` имеет права на карты — теперь меняет их и через заказ | вопрос владельцу в отчёте; путь не новый по правам (форма карты уже доступна) |
