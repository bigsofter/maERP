# architecture-critic: план E6, редакция 1 (2026-09-13)

Субагент Claude `architecture-critic` (opus), внутрисессионный отсев до обязательного `ai-review-plan`.
Вердикт: **approve_with_changes**. Находки сверены с кодом главной сессией; решения — в плане, редакция 2.

| # | Sev | Находка | Решение |
|---|---|---|---|
| P1-1 | P1 | «Записать» проведённого заказа перепроводит его: заказ дня с другим составом, правка карты технологом или пометка версии — и перепроведение старого заказа тихо возвращает старый состав текущей картой; `Оператор` не сможет записать чужой проведённый заказ (нужно право на карты) | принято: продукция, чей состав не менялся с проведённого состояния в базе, версию не регистрирует и право не проверяет; остаток (первое проведение при карте дня другого заказа) — в отчёт владельцу как следствие решений 1–2, в TECHDEBT |
| P2-1 | P2 | Пустая `Техкарта` = новая версия даже при составе, равном действующей карте | принято: кандидат — действующая версия `ТекущиеТехкарты` на сегодня |
| P2-2 | P2 | При проведении кодом строки материалов с пустой номенклатурой уходят в карту и молча теряются (маркер отмены) | принято: отказ в `ПроверитьПроведениеЗаказа`, шаг теста |
| P2-3 | P2 | `РаботаСДокументами.УстановитьЗаголовокФормыДокумента` у продажи покажет «Non reporté» вместо «Enregistré», в том числе в ECommerce-форме | подтверждено (`Module.bsl:41–57`); принято: общая функция считает заказ не на производство непроводимым |
| P2-4 | P2 | `ФормаВыбора` (автозаполнение, `commandBarLocation None`) покажет «Провести»/«Отмена проведения» | подтверждено; принято: явные скрытые кнопки в панели и контекстном меню |
| P3-1 | P3 | Проверка заполнения карты при программном `Записать(Проведение)` не вызывается — «карта сама откажет» ложно | подтверждено справкой (вызывается формой или `ПроверитьЗаполнение`); формулировка исправлена, проверка в заказе — единственная |
| P3-2 | P3 | Блокировка `СоставТехКарты` по продукции может конфликтовать с записью карты из карточки номенклатуры и формы карты (таймаут, без порчи) | принято: одним `Заблокировать()`, значения отсортированы; обе формы-источника в TECHDEBT |
| P3-3 | P3 | Отказ «разные упаковки» при выключенной опции — после очистки упаковки | принято |
| P3-4 | P3 | Шаг 10 теста: после исключения — только объект в памяти | принято |
| P3-5 | P3 | Вызов `ОбработкаПроведения` в режиме загрузки не подтверждён; отказ продаже до `ПриЗаписи` надёжнее | принято: отказ продублирован в `ПередЗаписью` при проведении, обоснование исправлено |
| P3-6 | P3 | Контекстные меню `ФормаСписка`/`ФормаВыбора` и «Связанные документы» предложат «Провести» | принято для списка и выбора; «Связанные документы» — отказ с сообщением, TECHDEBT |

Проверено критиком без проблем: точность количества (12,3 везде), чтение фасада ролями без прав на
карту, ECommerce-панель без автозаполнения, фикстуры, копирование, ввод реализации, АРМ.
