_OpenCode: модель opencode/gpt-6-astra (временная замена Codex)._

# Вердикт: approve

**Сборка 2.0.15.25.** Подтверждённых находок P0–P3 в проверенном диффе нет.

- В шести формах сохранены прежние имена элементов и пути данных. Новые ID не пересекаются с существующими; у добавленных страниц и контейнеров задана видимость. Структура Page/Pages соответствует образцу реализации.
- Изменение групп выпуска на страницы сохраняет управление видимостью `ГруппаГотоваяПродукция`. Связанные таблицы продукции и материалов заказа не разнесены по разным закладкам.
- В двадцати затронутых документах ссылок на удалённые `ОтборСумма`, `ВидСравненияУсловия` и `ЗнакУсловия` не осталось.
- Новая структура основания обработана отдельной веткой. Обычное заполнение по КП и реализации сохранено; принудительный производственный вид учитывает функциональную опцию. Заполнение материалов использует существующий механизм.
- Подписи панели «Детали» согласованы с заменёнными полями запросов. Секция «Что нового» присутствует на четырёх языках.

**Проверки:** `scripts/refcheck.py` и `scripts/listcheck.py` выполнены — новых расхождений нет.

**Ограничения:** запуск `queryfields.py` запрещён allow-list окружения; проверен предоставленный код, не результат выполнения. EDT, клиент 1С, АРМ и проведение на базе не запускались. Перед передачей владельцу нужны Refresh и проверка в EDT, затем проверка создания заказа через `Основание = Структура` и переключения режима выпуска в клиенте.

## Разбор автором (2026-09-14)

Полный промпт обёртки (1 МБ из-за переиндентации форм) не запустился — ревью собрано вручную: дифф без Form.form, формы — через
`git diff -w` ревьюером. Находок нет. Прогон на копии обновлённой `build/ib`: 25 сценариев без ошибок, смок — CheckConfig чисто,
шаг 1б ложно (96 строк известны), формы и печать без ошибок (две печатные формы запроса котировок «под подозрением» — номер в
шапке, не ошибка).
