_Claude Opus 5 (1M), независимое ревью диффа рабочего каталога, 2026-09-16._

```json
{
  "summary": "Сборка 2.0.15.35 делает то, что заявлено в секции «Что нового»: счётчики строк в заголовках нижних закладок, кнопки ввода на основании в панелях своих закладок по отметке ровно одного заказа, явные ширины колонок, колонки движения «Продукция» и «Материалы» с цветом и подвалом, количества в подсказке индикатора. Проверено отдельно: gen7.py воспроизводит Form.form байт в байт (diff пустой); подвал у таблицы «Документы» включён (footer=true, showInFooter у обеих колонок); элементы кнопок имеют <visible>true</visible>, стартовое скрытие делает ОбновитьВидимостьОснований; ЦветаСтиля.Цвет_Зелёный и Цвет_Красный есть в метаданных; Количество в ТЧТовары и ТЧГотоваяПродукция всех семи документов — базовые единицы (КоличествоУпаковок отдельным реквизитом), то есть находка P2-1 прошлого круга закрыта верно; право чтения по каждому документу проверяется до построения запроса, РАЗРЕШЕННЫЕ первой части объединения покрывает всё объединение; Проведен и ПометкаУдаления в запросе документов есть; ПередачаВПереработку действительно списывает ТЧТовары с ТоварыВНаличии, а ПоступлениеИзПереработки расходует их с ТоварыВПереработке — двойного вычета нет; точки обновления счётчиков закладок закрывают вставку, удаление, запись строки, снятие пустой вставленной строки и перечитывание заказа; смена верхней закладки, отбор и запись заказов идут через ОбновитьТекущийЗаказ, значит и счётчики, и видимость кнопок обновляются; при пустом заказе таблицы пустые, подвал пустой, заголовки с (0); строк длиннее 140 символов среди добавленных нет; заголовки страниц в модуле совпадают с T из gen.py на всех четырёх языках. Блокировки не нужны — запрос только читает; нагрузка на переключение строки заказа выросла на одно объединение из восьми выборок по табличным частям с отбором по индексированному полю Ссылка, это приемлемо. Найдено две функциональные проблемы (P2) и пять замечаний по сопровождаемости и интерфейсу (P3). Данные не портятся, конфигурация загружается — вердикт approve_with_changes.",
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/ManagerModule.bsl",
      "location": "ТекстЗапросаДвиженияДокументов, строки 2506–2526 диффа (текст части объединения) и комментарий к ЗаполнитьДвижениеДокументов, строки 2416–2421",
      "problem": "Комментарий к ЗаполнитьДвижениеДокументов утверждает: «Услуги в движение не идут - как и при проведении складских движений (регистр ТоварыВНаличии)». В запросе такого условия нет: часть объединения — «ВЫБРАТЬ %1 КАК Ключ, Т.Ссылка, СУММА(Т.Количество) ИЗ %2 КАК Т ГДЕ Т.Ссылка В (&Документы) СГРУППИРОВАТЬ ПО Т.Ссылка», без отбора по ТипНоменклатуры. Проведение складских движений услуги отсекает явно — AccumulationRegisters/ТоварыВНаличии/ManagerModule.bsl:510, «НЕ ВТ.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга)», а комплекты там же разворачиваются в состав (строка 445). Заявленный «складской взгляд на заказ» и код расходятся. Находка совпадает с P2 внешнего ревью astra (03-code-review.md) и осталась незакрытой: комментарий дописан, условие — нет.",
      "scenario": "Оператор вносит в ТЧТовары реализации по заказу строку продукции 10 шт и строку номенклатуры с типом «Услуга» (доставка) количеством 1 — платформа этого не запрещает, отдельная ТЧУслуги обязательной не является. Реализация проводится, склад списывает 10. На закладке «Документы» колонка «Продукция» показывает −11, подвал даёт «+0 -11 = -11», подсказка индикатора — «Продукция: приход 0, отгружено 11». Владелец сверяет с отчётом по складу и видит расхождение на услугу. То же с комплектом: строка комплекта количеством 1 даёт в колонке 1, а на складе списываются его комплектующие.",
      "recommended_fix": "Добавить в каждую часть объединения отбор, повторяющий проведение: «И НЕ Т.Номенклатура.ТипНоменклатуры = ЗНАЧЕНИЕ(Перечисление.ТипыНоменклатуры.Услуга)». Если разворачивать комплекты в состав не планируется — убрать из комментария ссылку на ТоварыВНаличии и назвать вещи своими именами: «количество строк документа, услуги исключены, комплекты считаются как одна позиция». В сценарий АРМПроизводство добавить строку-услугу в один из документов и проверить точное значение движения."
    },
    {
      "severity": "P2",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/ManagerModule.bsl",
      "location": "КоличестваЗаказа, строки 2353–2391 диффа",
      "problem": "Одна строка подсказки складывает числа, посчитанные в двух разных базах. «Пришло» и «ожидается» берутся из таблицы Материалы, то есть из регистра ПотребностиПроизводства: движения там распределены по продукции и обрезаны пределами документа (AccumulationRegisters/ПотребностиПроизводства/ManagerModule.bsl, СформироватьДвиженияЗакупки → РаспределитьПоПродукции(Строки, ПределыДокумента(...))), это количества, отнесённые именно к этому заказу. «Списано», «приход» продукции и «отгружено» берутся из колонок движения таблицы Документы, то есть суммы по документу целиком, включая строки чужих заказов. В окне «Что нового» оговорка «количество берётся по документу целиком» отнесена только к колонкам закладки «Документы», про подсказку индикатора такой оговорки нет — описание и поведение расходятся.",
      "scenario": "Заказу А нужно 10 кг материала, поступление на 10 кг проведено. Выпуск продукции делается общий на три заказа и списывает 25 кг, из них по заказу А — 10 кг. Владелец наводит подсказку на индикатор заказа А и читает «Материалы: пришло 10, списано 25, ожидается 0»: по цифрам выходит, что по заказу списали в два с половиной раза больше, чем пришло. Та же картина по продукции, если реализация собрана на несколько заказов: «приход 10, отгружено 30».",
      "recommended_fix": "Либо считать «списано», «приход» и «отгружено» в границах заказа — тем же распределением, каким регистр ПотребностиПроизводства считает «Пришло», либо оставить расчёт как есть, но развести базы в тексте: «Материалы по заказу: пришло %1, ожидается %3. По документам заказа целиком: списано %2» — и повторить эту оговорку пунктом секции 2.0.15.35 во всех четырёх языках."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/ТестыДокументов/ManagerModule.bsl",
      "location": "ПроверитьДвижениеДокументовАРМ, строки 4403–4426",
      "problem": "ПарыДвиженияДокументов задаёт восемь пар «документ — табличная часть» с колонкой и знаком, а тест проверяет две ситуации: у заказа поставщику движения нет, у поступления материалы с плюсом. Ни один расход не проверен, продукция не проверена вовсе. Перепутанные местами колонки (ВыпускПродукции: ТЧТовары в «Продукцию», ТЧГотоваяПродукция в «Материалы») или знак наоборот у ПередачаВПереработку, ВыпускПродукции, РеализацияТоваровУслуг, ВозвратПоставщику, ВозвратОтПокупателя пройдут все 30 сценариев незамеченными. Сценарий уже создаёт и проводит эти документы — ПроверитьВыпускАРМ (5022), ПроверитьОтгрузкуАРМ (5114), ПроверитьПередачуАРМ (5429), ПроверитьПоступлениеИзПереработкиАРМ (5469) — данные для проверки уже есть.",
      "scenario": "Разработчик следующей волны меняет порядок аргументов в вызове ДобавитьПаруДвижения для ВыпускПродукции и получает в колонке «Материалы» приход готовой продукции, а в «Продукции» — расход материалов. scripts/smoke.sh и все сценарии ТестыДокументов зелёные, ошибку находит владелец на своих данных.",
      "recommended_fix": "Дополнить ПроверитьДвижениеДокументовАРМ (или добавить проверки в существующие ПроверитьВыпускАРМ, ПроверитьОтгрузкуАРМ, ПроверитьПередачуАРМ) точными значениями: у выпуска МатериалыЧисло < 0 и ПродукцияЧисло > 0 с ожидаемыми количествами, у передачи в переработку МатериалыЧисло < 0 и пустая «Продукция», у реализации ПродукцияЧисло < 0 и строка начинается с «-»."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "ОбновитьЗаголовкиЗакладок, строки 1149–1178 диффа",
      "problem": "Семь заголовков страниц заданы НСтр в модуле формы и ровно теми же словами на тех же четырёх языках — в gen7.py через T (проверил построчно: совпадают сегодня). Заголовок из Form.form не виден никогда, его сразу перетирает ПриСозданииНаСервере. Правка заголовка в генераторе (единственном месте, куда по правилам проекта смотрят при работе с формой) не даст никакого эффекта, и расхождение обнаружится только глазами на живом клиенте — ни смок, ни refcheck, ни listcheck его не ловят. Кроме того, на СтраницаМатериалы счётчик считает строки материалов (по одной на материал заказа), а не документы, — семантика скобок у первой закладки отличается от остальных.",
      "recommended_fix": "Брать базовый заголовок из самой формы — запомнить Элементы[Имя].Заголовок в ПриСозданииНаСервере до первой подстановки счётчика (или в реквизит формы) и дальше собирать «%1 (%2)» из него; тогда источник заголовков остаётся один — gen7.py. Если словарь остаётся в модуле, дописать в комментарий к ОбновитьЗаголовкиЗакладок, что заголовки обязаны совпадать с T в scripts/formgen/arm/gen.py и gen7.py."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "ОбновитьВидимостьОснований (1198–1206 диффа) и её единственный клиентский вызов в ОбновитьТекущийЗаказ (2327)",
      "problem": "Видимость кнопок пересчитывается только при активизации строки, смене верхней закладки, изменении отбора и после записи заказов. Снятие отметки с уже текущей строки (Ctrl+клик по ней) состав ВыделенныеСтроки меняет, а ПриАктивизацииСтроки не вызывает — кнопка остаётся видимой при нуле отмеченных заказов. Кроме того, в условие видимости не входит МожноНаОсновании: у заказа-черновика кнопки видны, хотя ввод на основании запрещён.",
      "scenario": "Владелец отмечает заказ, видит «Поступление» на закладке «Поступления», Ctrl+кликом снимает отметку, нажимает кнопку и получает сообщение «Выберите заказ» вместо исчезнувшей кнопки. У черновика — «Вводить документы можно только по заказу в работе». Отказ мягкий, данные не портятся, но обещание «кнопка появляется, когда выбран ровно один заказ» из окна «Что нового» выполняется не всегда.",
      "recommended_fix": "Добавить вызов ОбновитьВидимостьОснований в ПриИзмененииФлажкаВыделения — или, если такого события у таблицы нет, включить в условие МожноНаОсновании (Форма.МожноНаОсновании И ЕстьЗаказ И Форма[право]), тогда для черновика и для пустого выбора кнопка пропадает по уже существующим точкам обновления."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "ОбновитьИтогиДвижения, строки 2361–2384 диффа",
      "problem": "Подвал агрегирует движение второй раз, независимо от КоличестваЗаказа в модуле менеджера: свой цикл, свои «Поступило» и «Списано», имена колонок заданы строкой СтрРазделить(\"Продукция,Материалы\", \",\") вместо ИмяКолонкиПродукции()/ИмяКолонкиМатериалов()/СуффиксЧисла(), которые ровно для этого и заведены. Считает при этом по реквизиту формы, где колонки движения имеют квалификатор Число(15,3), тогда как подсказка считает по исходной ТаблицаЗначений без ограничения дробной части: у номенклатуры с четырьмя знаками после запятой подвал и подсказка разойдутся в последнем разряде. Переименование колонки движения в менеджере оставит форму с прежними строковыми именами и уронит её на Элементы[\"ДокументыЗаказа\" + ИмяКолонки].",
      "recommended_fix": "Вынести агрегацию в экспортную функцию модуля менеджера (например ИтогиДвиженияДокументов(Документы) → Структура с Поступило и Списано по обеим колонкам) и вызывать её и из КоличестваЗаказа, и из ОбновитьИтогиДвижения; имена колонок брать из тех же функций, что и в менеджере, а не строкой."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "НастроитьЭлементы, строки 890–891 диффа",
      "problem": "ОтображениеПодсказки.Кнопка ставится на ЗаказыИндикатор и ПродукцияИндикатор — это колонки таблиц. В конфигурации такое отображение встречается только у декораций и надписей (CommonForms/ФормаРМКУправляемаяИнформацииОбОшибке/Module.bsl:33, CommonModules/БухгалтерскиеОтчетыКлиент/Module.bsl:1460), прецедента на колонке таблицы нет. Где платформа 8.5 нарисует значок у колонки — у заголовка (как записано в комментарии) или в каждой ячейке колонки индикатора — из кода не следует, а смок проверяет только открытие формы. Это ровно тот случай, про который правило tiny1C workflow-005: новую конструкцию собирать по работающему образцу из этой же конфигурации.",
      "scenario": "Владелец обновляется, открывает рабочее место и видит значок подсказки в каждой строке колонки «Индикатор» рядом с кружками — колонка, которую специально делали компактной, становится шире и шумнее. Обнаружить это до сдачи нечем: и refcheck, и listcheck, и смок проходят.",
      "recommended_fix": "Посмотреть на живом клиенте 8.5 обе верхние таблицы со значком подсказки и, если значок рисуется в ячейках, перенести пояснение в подсказку самой страницы/группы или в отдельную кнопку командной панели. Результат осмотра записать в отчёт по волне — как сделано с проверкой TitleDataPath."
    },
    {
      "severity": "P3",
      "file": "src/cf/src/DataProcessors/ТестыДокументов/ManagerModule.bsl",
      "location": "ПроверитьДвижениеДокументовАРМ, строки 4414–4416",
      "problem": "Проверка «пришедшие материалы попали в подсказку индикатора» сделана через СтрНайти(Количества, Формат(МатериалыЧисло, \"ЧГ=0\")) > 0. Строка Количества содержит пять чисел; при типичных тестовых количествах (1, 2, 10) искомая подстрока найдётся в любом из них — например «10» найдётся внутри «100», а «2» — внутри «12». Проверка зелёная и тогда, когда количество материалов в подсказку не попало вовсе.",
      "scenario": "Кто-то меняет порядок подстановки в шаблоне КоличестваЗаказа местами (пришло ↔ ожидается). Подсказка врёт, а проверка проходит, потому что то же число стоит в соседней позиции строки.",
      "recommended_fix": "Сверять подсказку целиком с ожидаемой строкой — собрать её тем же СтрШаблон с известными количествами — либо проверять позицию вхождения относительно слова «пришло»."
    }
  ]
}
```
