_OpenCode: модель opencode/gpt-6-astra._

{
  "verdict": "reject",
  "summary": "Найдены ошибки в команде отмены заказа и выборе черновика для следующей строки. scripts/refcheck.py и scripts/listcheck.py новых расхождений не выявили. Проверки EDT и выполнение сценариев на базе не проводились.",
  "findings": [
    {
      "severity": "P1",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "255–259, СтатусОтменить",
      "title": "Массив выделенных заказов присваивается реквизиту динамического списка",
      "scenario": "Пользователь выделяет заказ и нажимает «Статус → Отменить». Присваивание «Заказы = ВыделенныеЗаказы()» обращается к существующему реквизиту формы «Заказы» типа DynamicList, а не создаёт локальную переменную. Попытка присвоить ему массив приводит к ошибке до показа подтверждения; пометка на удаление не выполняется.",
      "recommendation": "Назвать переменную «ВыбранныеЗаказы», как в процедуре ИзменитьСтатус, и использовать её при проверке количества и создании оповещения."
    },
    {
      "severity": "P2",
      "file": "src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl",
      "location": "192–197, ПродукцияКлиентПриИзменении",
      "title": "Следующая строка выбирает первый черновик в таблице вместо последнего использованного",
      "scenario": "В таблице уже есть черновик A клиента. Пользователь добавляет строку этого клиента, явно выбирает «Новый заказ» и сохраняет её в черновик B. Строки B остаются на месте добавленной строки ниже A. При вводе следующей строки того же клиента цикл находит A первым и прерывается: строка сохраняется в A, а не в только что созданный B. Аналогично не сохраняется выбор другого существующего черновика.",
      "recommendation": "Хранить последний успешно использованный черновик отдельно для каждого клиента и обновлять его после записи строки; порядок строк таблицы не должен определять этот выбор."
    }
  ]
}
