_OpenCode: модель opencode/gpt-5.3-codex._

{
  "verdict": "reject",
  "findings": [
    {
      "severity": "P1",
      "file": "src/cf/src/Documents/ВыпускПродукции/ObjectModule.bsl",
      "location": "@@ -13,7 +13,6 @@ (удалён вызов `Блокировка.Заблокировать();` в `ОбработкаПроведения`)",
      "title": "Снята управляемая блокировка перед формированием движений по остаткам",
      "scenario": "Два пользователя одновременно проводят документы «ВыпускПродукции» по одним и тем же товарам/сериям. Оба сеанса доходят до `РегистрыНакопления.ТоварыВНаличии.СформироватьДвижения(ЭтотОбъект)` без фактической блокировки набора данных (объект `Блокировка` создан, но не активирован). В результате проверки/расчёты выполняются на устаревших остатках, и один из документов проводится с некорректным результатом (гонка по остаткам, возможные неверные движения/доступность).",
      "recommendation": "Вернуть `Блокировка.Заблокировать();` перед формированием движений. Текущий код отклоняется от паттерна в аналогичных документах (например, `ПеремещениеТоваров/ObjectModule.bsl`), где после `УправляемыеБлокировки.ТоварыВНаличии(...)` блокировка обязательно активируется."
    }
  ]
}
