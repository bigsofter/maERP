_OpenCode: модель opencode/big-pickle._

Now let me check whether `СформироватьДвижения` in the register module acquires its own lock:
The register module has **zero** lock-related code. The lock was built in the document's handler and its only purpose was to be activated by the now-removed `.Заблокировать()` call. This confirms the finding.

Now let me verify the `СформироватьДвижения` reads balances (to confirm the lock matters):
All the evidence is collected. Here's the review:

```json
{
  "verdict": "reject",
  "findings": [
    {
      "id": "bench-lock-001",
      "severity": "P1",
      "file": "src/cf/src/Documents/ВыпускПродукции/ObjectModule.bsl",
      "location": "ОбработчикПроведения, строка 15–16 (diff: удалена строка `Блокировка.Заблокировать()`)",
      "rule": "Управляемые блокировки: построенный объект блокировки должен быть активирован вызовом Заблокировать() до обращения к данным.",
      "description": "Удалена строка `Блокировка.Заблокировать();` — единственная точка, где реально захватывается управляемая блокировка. Объект `Блокировка` создаётся (стр. 14) и в него добавляется элемент через `УправляемыеБлокировки.ТоварыВНаличии` (стр. 15), но без `.Заблокировать()` ни один из блокируемых диапазонов регистра `ТоварыВНаличии` не блокируется. Модуль регистра `ТоварыВНаличии/ManagerModule.bsl` (где `СформироватьДвижения`) **не содержит собственных блокировок** — проверено `rg`.",
      "scenario": "Два документа «ВыпускПродукции» проводятся параллельно (или одновременно «ВыпускПродукции» и другой документ, списывающий тот же товар с того же места хранения). Оба вызывают `СформироватьДвижения` → `СписатьНоменклатуру`, которая читает текущие остатки. Без захвата блокировки оба видят одно и то же количество на складе, оба записывают расход — итоговый баланс `ТоварыВНаличии` уходит в минус. Потеря данных: сверхписание товара.",
      "suggested_fix": "Вернуть строку `Блокировка.Заблокировать();` на место между добавлением элемента блокировки и вызовом `СформироватьДвижения` (между текущими строками 15 и 16). Все остальные 20 документов проекта, имеющие `Блокировка + УправляемыеБлокировки`, завершают захват строкой `.Заблокировать()` — đây единственный случай, где она пропущена."
    }
  ]
}
```

**Рекомендация:** Восстановить удалённую строку. Это не баг оптимизации и не побочный эффект рефакторинга — это случайное удаление единственной точки захвата блокировки, без которой весь механизм `УправляемыеБлокировки` бесполезен. Без этой строки документ «ВыпускПродукции» не защищён от сверхсписания при параллельном проведении.
