_OpenCode: модель opencode/gpt-5.3-codex._

{
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "title": "Частичное выравнивание `КодСтроки` оставляет дубли и приводит к неожиданной очистке `Техкарта` при записи",
      "file": "src/cf/src/Documents/ЗаказПокупателя/ManagerModule.bsl",
      "location": "Функция `ОбеспечитьКодыСтрокТоваров` (блок с `Проверять = ...` и условием `Если Проверять И КодЗанят Тогда`)",
      "scenario": "Пользователь создаёт заказ по основанию, где у двух строк товаров одинаковый `КодСтроки` (в комментарии к коду это прямо отмечено как возможный случай). Затем в форме заполняет материалы только для одной строки командой `ЗаполнитьМатериалыПоТехкарте`. В `ЗаполнитьМатериалыПоТехкартам` вызывается `ОбеспечитьКодыСтрокТоваров(ТЧТовары, Строки)` — и из-за фильтра `Проверять` дубли во «вневыбранных» строках могут остаться. При последующей записи `РазвестиПовторныеКодыСтрокСМатериалами` в `ObjectModule` разводит дубли и очищает `Техкарта` у одной из строк. Для пользователя это выглядит как самопроизвольная потеря ранее заданной техкарты/привязки строки.",
      "recommendation": "Перед заполнением материалов гарантировать уникальность `КодСтроки` по всей `ТЧТовары`, а не только по переданному подмножеству строк (или как минимум явно защитить кейс дублей между выбранными и невыбранными строками). Это уберёт скрытую зависимость от порядка/набора строк и предотвратит неожиданную очистку `Техкарта` на записи."
    }
  ],
  "summary": "Критичных поломок загрузки/компиляции по диффу не вижу, но есть один функциональный риск в логике уникализации кодов строк при частичном заполнении материалов."
}
