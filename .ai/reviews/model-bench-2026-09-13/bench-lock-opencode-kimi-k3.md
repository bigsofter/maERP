_OpenCode: модель opencode/kimi-k3._

```json
{
  "verdict": "reject",
  "findings": [
    {
      "severity": "P1",
      "file": "src/cf/src/Documents/ВыпускПродукции/ObjectModule.bsl",
      "location": "ОбработкаПроведения, строки 13–17 (удалённая строка «Блокировка.Заблокировать();»)",
      "title": "Удалено применение управляемой блокировки — остатки ТоварыВНаличии читаются без блокировки",
      "scenario": "Два пользователя одновременно проводят документы, списывающие одну и ту же номенклатуру: например, два «Выпуск продукции» с одним материалом, или «Выпуск продукции» параллельно с «Реализация товаров услуг». Управляемые блокировки конфликтуют, только если их запрашивают обе транзакции: Реализация блокировку берёт (ObjectModule.bsl:51), а ВыпускПродукции после этой правки — нет. Обе транзакции читают РегистрНакопления.ТоварыВНаличии.Остатки (СписатьНоменклатуру, ManagerModule ТоварыВНаличии ~стр.555) и видят один и тот же остаток, например 10; каждая решает, что товара хватает, и списывает по 8. Итоговый остаток −6 вместо корректного отказа «недостаточно товара» у одной из них: отрицательные остатки, списание с несуществующих партий, искажённая себестоимость. То же касается регистра ДополнительныеРасходы, который входит в ту же блокировку (УправляемыеБлокировки.ТоварыВНаличии → ДополнительныеРасходы) и читается в СписатьНоменклатуру. Модуль УправляемыеБлокировки сам Заблокировать() не вызывает (прочитан целиком, 48 строк), СформироватьДвижения внутри блокировку не ставит — удалённая строка была единственным местом применения блокировки. Все остальные ~20 документов с этим паттерном (РеализацияТоваровУслуг, СписаниеТоваров, ПеремещениеТоваров, АктРазбора и т.д.) Заблокировать() по-прежнему вызывают — ВыпускПродукции становится единственным исключением. Прямое нарушение правила проекта «остатки читаются под управляемой блокировкой». Заодно строки «Блокировка = Новый БлокировкаДанных; УправляемыеБлокировки.ТоварыВНаличии(...)» превращаются в мёртвый код — признак того, что удаление случайное, а не осознанное решение.",
      "recommendation": "Вернуть строку «Блокировка.Заблокировать();» после УправляемыеБлокировки.ТоварыВНаличии(Блокировка, ТЧТовары). Если удаление было попыткой избежать взаимоблокировки — это не способ: нужно согласованное сужение/порядок блокировок во всех документах, а не снятие блокировки в одном."
    }
  ],
  "summary": "Дифф из одной строки, но она удаляет единственное применение управляемой блокировки в проведении ВыпускПродукции. Чтение остатков и списание становятся незащищёнными от гонки при параллельном проведении → отрицательные остатки и неверная себестоимость. P1, reject."
}
```
