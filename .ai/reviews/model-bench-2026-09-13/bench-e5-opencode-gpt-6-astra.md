_OpenCode: модель opencode/gpt-6-astra._

{
  "summary": "Проверены дифф и связанные обработчики, расчёт количества и существующий сценарий тестирования материалов заказа. Найдены два замечания P2: неполная очистка материалов при смене продукции вне основной формы и серверные вызовы в клиентском цикле пересчёта. Проверки на платформе 1С не запускались.",
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "file": "src/cf/src/Documents/ЗаказПокупателя/ObjectModule.bsl",
      "location": "ОчиститьМатериалыСменившейсяПродукции(), строки 248–257",
      "problem": "Серверная проверка определяет смену продукции только по заполненной техкарте. У вручную введённого состава техкарта пустая, поэтому изменение номенклатуры вне основной формы оставляет материалы прежней продукции. В форме ECommerce обработчик изменения номенклатуры материалы не очищает.",
      "scenario": "В основной форме создать заказ на производство с продукцией А, добавить материалы вручную без заполнения по техкарте и записать. Открыть этот заказ через форму ECommerce, заменить А на Б и записать. Код строки сохранится, проверка техкарты будет пропущена, и материалы продукции А останутся привязанными к продукции Б.",
      "recommended_fix": "Обрабатывать смену номенклатуры также в форме ECommerce; для серверной гарантии сравнивать номенклатуру сохраняемых строк с прежними значениями по коду строки независимо от наличия техкарты. Добавить тест смены продукции у вручную заполненного состава."
    },
    {
      "severity": "P2",
      "file": "src/cf/src/Documents/ЗаказПокупателя/Forms/ФормаДокумента/Module.bsl",
      "location": "ПересчитатьСтрокуМатериала(), строки 659–661; вызов из цикла ПересчитатьМатериалыСтроки()",
      "problem": "Клиентский пересчёт вызывает сервер для каждого материала с заполненной упаковкой. РассчитатьКоличество() обращается к ОбщегоНазначенияВызовСервера.РассчитатьКоэффициентЕдиницы(), а этот общий модуль серверный, с разрешённым вызовом сервера и без повторного использования возвращаемых значений.",
      "scenario": "При включённом учёте упаковок заполнить строку продукции составом из 100 материалов с упаковками и изменить количество продукции. Обработчик последовательно выполнит 100 серверных вызовов; при задержке сети 100 мс только обмен займёт около 10 секунд. Подбор и сканирование запускают пересчёт всех материалов заказа и усиливают задержку.",
      "recommended_fix": "Пересчитывать связанные материалы одним серверным вызовом либо заранее получать коэффициенты упаковок пакетом и выполнять клиентский цикл без обращений к серверу."
    }
  ]
}
