_OpenCode: модель opencode/glm-5.3._

Проверю ключевые риски этого диффа: форму (реквизиты `ЭтоЗаказНаПроизводство`/`РежимМатериалов`, элементы и обработчики должны быть объявлены в `Form.form`, которого в диффе нет), контекст `ПередЗаписью` (учёт `ОбменДанными.Загрузка`), существование вызываемых методов и тесты.
Форма уже содержит все реквизиты, элементы, команды и обработчики (правлена на предыдущем этапе задачи), `ПередЗаписью` корректно выходит при `ОбменДанными.Загрузка`, форматная строка `ЧН=0; ЧГ=0` — принятый в репо паттерн, тесты материалов заказа в `ТестыДокументов` есть. Осталось проверить существование и сигнатуры вызываемых методов общих модулей и прогнать refcheck.
Все вызываемые методы существуют, сигнатура `СообщитьПользователю` совместима со всеми вызовами (4-й параметр — `ПутьКДанным`, «Объект» — корректно), контракт структуры автоподбора (`Ссылка`) — тот же, что в существующем коде. Осталось: поведение `СоставТехкарт` при пустой дате (вызов из `ЗаполнитьПоОснованию` с незаполненной `Дата` нового заказа), refcheck и секция «Что нового».
`refcheck` заблокировался из-за `2>&1` — запущу точной разрешённой формой, и отдельно проверю обработку пустой даты в `ЗапросПоСоставу` и секцию «Что нового».
refcheck чист. Критичный момент: в `ЗапросПоСоставу` условие `Период <= &Дата` — если пустая дата не подставляется датой сеанса, заполнение по КП с незаполненной `Дата` нового заказа даст пустой состав. Проверю установку параметра и содержимое секции «Что нового».
Последнее сомнение — тип реквизита формы `РежимМатериалов`: код сравнивает `РежимМатериалов = 1`, что сломается, если реквизит булев.
Проверка завершена. Все ключевые риски большого диффа закрыты фактами: форма (правлена на предыдущем этапе задачи и уже в репо) содержит все реквизиты, элементы, команды и обработчики; `ПередЗаписью` выходит при `ОбменДанными.Загрузка`; refcheck чист; все вызываемые методы существуют с совместимыми сигнатурами; пустая дата при заполнении по КП подставляется датой сеанса внутри `ЗапросПоСоставу`; тесты материалов заказа в `ТестыДокументов` есть; секция «Что нового» 2.0.15.13 полная и соответствует коду; версия в `Configuration.mdo` совпадает.

```json
{
  "verdict": "approve",
  "summary": "Дифф добавляет табличную часть ТЧМатериалы и реквизит Техкарта в ЗаказПокупателя, согласование материалов со строками товаров перед записью, заполнение по техкартам (менеджер + команды формы) и заполнение при создании заказа по КП. Проверено: (1) Form.form — правлен на предыдущем этапе задачи и уже в репо: реквизиты РежимМатериалов (Число, precision 1 — сравнение «= 1» корректно) и ЭтоЗаказНаПроизводство, элементы ГруппаМатериалы/ТЧМатериалы/ТЧТоварыТехкарта/РежимМатериалов, команды ЗаполнитьМатериалыПоТехкарте и ЗаполнитьВсеМатериалыПоТехкартам, обработчики ПослеЗаписи, ТЧТоварыПриОкончанииРедактирования/ПослеУдаления/ПриАктивизацииСтроки, РежимМатериаловПриИзменении и все обработчики ТЧМатериалы — объявлены в handlers; (2) ПередЗаписью выходит при ОбменДанными.Загрузка до вызова СогласоватьМатериалы; (3) scripts/refcheck.py — расхождений нет; (4) все вызываемые методы существуют и сигнатуры совместимы: ТехнологическиеКарты.СоставТехкарт/КлючСоставаСтрок(Состав, ИмяПоляКоличества), РаботаСДокументамиКлиентСервер.РассчитатьКоличество/ПолучитьСледующийКодСтроки, ОбщегоНазначенияКлиентСервер.СообщитьПользователю(Текст, КлючДанных, Поле, ПутьКДанным, Отказ) — все вызовы диффа корректны, включая ПутьКДанным=\"Объект\" и Отказ в 5-м параметре; (5) при незаполненной дате нового заказа по КП ЗапросПоСоставу подставляет ТекущаяДатаСеанса() — комментарий менеджера соответствует коду; (6) сценарии материалов заказа есть в ТестыДокументов (заполнение по карте, неизменность чужих строк, заказ по КП, очистка у продажи, отказ при разном составе); (7) секция «## 2.0.15.13» в ОписаниеИзменений полная, на ru/fr/en/es, описывает именно этот функционал, версия Configuration.mdo совпадает; (8) форматная строка \"ЧН=0; ЧГ=0\" — повсеместный рабочий паттерн репо; (9) директивы компиляции явные, серверные вызовы не в цикле, запросы вне циклов, НСтр на 4 языках, типы в .mdo платформенные, строки ≤ 140, экспортные методы документированы, build/DT-INF/XML-выгрузки не тронуты; (10) повторное проведение/запись идемпотентны (ОбеспечитьКодыСтрокТоваров и СогласоватьМатериалы при повторном вызове ничего не меняют). Спорные моменты, отброшенные без находки: «НомерСтроки - 1» как индекс в ПроверитьСоставОднойПродукции — стандартный паттерн модуля объекта (в ПередЗаписью нумерация ТЧ объекта плотная); обращение ВыбранноеЗначение.Ссылка без проверки свойства — тот же контракт, что в существующем ОбрабокаВыбораПриАвтоподбореНоменклатуры; дублирование формулы пересчёта клиент/сервер — прокомментировано в коде как осознанное.",
  "findings": [],
  "notes": [
    "Форма ФормаДокумента и тесты ТестыДокументов не входят в этот дифф, но уже содержат полную привязку новых обработчиков/реквизитов/команд и сценарии материалов — правка многоэтапная, этот этап — модульная часть. Проверял по фактическому состоянию репо, не только по диффу.",
    "Синтаксическая проверка и ссылочная целостность метаданных — на стороне EDT: владельцу нужно сделать Refresh и проверку в EDT; автотесты (scripts/smoke.sh, ТестыДокументов) прогнать на обновлённой из EDT build/ib."
  ]
}
```
