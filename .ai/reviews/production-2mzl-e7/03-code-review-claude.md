# ai-review: код E7 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

`.ai/bin/ai-review production-2mzl-e7 --tier codex` 19:33Z: `codex exec` exit 1 — «You've hit your usage limit… try again
at 11:59 PM» (`.ai/state/production-2mzl-e7/review-20260913T193350Z.jsonl.stderr`). Гейт выполнен субагентом Claude
`general-purpose` (opus) с промптом `.ai/prompts/reviewer.md`.

Вердикт: **request_changes** (1×P1). Решения сверены с кодом главной сессией.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P1 | `ПределыПоОснованию`: знак сторно других возвратов перевёрнут — второй возврат по поступлению сторнирует исчерпанную продукцию, «Пришло» уходит в минус (шаг 6 сценария упал бы) | подтверждено (то же — ревью данных, P1); исправлено: для возврата прибавляется уже отрицательное «Пришло» других возвратов, для поступления вычитается нетто других поступлений и их возвратов |
| 2 | P2 | Связанные документы ищутся по неиндексированному `Основание` шапок поступлений и возвратов — полный просмотр под блокировкой | принято: связанные движения отбираются по индексированному `Заказ` регистра и типу/основанию регистратора, без `ИЛИ`, без двойного разыменования |
| 3 | P3 | Итог `КЗаказу` в отчёте на группировках гасит дефицит одного материала излишком другого | принято: итог — `Сумма(КЗаказу)` |
| 4 | P3 | В сценарии роль предварительного заказа играл основной заказ с основанием | принято: отдельный заказ `ВидЗаказа = Предварительный` без основания, проверка отсутствия его движений |
| 5 | P3 | «Создать на основании → Поступление» видна у продаж и даёт исключение | принято: TECHDEBT («Найдено в E7») |

## Круг --verify (2026-09-13) — субагент Claude + `.ai/prompts/reviewer-verify.md`, не Codex

Вердикт: **approve_with_changes**. Все 10 уникальных находок трёх ревью закрыты (расчёты шага 6, перепроведения
возврата и поступления с возвратом по другому поступлению сошлись). Новая P3: откат пределов поступления к остаткам
решался по непустой таблице пределов — после дельты её могли заполнить одни движения других поступлений, и при заказе
поставщику без своих движений материал терял «Пришло». **Исправлено:** откат решается по наличию движений самого
заказа поставщику по заказу клиента (`ЕстьДвиженияПоЗаказу`), та же функция — у `ЗаказФормируетПотребность`.
