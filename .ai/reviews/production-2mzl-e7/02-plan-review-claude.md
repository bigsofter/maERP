# ai-review-plan: план E7, редакция 2 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59; владелец 2026-09-13 решил начать сейчас и провести обязательное ревью плана субагентом
Claude `architecture-critic` (opus) с промптом `.ai/prompts/plan-critic.md` — по `.ai/README.md` («Цепочка бэкендов»),
не OpenCode. `.ai/bin/ai-review-plan` не запускался.

Вердикт: **reject** (1×P1), архитектурных переделок не требует. Все находки сверены с кодом главной сессией; решения —
план, редакция 3 (раздел 13).

| # | Sev | Находка | Решение |
|---|---|---|---|
| P1-1 | P1 | Колонка «Заказ клиента» списка поступлений разыменовывает `ВЫРАЗИТЬ(Док.Основание КАК Документ.ЗаказПоставщику).Основание`; у `Оператор` и `Логист` нет `Read` на `Документ.ЗаказПоставщику` (сверено скриптом) — список не откроется под ними | подтверждено; принято (а): колонка видна только при опции «Производство» и праве чтения заказа поставщику, скрыта на сервере (`Видимость`, `ПользовательскаяВидимость`), динамическое считывание не выбирает скрытое поле; в «посмотреть глазами» — список под `Логист` |
| P2-1 | P2 | Движения регистратора без отбора по заказу; смена основания проведённого заказа поставщику | принято: все запросы по регистратору с `Заказ = &Заказ` (в коде так и есть); при перепроведении блокируются и заказы из прежних движений документа |
| P2-2 | P2 | «Пришло других поступлений по заказу поставщику» без возвратов по ним — замена брака уходит на другую продукцию | принято: нетто с возвратами других поступлений (возвраты своего поступления не учитываются) |
| P2-3 | P2 | Движения других документов по неиндексированному `Заказ` | принято: `<indexing>Index</indexing>` у измерения `Заказ` нового регистра |
| P2-4 | P2 | `ЗаказФормируетПотребность` зависит от текущей опции «Производство» — перепроведение закупок при выключенной опции тихо стирает «Пришло» | принято: признак для закупок — наличие движений регистра заказа (`Регистратор = &Заказ И Заказ = &Заказ`) под блокировкой; опция проверяется только при вводе на основании |
| P2-5 | P2 | «Кнопка "Подобрать из предварительных" скрыта» — неверно, скрывать нельзя | подтверждено (строка видимости закомментирована); принято: кнопку не трогаем, формулировка плана исправлена |
| P3-1 | P3 | Шаг 6 сценария: какие строки остаются в возврате | принято: в возврате остаётся строка поступления «M1 без продукции» |
| P3-2 | P3 | Шаг 2: предварительный заказ не должен писать «Заказано» | принято: без основания; проверка нулевого «Заказано» после шага 2 |
| P3-3 | P3 | Шаг 8: материалы пересчитывает форма; ранний возврат в проверке | принято: пересчёт материалов процедурой модуля менеджера; сравнение потребности объединено с условием составов (так и реализовано) |
| P3-4 | P3 | Строки поступления по «предварительным» строкам итогового заказа | принято (реализовано: признак `ИзПредварительного` строки заказа поставщику) |
| P3-5 | P3 | E8: стадии 3–4 по строкам продукции застрянут из-за излишка на последней строке | принято: обязательство для E8 — стадии 3–4 по сумме материала и характеристики заказа |

Рекомендация ревьюера: повторить `ai-review-plan` на Codex по редакции 3, когда лимит восстановится.
