# ai-database: E7 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59 (см. `03-code-review-claude.md`). Гейт выполнен субагентом Claude `general-purpose` (opus) с
промптом `.ai/prompts/database.md`.

Вердикт: **request_changes** (1×P1). Метаданные (уникальность uuid, типы, индекс `Заказ`), registerRecords,
перепроведение и отмена, третий тип основания поступления, критерий `СвязанныеДокументы`, последовательность
`Основная`, взаимоблокировки — без замечаний.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P1 | Знак сторно других возвратов в `ПределыПоОснованию` | исправлено (см. `03`, находка 1) |
| 2 | P3 | Заказ клиента закупки читается дважды — до блокировки и после; при смене основания между чтениями движения ушли бы в незаблокированный заказ | принято: блокировка кладёт найденный заказ в `ДополнительныеСвойства`, движения пишутся по нему |
| 3 | P3 | Скан шапок документов по `Основание` при проведении поступлений и возвратов | принято (см. `03`, находка 2) |
