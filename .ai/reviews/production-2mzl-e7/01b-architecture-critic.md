# architecture-critic: план E7, редакция 1 (2026-09-13)

Субагент Claude `architecture-critic` (opus), внутрисессионный отсев до обязательного `ai-review-plan`.
Вердикт: **approve_with_changes**. Находки сверены с кодом главной сессией; решения — в плане, редакция 2 (раздел 12).

| # | Sev | Находка | Решение |
|---|---|---|---|
| P2-1 | P2 | Итоговый заказ поставщику из предварительных (`ТЧТовары.ЗаказПоставщику`): «Пришло» не пишется при пустом основании итогового, «Заказано» удваивается при заполненном | принято: строки с заполненным `ЗаказПоставщику` в движениях потребности не участвуют; поступление по итоговому заказу без основания — движений нет; кнопка подбора скрыта в форме — TECHDEBT |
| P2-2 | P2 | Сторно возврата по остатку всего заказа приписывает возврат не той продукции | принято: продукция и пределы сторно — из движений «Пришло» поступления-основания минус другие возвраты по нему; поступление по заказу поставщику распределяется по «Заказано» этого заказа минус «Пришло» других поступлений по нему |
| P2-3 | P2 | Строка без продукции раньше строки с явной продукцией того же материала съедает её дефицит | принято: два прохода — сначала явная продукция, затем распределение |
| P2-4 | P2 | Запрос к таблице движений по неиндексированному `Заказ` — полный просмотр регистра | принято: `Остатки(, Заказ = &Заказ)` минус собственные движения по индексу регистратора |
| P2-5 | P2 | Сценарий: порядок отмены проведения и проведённое поступление по заказу клиента ломают ожидания шагов 5–7 | принято: поступление по заказу клиента — только заполнение; отмены в обратном порядке, заказ клиента последним |
| P2-6 | P2 | Исключение при вводе на основании заказа «Производство» при выключенной опции — регресс | принято: без опции — прежнее поведение (состав товаров) |
| P3-1 | P3 | Проверка `Проведен` до блокировки регистра | принято: заказ по цепочке → блокировка → проверка проведения и вида; отмена проведения заказа клиента регистр не блокирует — TECHDEBT |
| P3-2 | P3 | Двусмысленный знак предела сторно | принято, формулировка исправлена |
| P3-3 | P3 | Излишек уходит на продукцию с `Потребность = 0` | принято: в распределении только строки с `Потребность > 0` |
| P3-4 | P3 | Логика заполнения описана дважды | принято: одна экспортная процедура модуля менеджера заказа поставщику над коллекцией строк |
| P3-5 | P3 | Поступление по заказу клиента — строки по продукции без колонки | принято: свёртка по материалу и характеристике |
| P3-6 | P3 | Фоновое перепроведение последовательности включает поступления и возвраты | принято: TECHDEBT |
| P3-7 | P3 | Смена коэффициента упаковки даёт отказ записи проведённого заказа с непонятным сообщением | принято: TECHDEBT (коэффициент сейчас всегда 1) |
| P3-8 | P3 | Отчёт недоступен разделом `МенеджерПоЗакупкам`; поля условия `ТолькоДефицит` | принято: отчёт и в `Subsystems/Закупки`; условие на `ПотребностьОстаток − ЗаказаноОстаток` |

Проверено критиком без проблем: исключение своего регистратора при перепроведении (движения удаляются только при отмене
проведения), `ФормированиеЗаказовПоставщику` и `СоздатьПоступленияНаСервере` движений не дадут, новый тип основания
поступления не ломает `ВзаиморасчётыСПоставщиками`, `ДвиженияСвязанныхДокументов`, `ТоварыВНаличии`; порядок блокировок
без цикла.
