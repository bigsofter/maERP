# ai-security: E7 (2026-09-13) — **гейт на субагенте Claude, не на Codex**

Codex в лимите до 23:59 (см. `03-code-review-claude.md`). Гейт выполнен субагентом Claude `general-purpose` (opus) с
промптом `.ai/prompts/security.md`.

Вердикт: **approve_with_changes**, P0/P1 нет. Права сверены скриптом: чтение регистра есть у всех ролей, вводящих
закупки; роли, проводящие заказы клиента, регистр не читают; отчёт — только ролям с чтением заказов клиентов;
привилегированное проведение читает только основание и собственные движения.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P2 | Защита колонки «Заказ клиента» держится на точном совпадении подстроки `СтрЗаменить` — переформатированный запрос молча оставит соединение и уронит список у `Логист`/`Оператор` | принято: выражение ищется в тексте запроса под любой ролью (в том числе в смоке под администратором), нет — исключение разработчику |
| 2 | P3 | `getInvisibleFieldPresentations = true`: скрытая колонка выражение не отключает, формулировка плана неверна | принято: план (раздел 13) и TECHDEBT исправлены; защита — по праву, не по видимости |
| 3 | P3 | `View` на регистр у ролей без отчёта (`Казначей`, `Логист`, `Оператор`) — лишнее | принято: оставлен только `Read` |
| 4 | P3 | Организация закупки не сверяется с организацией заказа клиента | принято: TECHDEBT (разграничения по организациям нет) |
