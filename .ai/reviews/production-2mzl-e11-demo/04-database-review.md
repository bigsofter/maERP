_OpenCode: модель opencode/gpt-6-astra._

{
  "summary": "Проверены изменения генератора демо-данных, транзакционная граница и использование общих помощников тестов. Изменений структуры хранения, типов и UUID нет; найден дефект защиты от повторного создания при конкурентном запуске. Проверка статическая: код и тесты не запускались, обращения к базам не выполнялись.",
  "verdict": "approve_with_changes",
  "findings": [
    {
      "severity": "P2",
      "file": "src/cf/src/DataProcessors/ТестовыеДанные/ManagerModule.bsl",
      "location": "СоздатьДемоНабор: проверка МаркерДемоНабора до НачатьТранзакцию",
      "problem": "Проверка существования набора выполняется вне транзакции и не повторяется после получения блокировки. Два запуска могут оба увидеть отсутствие маркера и последовательно записать два полных набора. Запись константы внутри ЗаполнитьДемоНабор может сериализовать выполнение, но не устраняет уже принятое решение о создании.",
      "scenario": "В тестовой базе без демо-набора два сеанса запускают заполнение почти одновременно. Оба проходят проверку отсутствия контрагента-маркера. Первый фиксирует набор; второй после ожидания блокировок продолжает создание без повторной проверки. В базе остаются дубли справочников и проведённых документов, удваивающие демо-обороты; последующие запуски сообщают только «уже заведён».",
      "recommended_fix": "Внутри транзакции получить общую для генератора исключительную управляемую блокировку, затем повторно проверить маркер и только при его отсутствии создавать набор. Блокировка должна удерживаться до фиксации или отката; раннюю проверку можно оставить лишь как оптимизацию."
    }
  ]
}

## Разбор автором (2026-09-14)

Exit 3 засчитан пройденным гейтом по правилу владельца (модель `opencode/gpt-6-astra`); дополнительно — субагент Claude
(`04-database-review-claude.md`). P2 «повторная проверка маркера» — принята: внутри транзакции исключительная управляемая
блокировка константы автовыпуска и повторная проверка маркера; при найденном маркере — `ЕСТЬ;…;заведён параллельным запуском`.
