_Субагент Claude `architecture-critic` (Opus), 2026-09-14, план редакции 1._

# Вердикт: approve_with_changes

- **P1 — неверная стадия P9**: автовыпуск при реализации 4 из 6 выпускает 4 → «В производстве ●●◐◐○»
  (`ПроизводственныеЗаказы/Module.bsl:966-995`, прецедент `ТестыДокументов:2871`). Либо переименовать, либо ручной выпуск 6
  до реализации 4 → «Ожидает отгрузки ●●●◐○». Убрать «фиксируются по первому прогону».
- **P1 — ФО «Производство» в транзакции**: значения ФО кэшируются в сеансе (`ЗаказПокупателя/ManagerModule.bsl:201-205`);
  в режиме проверки ФО не трогать (ПРОПУСК), в заполнении — до транзакции со сбросом кэша; константу автовыпуска можно в
  транзакции (`ТестыДокументов:2780`).
- **P2 — P4 «без техкарты»**: проведение регистрирует карту (`ЗаказПокупателя/ObjectModule.bsl:596-603, 932-985`), нужны
  права на техкарты (`:896`) — переименовать, запуск под администратором.
- **P2 — помощники тестов не экспортные** и завязаны на контекст — решить явно; экспортировать ~10 помощников проще, чем
  копировать ~400 строк.
- **P2 — маркер набора**: код контрагента уникален и длиной 9 (`Контрагенты.mdo:150,154`) — искать по наименованию, код не
  задавать; помеченный маркер — тоже «набор есть»; частичный набор — возврат базы.
- **P2 — форма ждёт `Итог.Было`** (`СмокТест/Forms/Форма/Module.bsl:210-212`) — вернуть `Было` или отдельный ИТОГ; `Кейсы`
  клиенту не отдавать.
- **P3 — «Отгружен» достижим** (`РаботаСДокументами/Module.bsl:560-630`): выпуск по заказу-продаже после более поздней
  реализации; недостижим только «Доставлен».
- **P3 — переработка «если»**: решить; контрагент-субподрядчик (`ТестыДокументов:4027`).
- **P3 — полночь**: `Макс(НачалоДня, сейчас − запас)`, при нехватке секунд — ОШИБКА.
- **P3 — P11 строго последним** (`ДатаРедактированияТехкарты`, `ЗаказПокупателя/ObjectModule.bsl:884`).
- **P3 — материалы заказов явно** (`ЗаказПокупателя/ObjectModule.bsl:677`, метод менеджера `:116`); Lampe — «Закупка».
- **P3 — количества на складе** с запасом, числами в плане.
- **P3 — ошибки в транзакции**: к базе не обращаться до `ОтменитьТранзакцию`, причины — `ПолучитьСообщенияПользователю`.
- **P3 — подсчёт документов** по объектам прогона.
- **P3 — мелочи**: договоры — `Описание` (bsl-008); `ПервыйЭлемент` без упорядочивания; bsl-011; НСтр ×4.

Проверено без расхождений: P1 «Выполнен ●●●●●», P2 «Ожидает оплаты ●●●●◐» (`ТестыДокументов:3316`), P3 «Ожидает отгрузки
●●●○○», P5, P6, P7 «Формируется потребность ○○○○◐» (`:2982-2986`), P8; «Собран» и «В производстве» у заказа-продажи
достижимы, основание-заказ у ручного выпуска допустимо.

## Разбор автором

Все находки приняты — редакция 2 плана, §9.
