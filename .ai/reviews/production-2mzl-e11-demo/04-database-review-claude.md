_Субагент Claude general-purpose, ai-database (временная замена Codex), 2026-09-14._

# Вердикт: approve_with_changes

Блокирующих находок нет; метаданные не менялись, реструктуризации и риска для баз клиентов нет; генератор пишет документы
штатными путями.

- **P3-1** `ТестовыеДанные/ManagerModule.bsl:210` — константа автовыпуска пишется всегда и держит блокировку до конца
  транзакции; писать только при `Ложь`.
- **P3-2** `:188, :234, :1632` — запуск около полуночи: документы вчерашним днём, фиксация после 00:00, P11 «завтра»
  становится действующей; проверять `НачалоДня(ТекущаяДатаСеанса()) = НачалоДня(Начало)` в конце.
- **P3-3** `ТестыДокументов/ManagerModule.bsl:3929-3930, :4028` — лиды и заявки считаются по суффиксу во всей базе, а не по
  объектам прогона; считать по контрагентам и именам набора.
- **P3-4** процесс — прогон на копии с частичной загрузкой не заменяет гейт на `build/ib`: после обновления — смок и 25
  сценариев → `fixtures.sh --demo` ×2 → снова сценарии и смок на заполненной базе.

Проверено без находок: откат в `Исключение` первым оператором, очередь сообщений, вложенная транзакция техкарт, ФО до
транзакции со сбросом кэша, автовыпуск читает константу напрямую; маркер (в том числе помеченный), одна транзакция, длины
наименований, шаг 2 с, запас 900 с, P11 последним; экспорт помощников не меняет прежние сценарии; сценарий в транзакции
прогона, суффикс разводит справочники; прямо пишутся только ручные поля; `Лиды` — все поля измерения; набор не создаёт
организаций, валют, банков; после включения автовыпуска прежние сценарии не задевает (`ПоказателиТехкарты` без дефицита,
`СтадииЗаказа` и `АвтовыпускПриОтгрузке` ставят константу сами); реквизиты и значения перечислений существуют;
`fixtures.sh` без `--demo` не изменился.

## Разбор автором

Все четыре приняты: P3-1–P3-3 — правки кода (круг `--verify`), P3-4 — порядок проверки после обновления базы.
