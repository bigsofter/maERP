_OpenCode: запасная модель opencode/big-pickle — основная opencode-go/kimi-k3 не ответила (лимит, зависание или ошибка)._







Анализ завершён. Проверил ключевые точки: сигнатуру `УстановитьЗаголовокФормыДокумента` (третий параметр `Заголовок = ""` существует — вызов корректен), `УстановитьОтборПоПолю` (с `Неопределено` снимает отбор), паттерн обработчика обновления против выпущенного E2 (2.0.15.7, тот же `= ЗНАЧЕНИЕ(...ПустаяСсылка)` в WHERE для того же сценария реструктуризации), mainTable списка и наличие колонки в запросе, права ролей на подсистему «Производство».

```json
{
  "verdict": "approve_with_changes",
  "summary": "Новый реквизит ВидОперации документа ЗаказПокупателя с перечислением ВидыОперацийЗаказаПокупателя: реструктуризация безопасна (новый реквизит, существующие записи получают NULL и заполняются обработчиком обновления «Продажа»), типы в .mdo корректны (EnumRef, дат нет), uuid новых объектов новые, проведение и транзакции не затронуты, обработчик обновления идемпотентен и повторяет проверенный паттерн E2, запросы без циклов, колонка и фильтр списка согласованы с запросом и счётчиком страниц. P0/P1 не найдено. Находки: отсутствие индекса по новому реквизиту отбора, непоследовательные права на команду, фильтр «Продажа» не включает NULL, монопольный обработчик без порций.",
  "findings": [
    {
      "severity": "P2",
      "title": "Новый реквизит ВидОперации не индексирован, хотя используется для отбора",
      "file": "src/cf/src/Documents/ЗаказПокупателя/ЗаказПокупателя.mdo",
      "detail": "В .mdo реквизита нет <indexing>. ВидОперации отбирается в трёх местах: тумблер «Операция» динамического списка (Равно/НеРавно), условие счётчика страниц (Док.ВидОперации = / <> &ВидОперацииПроизводство) и запрос обработчика обновления (ГДЕ ВидОперации = ПустаяСсылка). На тиражной базе с сотнями тысяч заказов фильтр списка и запрос обработчика — полное сканирование таблицы документа.",
      "scenario": "Клиент с 200 тыс. заказов открывает общий список и ставит тумблер «Операция» на «Продажа»: динамический список сканирует таблицу документов без индекса по ВидОперации; на слабом сервере отрисовка списка заметно деградирует."
    },
    {
      "severity": "P2",
      "title": "Права на команду «ЗаказыНаПроизводство» выданы ролям без подсистемы «Производство», а роли с подсистемой — нет",
      "file": "src/cf/src/Roles/Кладовщик/Rights.rights",
      "detail": "View на команду добавлен МенеджерПоПродажам и Оператор, но у этих ролей нет прав на подсистему «Производство» (команда размещена только в её командном интерфейсе) — грант не работает. Кладовщик, у которого подсистема «Производство» есть, права на команду не получил: видит раздел без команды. Фактически команду видит только ОператорПроизводства.",
      "scenario": "Пользователь с ролью Кладовщик открывает раздел «Производство»: команда «Заказы клиентов на производство» отсутствует, хотя раздел доступен; у МенеджерПоПродажам и Оператора команда не видна вовсе, несмотря на выданное право."
    },
    {
      "severity": "P3",
      "title": "Фильтр «Продажа» (ВидОперации <> Производство) не включает заказы с пустым видом операции",
      "file": "src/cf/src/Documents/ЗаказПокупателя/Forms/ФормаСписка/Module.bsl",
      "detail": "Комментарий утверждает: «пустой вид операции у заказов, записанных в обход страховки ПередЗаписью, тоже считается продажей». Но в языке запросов NULL <> Производство даёт NULL — такие записи из условия выпадают. Пустой вид операции возможен у заказов, записанных с ОбменДанными.Загрузка = Истина (страховка ПередЗаписью пропускается), например при обмене данными с базой без этого реквизита. То же условие в ДобавитьУсловиеВидаОперации — счётчик и список согласованы между собой, но оба противоречат заявленному поведению.",
      "scenario": "Заказ, загруженный обменом данных с пустым видом операции, не виден в списке с тумблером «Продажа» (и не учтён счётчиком страниц), хотя виден в «Все» — пользователь решает, что заказ потерялся."
    },
    {
      "severity": "P3",
      "title": "Обработчик обновления заполняет заказы по одному в монопольном режиме без порций",
      "file": "src/cf/src/CommonModules/ОбновлениеПрограммыОбработчики/Module.bsl",
      "detail": "ЗаполнитьВидОперацииЗаказовПокупателей пишет каждый документ отдельной транзакцией (атомарно, повторный запуск идемпотентен — это плюс), но в РежимМонопольно цикл ПолучитьОбъект+Записать по всем заказам блокирует пользователей на всё время. Паттерн совпадает с выпущенным обработчиком E2 (2.0.15.7); добавлено сообщение в журнал о количестве к заполнению. На очень больших базах стоит рассмотреть порционную обработку с сохранением прогресса.",
      "scenario": "Клиент с 40 тыс. заказов обновляется на 2.0.15.9: монопольный режим держит пользователей вне базы десятки минут, пока каждый заказ читается и перезаписывается."
    }
  ],
  "verified_ok": [
    "Реструктуризация безопасна: новый реквизит и новое перечисление, существующие данные не теряются; обработчик обновления заполняет старые заказы «Продажа» (запрос = ЗНАЧЕНИЕ(...ПустаяСсылка) находит NULL — тот же проверенный паттерн, что в E2 2.0.15.7, и покрыт тестом СценарийВидОперацииЗаказа)",
    "Типы в .mdo корректны: EnumRef.ВидыОперацийЗаказаПокупателя, значений перечисления в XML СКД нет, дат нет",
    "uuid новых объектов (перечисление, команда, реквизит) новые; uuid существующих объектов не менялись",
    "Проведение не затронуто; запись обработчиком с Загрузка = Истина не трогает итоги, историю статусов и движения (Записать без Проведения)",
    "Обработчик обновления идемпотентен, не падает на документах до появления реквизита, помеченные на удаление тоже заполняются",
    "Запросы без циклов: проверка КП по производству — один запрос с ВНУТРЕННЕЕ СОЕДИНЕНИЕМ, параметр-массив для В ()",
    "Динамический список: колонка ВидОперации добавлена в произвольный запрос, mainTable = Document.ЗаказПокупателя; счётчик страниц учитывает тот же отбор",
    "УстановитьЗаголовокФормыДокумента принимает третий параметр Заголовок = \"\" — вызов корректен",
    "Обработчики представления клиент-безопасны (НСтр/Формат/СтрШаблон/ПредопределенноеЗначение), пустой вид операции даёт стандартное представление"
  ]
}
```
