# 04b — разбор данных субагентом Claude (general-purpose, opus), дифф E4

Причина: `ai-database` трижды не прошёл на своих бэкендах 2026-09-13 — OpenCode `database` (Go/kimi-k3)
зависал без единого события до таймаута 600 с (исчерпан пятичасовой лимит подписки Go), Codex —
`usage limit … try again at 6:58 PM`. Это запасной путь, не замена обязательного гейта.

Вердикт: **approve**, 3 × P3. Все сверены с кодом.

| # | Находка | Решение |
|---|---|---|
| DB-E4-1 | Объектная запись каждого заказа в монопольном режиме, версия в истории данных у каждого заказа, прогресса в журнале нет | принято частично: число заказов пишется в журнал до цикла; про версии в истории и длительность — в отчёт владельцу |
| DB-E4-2 | Заказы, загруженные после 2.0.15.9 в режиме загрузки, останутся с пустым видом | принято: шаг в `docs/MERGE-DRISSOTEX.md` (повторный вызов обработчика), инвариант «продажа = не производство» уже в плане волны |
| DB-E4-3 | Тумблер синхронизируется только с отбором «Производство» | принято: ограничение записано комментарием в `НастроитьВидОперации` |

Проверено и в порядке: 10 новых uuid/typeId уникальны, существующие uuid не изменены, порядок версий
обработчиков, отсутствие движений (posting = Deny), подписки при загрузке, согласованность отбора
динсписка и счётчика страниц, откат тестовых данных.
