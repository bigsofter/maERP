# 01-explore — E4, вид операции заказа клиента

`ai-explore` (OpenCode `explorer`, kimi-k3) — **BACKEND_UNAVAILABLE**: `opencode run timed out after 600s`
(2026-09-13). Фоллбэк — субагент Claude `repository-explorer`, суженные вопросы. Факты сверены выборочно.

## Места создания и записи `ЗаказПокупателя`

- `InformationRegisters/Лиды/Forms/ФормаЗаписи/Module.bsl:215–226` — `СоздатьДокумент()` →
  `ЗаполнитьРеквизитыПоУмолчанию` → `Записать()`; без `Заполнить()`, без режима загрузки.
- `InformationRegisters/Лиды/Forms/ФормаЗаписи/Module.bsl:184` — `ПолучитьОбъект()`, `Доставлен = Истина`, запись.
- `DataProcessors/ЛичныйКабинетПартнера/Forms/ФормаНоменклатура/Module.bsl:223–244` — `СоздатьДокумент()`,
  поля вручную, `Записать(Запись)`; без `Заполнить()`, без режима загрузки.
- `DataProcessors/ТестовыеДанные/ManagerModule.bsl:1095, 1120, 1757` — `НовыйДокумент(Контекст, "ЗаказПокупателя")`;
  `:719` — первый существующий заказ для записи регистра `Лиды`.
- HTTP-сервисов, веб-сервисов и обмена с хабом, создающих заказы, не найдено.

## Запросы по `Документ.ЗаказПокупателя` (для E7/E8/E12)

- `DataProcessors/АРМПроизводство/Forms/Форма/Module.bsl:83–102` (`ДСЗаказы`), `:109–117` (`ДСТоварыЗаказа`).
- `DataProcessors/Дашборд/ManagerModule.bsl:791–830` — пакет по статусам заказов.
- `CommonModules/ОбновлениеПрограммыОбработчики/Module.bsl:237–245, 387–420` — прежние обработчики.

## drissotex

`build/drissotex/Documents/ЗаказПокупателя.xml` — реквизита вида операции нет. Новый реквизит — новый uuid.

## Представление заказа как строка

Только отображение (`ТестовыеДанные:735`, `ПредложениеПоставщика/ФормаСписка:435`,
`ЛичныйКабинетПартнера/ФормаИстории:50`); разбора представления нет.
