_OpenCode: модель opencode/gpt-6-astra (временная замена Codex)._

{
  "verdict": "reject",
  "summary": "Из 13 перечисленных замечаний 12 закрыты (fixed): закрытый период — МожноИзменитьАвтовыпуск и передача Отказ, материалы — ОстаткиМатериалов/СписатьИзОстатков, исходный сценарий A/B — ДобавитьСтрокиПродукции, отчёт — вычитание ИзОстатка, блокировки — единая ветка остатков, остальные защиты — фильтр склада, проверка значения свойства, ТранзакцияАктивна и ПередУдалением; 1 замечание о МоментВремени отклонено автором, код по нему не изменён. Обнаружен 1 новый дефект P1 в учёте прошлых отгрузок по сериям; проверка статическая, без выполнения на платформе.",
  "findings": [
    {
      "severity": "P1",
      "title": "Новый расчёт по сериям не разворачивает серии предыдущих отгрузок",
      "file": "src/cf/src/CommonModules/Производство/Module.bsl",
      "location": {
        "start_line": 1086,
        "end_line": 1097
      },
      "scenario": "Заказано 4 единицы продукции, вручную выпущены A × 1 и B × 1. Первая реализация отгрузила обе единицы одной строкой с признаком УказаноНесколькоСерий и распределением A/B в ТЧСерии. Во второй реализации снова отгружаются A × 1 и B × 1; материалов достаточно для оставшихся двух единиц заказа. Расчёт ошибочно считает прежние A/B свободными, не создаёт нужный автовыпуск, и реализация отказывается из-за отсутствия продукции.",
      "description": "ВыпущеноИОтгруженоРанее теперь группирует историю по серии, но берёт ТоварыРеализаций.Серия непосредственно из ТЧТовары. При УказаноНесколькоСерий фактически списанные серии и их количества находятся в ТЧСерии: действующая ОбъединитьТоварыССериями разворачивает их по КодСтроки с пересчётом количества в базовые единицы. Поэтому историческая отгрузка не уменьшает свободное количество соответствующих серий, хотя текущая реализация разворачивается правильно. Аналогичное прямое чтение серии добавлено для возвратов.",
      "recommendation": "В запросе истории разворачивать строки реализаций и возвратов с УказаноНесколькоСерий через ТЧСерии, сохраняя правила пересчёта количества действующего списания; обычные строки учитывать без разворачивания. Добавить регрессионный сценарий двух последовательных отгрузок, где первая использует несколько серий в одной строке."
    }
  ]
}
