# architecture-critic: план E5, редакция 1 (2026-09-13)

Субагент Claude `architecture-critic` (opus), внутрисессионный отсев до внешнего ревью.
Вердикт: **approve_with_changes**. Все находки сверены с кодом главной сессией; решения — в плане,
редакция 2.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P1 | `ТЧМатериалы.Количество` в единицах упаковки расходится с соглашением документов (`КоличествоУпаковок` / базовое `Количество`); имя `КоличествоНаЕдиницу` ≠ `КоличествоЗаЕдиницу` выпуска | принято: колонки `КоличествоЗаЕдиницу`, `КоличествоУпаковок`, `Количество`; основа — базовое количество продукции, обязательство для E9 |
| 2 | P1 | Заполнение текущей строки по коду ломается на нулевых и повторных кодах (нормализация до отбора) | принято: в общую процедуру передаются строки коллекции, а не коды |
| 3 | P1 | Копирование строки товаров + Esc оставляет материалы с кодом, который получит следующая строка | принято: очистка материалов нового кода в `ПриНачалеРедактирования`, сирот — в `ПриОкончанииРедактирования` с отменой |
| 4 | P2 | Автоподбор меняет номенклатуру без `ПриИзменении` | принято: очистка и в `ТЧТоварыНоменклатураОбработкаВыбора` |
| 5 | P2 | Отбор строк не применяется при открытии, смене вида, после записи, при добавлении | принято: `ПрименитьОтборМатериалов()` во всех точках |
| 6 | P2 | Клиентские обработчики меняют поведение продаж | принято: реквизит формы `ЭтоЗаказНаПроизводство`, ранний выход |
| 7 | P2 | Общий код над двумя видами коллекций; `СообщитьПользователю` безусловно ставит `Отказ` | принято: подмножество методов, сообщения без `Отказ`, ключ данных для объекта |
| 8 | P3 | Коды из КП/реализации копируются, а не нулевые; модуль менеджера не только серверный | принято: поправлено исследование, область `#Если Сервер …` |
| 9 | P3 | Блок E5 при уже установленном `Отказ` | принято: пропуск |
| 10 | P3 | Нормализация нулевых кодов меняет перезаполнение цен реализации по заказу | принято: в `ПередЗаписью` только повторы, на которые ссылаются материалы |
| 11 | P3 | Добавление материала в режиме «все» без текущей строки даёт код 0 и молчаливое удаление | принято: отказ в обоих режимах |
| 12 | P3 | Выключенная опция «Производство» — элементы формы | принято: `Элементы.Найти` |
| 13 | P3 | Две растягиваемые таблицы | принято: в «что посмотреть глазами» |
| 14 | P3 | Пробелы тестов (упаковка ≠1, коды 0/повтор, характеристики, загрузка) | принято, кроме коэффициента: `РассчитатьКоэффициентЕдиницы` всегда 1 |
| 15 | P3 | Смена даты и копирование заказа не перезаполняют материалы | принято как известное поведение, сказать владельцу |
| 16 | P3 | Пустой материал без `fillChecking` | принято: `fillChecking = ShowError` |
| 17 | P3 | E6 должен сравнивать составы, а не ссылки (`ЗаписатьТехкарту` перезаписывает день) | заметка в `production-2mzl.md` |
| 18 | P3 | Refresh в EDT перед правкой Form.form | в отчёте владельцу |
