# Ревью плана E5: бэкенд и решения по находкам (2026-09-13)

**Бэкенд.** Обязательный гейт `ai-review-plan` (Codex) не прошёл: `You've hit your usage limit … try
again at 6:58 PM` (`.ai/state/production-2mzl-e5/plan-review-*.jsonl`). По правилу владельца — цепочка
OpenCode (`--tier free`): первая попытка — Go завис (сторож 90 с), Zen `big-pickle` вышел по таймауту
600 с без отчёта; вторая, на редакции 2 плана, — **OpenCode Go `kimi-k3`**, тот же промпт
`plan-critic.md`, отчёт `02-plan-review-free.md`. Гейт прошёл **не на Codex**.

Вердикт ревьюера: approve_with_changes, P0/P1 нет.

| # | Sev | Находка | Решение |
|---|---|---|---|
| 1 | P2 | Копия `КлючСостава` по полю `Количество` дала бы ложный отказ строкам одной продукции с разным количеством | принято: `КлючСостава` экспортная с `ИмяПоляКоличества`, сравнивается `КоличествоЗаЕдиницу`; тест с разными количествами |
| 2 | P2 | Двусмысленный «только просмотр» колонок количества | принято: `КоличествоУпаковок` и `Количество` обе только просмотр |
| 3 | P2 | «Заполнить все» затирает ручные правки без вопроса | принято: `ПоказатьВопрос` при непустых материалах |
| 4 | P3 | ECommerce-форма: замена номенклатуры не чистит ручные материалы | отклонено для кода: бриф запрещает менять ECommerce-форму; риск в плане и `docs/TECHDEBT.md` |
| 5 | P3 | Копия материала в режиме «все» привязывается к чужой продукции | принято: копия получает код текущей строки товаров |
| 6 | P3 | Путь сообщения из формы — `ПутьКДанным`, а не `Поле` | принято |
| 7 | P3 | Нет записи продажи при выключенной опции «Производство» | отклонено для теста: функциональная опция не ограничивает доступ кода к данным (прецедент `ВидОперации` E4 в `ПередЗаписью`); пункт в «что посмотреть глазами» при выключенной опции |
| 8 | P3 | «В модуле менеджера нет клиентского обработчика представления» | **rejected**: `ОбработкаПолученияПредставления` есть, `ManagerModule.bsl:280`; директива остаётся |
| 9 | P3 | Сравнение по номенклатуре+характеристике противоречит модели карты по номенклатуре | принято: продукция = номенклатура без характеристики |
