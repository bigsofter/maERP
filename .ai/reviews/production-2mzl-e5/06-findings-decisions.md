# E5: решения по находкам ревью кода (03) и данных (04), 2026-09-13

| Источник | Sev | Находка | Решение |
|---|---|---|---|
| 03 P2-1 | P2 | Смена номенклатуры + Esc: материалы уже удалены и не возвращаются; количество после Esc | **fixed**: номенклатура запоминается в начале редактирования, материалы очищаются в `ПриОкончанииРедактирования` только при принятом редактировании; после окончания (и отмены) — отложенное согласование: сироты, пересчёт строки, отбор |
| 03 P2-2 | P2 | `fillChecking` материала блокирует запись заказа, переведённого в продажу, по скрытой таблице | **fixed**: `ОбработкаПроверкиЗаполнения` снимает `ТЧМатериалы.Номенклатура` у заказа не на производство и при выключенной опции |
| 03 P2-3 | P2 | Пересчёт после подбора/штрихкода — вызов сервера на каждый материал | **fixed**: один серверный вызов `ПересчитатьВсеМатериалыНаСервере` |
| 03 P3-4, 04 P3-4 | P3 | «Заполнить все» переномеровывает коды строк без карты (перезаполнение цен реализации); разное правило повторного кода в `ПередНачаломДобавления` | **fixed**: коды нормализуются только у строк, получающих материалы; у строк без карты материалы удаляются лишь по коду, принадлежащему одной строке; в форме повторный код остаётся у первой строки |
| 03 P3-5 | P3 | Ручной материал без упаковки даёт ложный отказ «разный состав» при опции упаковки | **fixed**: ручной материал получает единицу номенклатуры при опции «Использовать упаковку» |
| 03 P3-6 | P3 | Колонка `Количество` скрыта (`userVisible=false`) вопреки плану; `ТЧТоварыПослеУдаления` без раннего выхода | колонка — осознанно (коэффициент упаковки сейчас 1, дублирует `КоличествоУпаковок`, включается «Изменить форму»); ранний выход **fixed** |
| 03 P3-7 | P3 | Три строки `НСтр` длиннее 140 при табуляции = 4 | rejected: BSL LS считает по правилу проекта, замечаний нет |
| 03 P3-8 | P3 | «Что нового»: сообщение о позиции без карты — не для всех случаев | **fixed** на четырёх языках: «производимая позиция» |
| 04 P3-1 | P3 | Материалы строки со сменившейся продукцией удаляются при записи без сообщения | **fixed**: сообщение по строке; удаление материалов удалённой строки — без сообщения (строку удалил пользователь) |
| 04 P3-2 | P3 | Квадратичная сложность: код материала и `НайтиСтроки` в проверке состава | **fixed**: код материала считается один раз на строку; материалы группируются по коду один раз |
| 04 P3-3 | P3 | Отрицательный расход обнуляется в неотрицательном `КоличествоУпаковок` | отложено: тот же тип у выпуска; запись в `docs/TECHDEBT.md` |
| 04 P3-5 | P3 | У `ТЧТовары.Техкарта` нет параметров выбора | rejected: колонка в форме только для просмотра, заполняется кодом |

Уровни 1–3 после фиксов нужно повторить: изменились модули объекта, менеджера, формы и сценарий
`МатериалыЗаказа` — после Refresh и обновления `build/ib` владельцем.

## Круг Codex (2026-09-13 18:59, `03-code-review.json`, после исправлений выше)

Бэкенд — Codex CLI по подписке, полный круг. Вердикт approve_with_changes, три P2, все подтверждены по коду.

| # | Sev | Находка | Решение |
|---|---|---|---|
| C1 | P2 | Копия строки товаров со сменой продукции оставляет скопированные ручные материалы (проверка исключала новую строку) | **fixed**: номенклатура запоминается и у копии; очистка при принятом редактировании любой строки |
| C2 | P2 | Пересчёт материалов строки на клиенте вызывает сервер на каждый материал с упаковкой | **fixed**: без упаковок — клиент без сервера, с упаковками — один серверный вызов |
| C3 | P2 | Колонка характеристики материалов не входит в опцию `ИспользоватьХарактеристикиНоменклатуры` | **fixed**: `ТЧМатериалы.Характеристика` добавлена в состав опции |

`ai-review --verify --tier codex` не прошёл: Codex снова в лимите до 23:59 (полный круг израсходовал окно).
Верификация — субагент Claude, `03b-code-review-verify-claude.md`.
