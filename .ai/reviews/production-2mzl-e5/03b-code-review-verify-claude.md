# ai-review --verify (фоллбэк), E5 — субагент Claude, 2026-09-13

Бэкенд: Codex в лимите до 23:59 (полный круг 18:59 израсходовал окно). Верификация по промпту обёртки
`.ai/state/production-2mzl-e5/review-verify-prompt.md` (дельта с прошлого ревью + находки Codex).
**Гейт `--verify --tier codex` прошёл не на своём бэкенде.** Итог: approve_with_changes.

| Находка Codex | Статус |
|---|---|
| C1 копия строки со сменой продукции | closed (`Module.bsl` — условие `(Не НоваяСтрока Или Копирование)`, очистка при принятом редактировании; Esc, строка без смены, пустая строка, отказ кратности — корректно) |
| C2 сервер на каждый материал | closed (один контекстный вызов при наличии упаковок, иначе клиент) |
| C3 характеристика вне опции | closed (`ИспользоватьХарактеристикиНоменклатуры.mdo`, колонка привязана к реквизиту) |

Новые: P3 — отложенный обработчик делал контекстный вызов сервера при каждом окончании редактирования
(**fixed**: пересчёт только после отмены); P3 — комментарий «одним вызовом на строку» расходился с кодом
(**fixed**). Ручная проверка: контекстный вызов из обработчика ожидания после Esc, когда пользователь
уже редактирует другую ячейку, не должен сбрасывать редактирование — в «что посмотреть глазами».
