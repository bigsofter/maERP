# Отчёт: E10 — самостоятельный выпуск и настройки (2.0.15.23)

## Task

Бриф `docs/plans/production-2mzl-e10-vypusk-nastroyki.md`: форма ручного выпуска без мёртвого кода предка и пустых серверных
вызовов, удаление константы «Упрощённый выпуск ГП», совет «перезаполнить выпуски» без автовыпусков, запись о неподключённых
проводках выпуска (Р1), закрытие Р10 в части констант выпуска, сценарий уровня 3.

Решения владельца 2026-09-14 (приняты по его предложениям — варианты в шаблоне запроса выбраны не были): константу удалить;
четыре непроизводственные константы Р10 оставить; колонку «Цена» оставить, убрать только пустой вызов; автовыпуски в совете
не учитывать; одна сборка. Временно: обязательные гейты — модель Codex через OpenCode.

## Plan

`.ai/plans/production-2mzl-e10.md`, редакция 2 (§9 — правки `architecture-critic`). Отклонение от брифа: серверная часть
заполнения материалов перенесена из формы в модуль менеджера `ВыпускПродукции` (`ЗаполнитьМатериалыПоТехкартам`, образец —
заказ клиента) вместе с пересчётом количества строк: форма обращается к серверу один раз, сценарий уровня 3 вызывает ту же
процедуру, что форма.

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| исследование | главная сессия | форма, константа, drissotex, совет, сценарии |
| architecture-critic | субагент Claude (Opus) | approve_with_changes: 1×P2, 5×P3 — все приняты, редакция 2 |
| **ai-review-plan** | **`opencode/gpt-6-astra` (временно вместо Codex)**, `opencode run --agent reviewer` с `plan-critic.md` | approve |
| ai-test | скрипты | refcheck, listcheck — PASS |
| **ai-review** | **`gpt-6-astra`** (`--tier free`, exit 3 засчитан по правилу владельца) | approve_with_changes: 1×P2 — отклонена |
| **ai-database** | **`gpt-6-astra`** (exit 3 засчитан) + субагент Claude | та же P2 — отклонена / approve, 1×P3 — в TECHDEBT |
| **ai-security** | **`gpt-6-astra`** (exit 3 засчитан) + субагент Claude | approve / approve |
| **ai-review --verify** | **`gpt-6-astra`**, `opencode run --agent reviewer` с `reviewer-verify.md` | approve: P2 rejected-confirmed, P3 fixed |
| ai-final-check | скрипты | 5 гейтов, 4 PASS, 1 FAIL формально (см. ниже) |

**Ни один обязательный гейт не прошёл на основном аккаунте Codex**: план, код, данные, безопасность и verify — модель Codex
через OpenCode (`gpt-6-astra`); данные и безопасность — дополнительно субагенты Claude. `TINY1C_HOME` — копия правил в
`.ai/state/tiny1c`.

## Files changed

Коммит `526bdf0`. Новый: `Documents/ВыпускПродукции/ManagerModule.bsl`. Удалён: `Constants/ИспользоватьУпрощенныйВыпускГП`.
Изменены: `ВыпускПродукции/Forms/ФормаДокумента` (`Module.bsl`: 11 процедур, переменная и область; `Form.form`: поле и
дополнительная колонка доли стоимости, группа колонок, 10 обработчиков, колонка «Стоимость» поднята на прежнее место),
`CommonModules/ТехнологическиеКарты` (`ЕстьВыпускиПослеДаты`), `Roles/Кладовщик/Rights.rights` (право на константу),
`Configuration.mdo` (состав, версия), `ТестыДокументов` (сценарий `РучнойВыпуск`, проверки в `АвтовыпускПриОтгрузке`),
`ОписаниеИзменений`; `docs/TESTING.md`, `docs/TESTS.xlsx`, `docs/TECHDEBT.md`, `docs/ROADMAP.md`,
`docs/plans/production-2mzl.md`, `production-2mzl-prompt.md`. Uuid существующих объектов не менялись, права ролей — только
удаление права на удалённый объект.

## Tests executed

- `scripts/lint.sh` по всей конфигурации: в добавленных строках новых замечаний нет. Гейт «нет новых замечаний» в
  `ai-final-check` — **FAIL формально**: `YoLetterUsage` в `ТестыДокументов:2950` — имя табличной части `ТЧЗачётАвансов`
  из метаданных, строка E8 (`af624b51`); baseline 2026-08-26 старше модуля. В форме выпуска замечаний стало меньше
  (CommentedCode 11 → 0, QueryNestedFieldsByDot 4 → 0 и др.).
- `scripts/refcheck.py`, `scripts/listcheck.py` — PASS; «Что нового» 2.0.15.23 — 5 пунктов на ru/fr/en/es; `Form.form` —
  XML валиден, пары обработчиков форма ↔ модуль сходятся.
- **На копии полностью обновлённой `build/ib` 2.0.15.23 (2026-09-14, `build/ib-e10`)**:
  - `scripts/doctests.sh` — **24 сценария, ошибок 0, пропусков 0** с первого прогона; `РучнойВыпуск` пройден (заодно
    подтверждено отклонение P2: `Выгрузить(, Колонки)` на табличной части объекта работает);
  - `scripts/smoke.sh` — CheckConfig без замечаний; шаг 1б ложно сработал (96 строк, после нормализации BOM и Unicode
    все в базе известных); шаг 2 отдельно — 398 форм, 110 проверок на образцах, 53 печатные формы, ошибок 0.

  Исправлений по прогону не было — версия остаётся 2.0.15.23.

## Review findings / fixed / rejected

- **Отклонено (P2, ревью кода и данных gpt-6-astra):** «`ТабличнаяЧасть.Выгрузить` не принимает колонки». Тот же вызов
  работает в `ТехнологическаяКарта/ObjectModule.bsl:89` (`СвёрнутыйСостав` из `ОбработкаПроведения`); verify —
  rejected-confirmed; прогон на базе — сценарий `РучнойВыпуск` ОК.
- **Принято (P3, ревью данных Claude):** смена карты задним числом при одних автовыпусках — без сигнала (решение владельца 4)
  → TECHDEBT «Найдено в E10».
- **Принято (architecture-critic):** ручная проверка формы в «Проверка E10»; уточнены утверждения про коэффициент упаковки и
  изменение поведения при пустом результате фасада; отрицательный контроль совета; процедура — в модуле менеджера; мелочи.

## Remaining risks

- Процедура заполнения на данных формы уровнем 3 не проверяется — ручная приёмка («Проверка E10» в `docs/TESTING.md`).
- Коэффициент единицы при заполнении всегда 1 (`РассчитатьКоэффициентЕдиницы` предка закомментирована) — TECHDEBT.
- Автовыпуски после смены карты задним числом не пересчитываются и не сигналят — TECHDEBT.
- Проводки выпуска в `Хозрасчетный` не пишутся (Р1) — TECHDEBT, ROADMAP блок 3.
- Подписи формы выпуска частично только на русском (gf-002) — TECHDEBT.
- В базах клиентов при обновлении пропадает значение удалённой константы (ни на что не влияло); в базе drissotex константы
  нет.
