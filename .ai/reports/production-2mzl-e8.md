# Отчёт: E8 — стадии заказа клиента на производство (2.0.15.20)

## Task

Бриф `docs/plans/production-2mzl-e8-stadii.md`: стадии заказа на производство расширением `СтатусыЗаказовПокупателя` в
регистре `ИсторияЗаказовПокупателейПоСтатусам`, доли выполнения и индикатор, общий модуль `ПроизводственныеЗаказы`,
пересчёт из документов контура, декорация формы, колонки и оформление списка, дашборд и АРМ, сценарий уровня 3.
Решения владельца 2026-09-14: одна сборка; долг — остаток по заказу и его реализациям; стадия всегда равна расчёту;
пять кружков ○◐●; отмена проведения заказа со связанными закупками разрешена; поздняя веха закрывает ранние стадии
(вопрос задан по ходу исследования).

## Plan

`.ai/plans/production-2mzl-e8.md`, редакция 3 (разделы 9–11 — правки ревью). Отклонения от брифа:
- каскад с поздней вехой: отгружено всё → «Ожидает оплаты»/«Выполнен», выпущено всё → «Ожидает отгрузки» без проверки
  закупок; пришедшее без заказа поставщику считается заказанным;
- оплата = начислено реализациями − долг (аванс ПКО «на основании заказа» виден после зачёта в реализации);
- колонка стадии — существующая «Статус», новая — «Индикатор»;
- пересчёт и из РКО, списания безналичных, корректировки долга и сертификата (13 документов);
- отмена проведения документов, затронувших заказ на производство, — новый `ОбработкаУдаленияПроведения` с явной
  очисткой движений под теми же блокировками, что при проведении;
- индексы `Основание` у реализации, выпуска и возврата от покупателя;
- отбор «Заказы к отгрузке» списка реализаций, кабинет партнёра, чтение истории статусов девяти ролям.

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| исследование | главная сессия + 2× `repository-explorer` | статусы, корзины, точки пересчёта, взаиморасчёты |
| architecture-critic | субагент Claude (opus) | approve_with_changes: 1×P1, 4×P2, 7×P3 — редакция 2 |
| **ai-review-plan** | **не Codex** (`codex exec` exit 1, usage limit до 2026-09-19): субагент Claude `architecture-critic` + `plan-critic.md` | approve_with_changes: 3×P2, 8×P3 — редакция 3 |
| ai-test | скрипты | refcheck, listcheck — PASS |
| **ai-review** | **не Codex**: субагент Claude + `reviewer.md` | request_changes: 1×P1, 3×P3 |
| **ai-database** | **не Codex**: субагент Claude + `database.md` | approve_with_changes: 3×P3 |
| **ai-security** | **не Codex**: субагент Claude + `security.md` | approve_with_changes: 1×P2, 3×P3 |
| **ai-review --verify --tier codex** | **не Codex**: субагент Claude + `reviewer-verify.md` | approve_with_changes: прошлые закрыты, 2 новые P3 — исправлены |
| ai-final-check | скрипты | 5 гейтов, 4 PASS, 1 FAIL формально (см. ниже) |

**Все обязательные гейты прошли на субагентах Claude, не на Codex.** После снятия лимита — повторить `ai-review` E7–E8
на Codex.

## Files changed

Новые: `CommonModules/ПроизводственныеЗаказы` (`.mdo`, модуль). Изменены: `Enums/СтатусыЗаказовПокупателя` (7 значений),
`InformationRegisters/ИсторияЗаказовПокупателейПоСтатусам` (6 реквизитов), `.mdo` реализации, выпуска, возврата от
покупателя (индекс `Основание`), модули объектов 13 документов, `РаботаСДокументами`, `УправляемыеБлокировки`, формы
заказа (документ, список: запрос, колонка, условное оформление, «Детали»), список реализаций, `Дашборд`,
`АРМПроизводство`, `ЛичныйКабинетПартнера`, `ТестыДокументов`, роли `PDV`, `Казначей`, `Кладовщик`, `Логист`,
`МенеджерПоПродажам`, `МенеджерПоЗакупкам`, `Оператор`, `ECommerce`, `MobileClient` (Read/View истории статусов),
`ОписаниеИзменений`, `Configuration.mdo`; `docs/TESTING.md`, `docs/TESTS.xlsx`, `docs/ROADMAP.md`, `docs/TECHDEBT.md`,
`docs/plans/production-2mzl.md`, `production-2mzl-prompt.md`. Uuid существующих объектов не менялись.

## Tests executed

- `scripts/lint.sh`: гейт «нет новых замечаний против baseline» — **FAIL формально**: `EventHandlerOutsideEventRegion`
  (новые обработчики в старых модулях без областей), `SetPrivilegedMode` (намеренно, 4 процедуры модуля; у заказа — из
  E6), `YoLetterUsage` (имена метаданных с «ё»), одно `LogicalOrInTheWhereSectionOfQuery`. Остальные замечания разобраны.
- `scripts/refcheck.py`, `scripts/listcheck.py` — PASS; «Что нового» 2.0.15.20 — ru/fr/en/es по 8 пунктов.
- **На копии полностью обновлённой `build/ib` 2.0.15.20 (2026-09-14):** `doctests` — 22 сценария, ошибок 0, пропусков 0
  с первого прогона (в том числе `СтадииЗаказа`); смок — CheckConfig без замечаний, шаг 1б ложно сработал (96 строк, после
  нормализации BOM/Unicode все известны), шаг 2 отдельно — 398 форм, 110 на образцах, 53 печатные, ошибок 0.

## Review findings / fixed / rejected

Решения по каждой находке — `.ai/reviews/production-2mzl-e8/01…05`. Исправлено: привилегии во всех процедурах модуля и
чтение только своих регистров; оплата от долга; одинаковые блокировки проведения и отмены (в том числе РКО и списание
безналичных); отбор «Заказы к отгрузке»; кабинет партнёра; признак записи стадии — индикатор, фильтр по старому и новому
виду операции; долг по всем реализациям заказа; «выпущено всё» раньше «потребности нет»; символы индикатора по точным
значениям; история последней в записи заказа; пропуск при фоновом перепроведении; **P1 ревью кода — очистка наборов
при отмене проведения** (объект, проведённый в том же экземпляре, возвращал движения); статус продажи при отмене
проведения со сменой вида — после очистки потребностей; одноразовая отметка; права чтения истории статусов.
Отклонено: лишний запрос `ЭтоЗаказНаПроизводство` (разовый, по ссылке); `ВЫБОР` в `ГДЕ` (допустим, подтверждён прогоном).

## Remaining risks

- Смок и уровень 3 идут под администратором: пересчёт из-под `PDV`, `Казначей`, `Кладовщик` — ручная приёмка.
- Решение владельца: скрывать ли пятый кружок (оплату) у `ОператорПроизводства`.
- TECHDEBT «Найдено в E8»: переработка не считается выпуском; аванс «на основании заказа» до зачёта; оплата другим
  контрагентом; гонка перепривязки; фоновое восстановление последовательности; подписка платёжек при отмене проведения;
  прямое `Удалить()` проведённого документа; права на запись статусов продаж и ручная правка истории.
- Формы (шапка, список, дашборд, АРМ) — ручная приёмка по списку в `docs/TESTING.md`, «Проверка E8».
