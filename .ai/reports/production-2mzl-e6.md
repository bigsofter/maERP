# Отчёт: E6 — проведение заказа клиента на производство и регистрация техкарты (2.0.15.14)

## Task

Бриф `docs/plans/production-2mzl-e6-provedenie.md`: заказ клиента на производство проводится, проведение
фиксирует состав версией технологической карты; продажа не проводится; запрет смены вида операции у
проведённого заказа; сценарий уровня 3. Решения владельца 2026-09-13: версия карты заказа становится
текущей; дата версии — день проведения; `Кладовщик` заказы не проводит.

## Plan

`.ai/plans/production-2mzl-e6.md`, редакция 3 (раздел 10 — правки ревью плана). Отклонения от брифа:
- регистрация карты и колонка «Техкарта» — в `ПередЗаписью` при проведении: изменения объекта в
  `ОбработкаПроведения` не сохраняются; там же отказ продаже (до `ПриЗаписи`), `ОбработкаПроведения` —
  вторая линия;
- права ролей не менялись: у `ОператорПроизводства` всё уже было; сверх брифа — новая версия карты через
  заказ только при праве на карты (закрывает обход привилегированной записи ролями `Оператор`, `Клиент`,
  `ЛичныйКабинет`, `PDV`);
- проверка заполнения при записи из формы перенесена в объект: у проводимого документа форма проверяет
  заполнение только при проведении (справка 8.5), ECommerce-форма не менялась;
- заголовок форм: общая `УстановитьЗаголовокФормыДокумента` показывает продажу как раньше;
- «строка продукции без материалов» — только способ пополнения «Производство»;
- фасад техкарт: экспортная `ЗаблокироватьКартыПродукции`, `ЗаписатьТехкарту` в своей транзакции с
  блокировкой; `ТехнологическаяКарта.ПередЗаписью` с той же блокировкой (находка ревью данных).

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| исследование | главная сессия + субагент `repository-explorer` (haiku) | права ролей перепроверены скриптом — отчёт explorer был неточен |
| architecture-critic | субагент Claude (opus) | approve_with_changes: 1×P1, 4×P2, 6×P3 — учтены в редакции 2 |
| **ai-review-plan** | **не Codex** (лимит до 23:59): субагент Claude `architecture-critic` + `plan-critic.md` | approve_with_changes: 2×P2, 6×P3 — учтены в редакции 3 |
| ai-test | скрипты | refcheck, listcheck — PASS; уровни 1–3 не запускались (нет обновлённой базы) |
| **ai-review** | **не Codex**: субагент Claude `general-purpose` + `reviewer.md` | approve_with_changes: 1×P2, 6×P3 |
| **ai-database** | **не Codex**: субагент Claude + `database.md` | approve_with_changes: 1×P2, 4×P3 |
| **ai-security** | **не Codex**: субагент Claude + `security.md` | request_changes: 1×P1, 2×P2, 1×P3 |
| **ai-review --verify --tier codex** | **не Codex**: субагент Claude + `reviewer-verify.md`, два круга | круг 1: новая P1 (транзакция фасада) + 2×P3 → исправлено; круг 2 — см. `03-code-review-claude.md` |
| ai-final-check | скрипты | см. «Tests executed» |

**Все обязательные гейты (ревью плана, кода, данных, безопасности, верификация) прошли на субагентах Claude,
не на Codex**: Codex вернул «usage limit… try again at 11:59 PM» (`.ai/state/production-2mzl-e6/`).

## Files changed

`ЗаказПокупателя.mdo` (`posting = Allow`); модули объекта и менеджера заказа; `ФормаДокумента`
(`Form.form`: пять явных кнопок; модуль: `НастроитьПроведение`), `ФормаСписка` (кнопки в панели и
контекстном меню; `НастроитьКомандыПроведения`), `ФормаВыбора` (скрытые кнопки);
`CommonModules/РаботаСДокументами` (заголовок), `CommonModules/ТехнологическиеКарты` (блокировка, транзакция),
`Documents/ТехнологическаяКарта/ObjectModule.bsl` (`ПередЗаписью`), `ТестыДокументов` (сценарий
`ПроведениеЗаказа`), `ОписаниеИзменений`, `Configuration.mdo`; `docs/TESTING.md`, `docs/TESTS.xlsx`,
`docs/ROADMAP.md`, `docs/TECHDEBT.md`, `docs/plans/production-2mzl.md`, `production-2mzl-prompt.md`.
Роли, ECommerce-форма, uuid объектов — не менялись.

## Tests executed

- `scripts/lint.sh`: на добавленных строках осталось два принятых замечания — `EventHandlerOutsideEventRegion`
  у `ОбработкаПроведения` (модуль объекта заказа целиком без стандартных областей) и `SetPrivilegedMode`
  (намеренно). Гейт `ai-final-check` «нет новых замечаний против baseline» — **FAIL** формально: кроме этих
  двух, baseline от 2026-08-26 не знает замечаний модуля `ТехнологическаяКарта` из E1 (буква «ё»,
  `SetPrivilegedMode`, в HEAD уже есть); `CyclomaticComplexity` диспетчера сценариев подавлен той же причиной,
  что и когнитивная сложность.
- `scripts/refcheck.py`, `scripts/listcheck.py` — PASS; «Что нового» 2.0.15.14 — ru/fr/en/es по 7 пунктов.
- **Не прогонялось на базе:** CheckConfig, смок и `doctests` (20 сценариев, в том числе новый `ПроведениеЗаказа`
  и затронутый фасадом `ДатыТехкарты`) — `build/ib` не обновлена до 2.0.15.14.

## Review findings / fixed / rejected

Решения по каждой находке — `.ai/reviews/production-2mzl-e6/01b…05`. Исправлено: проверка права устойчива
к привилегированному режиму (явный пользователь ИБ, три права); узкий привилегированный режим; сообщения
после записи и только без отказа; представление до записи; блокировка карт во всех путях записи, сортировка
значений, прежняя продукция карты; отказ по дате в фасаде без порчи внешней транзакции; запись проведённого
заказа с изменёнными материалами отклоняется; базовая упаковка без отменённой карты; «Что нового»;
тестовые шаги (версия данных карты, две одинаковые строки, обе ветки упаковки, предусловие права,
`ЗаказПроводится`, отмена проведения со сменой вида). Не исправлялось, записано в TECHDEBT: команды
проведения в «Связанных документах», кабинете клиента и закладке заказов списка реализаций (кнопка в
таблице на закладке роняет клиент 8.5); обработка «Проведение документов»; перенос заказов в режиме
загрузки; права `ECommerce` на карты — вопрос владельцу.

## Remaining risks

- Уровни 1–3 не прогонялись: сценарии `ПроведениеЗаказа`, `ДатыТехкарты` и остальные подтверждаются
  только после обновления `build/ib`.
- Явные стандартные кнопки в автозаполняемых панелях — образца в maERP нет (есть в УНФ); задвоение кнопок
  смок не видит.
- Охват привилегированного режима проведения платформой не проверен; защита — `ЕстьПравоМенятьТехкарты`.
  Отказ под пользователем без права на карты — ручная проверка.
- «Техкарта» строки — ссылка на карту дня, её состав может смениться записью карты в тот же день
  (следствие решений 1–2; обязательство для E7–E9 записано).
