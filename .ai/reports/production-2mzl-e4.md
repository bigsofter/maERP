# Отчёт: E4 — вид операции заказа клиента (2.0.15.9)

## Task

Этап E4 волны «производственный контур 2mzl»: признак «Продажа / Производство» у заказа клиента, его
заполнение, представление, навигация и миграция. Бриф `docs/plans/production-2mzl-e4-vid-operacii.md`.
Решения владельца 2026-09-13: смена вида у записанного заказа разрешена (запрет — в E6), название —
«Заказ клиента на производство».

## Plan

`.ai/plans/production-2mzl-e4.md`. После ревью плана добавлены `fillFromFillingValue` и явные права на
общую команду; после ревью данных исправлен состав ролей.

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| ai-explore | OpenCode Go (kimi-k3) | **BACKEND_UNAVAILABLE** — таймаут 600 с; фоллбэк `repository-explorer` (Claude) |
| architecture-critic | субагент Claude (opus) | approve_with_changes, 2 × P2, 3 × P3 — все приняты |
| ai-review-plan | Codex | reject, 2 × P1 — приняты; повторный прогон не выполнен: лимит Codex |
| ai-review (полный круг) | Codex → **лимит**; `--tier free` → OpenCode Go (kimi-k3) | reject: P1 (принят частично), P3 (отклонён) |
| ai-security | OpenCode Go (kimi-k3) | approve, P3 — принят после ошибочного отклонения |
| ai-database | OpenCode Go завис ×3, Codex — лимит; разбор субагентом Claude; 4-й прогон — запасная Zen `opencode/big-pickle` | Claude: approve, 3 × P3 приняты; Zen: approve_with_changes, P2 права — принят, P2 индекс и 2 × P3 — отклонены |
| ai-review --verify | Go завис → запасная Zen `opencode/big-pickle` | approve_with_changes: P1 закрыт, P3 не исправлялся осознанно |
| ai-final-check | локально | 5 гейтов, 5 PASS; пропуски: EDT, уровни 1–3 |

**Обязательные гейты, не прошедшие на своём бэкенде:** повторный `ai-review-plan` и полный `ai-review`
в Codex, `ai-review --verify --tier codex` — лимит Codex до 18:58 2026-09-13; `ai-database` прошёл на
запасной бесплатной модели и субагенте Claude, а не на Kimi/Codex. Перезапустить Codex-гейты после
снятия лимита.

По ходу добавлен сторож первого события и цепочка моделей OpenCode (отдельный коммит): исчерпанный
лимит Go молча вешает `opencode run`, до сторожа каждая попытка стоила 600 с.

## Files changed

- Метаданные: `Enums/ВидыОперацийЗаказаПокупателя` (новое), `CommonCommands/ЗаказыНаПроизводство` (новое),
  `Documents/ЗаказПокупателя/ЗаказПокупателя.mdo` (реквизит), `Configuration.mdo` (регистрация, 2.0.15.9),
  `FunctionalOptions/Производство`, `Subsystems/Производство` (+`CommandInterface.cmi`),
  `Roles/ОператорПроизводства`, `Roles/Кладовщик`.
- Модули: `ЗаказПокупателя/ObjectModule.bsl`, `ManagerModule.bsl`, формы документа и списка
  (`Form.form` + `Module.bsl`), `ОбновлениеПрограммыОбработчики`, `ТестыДокументов`.
- Макет `ОписаниеИзменений` (секция 2.0.15.9 ×4 языка).
- Документы: `TESTING.md`, `TESTS.xlsx`, `ROADMAP.md`, `TECHDEBT.md`, `MERGE-DRISSOTEX.md`,
  `plans/production-2mzl.md`, `plans/production-2mzl-prompt.md`, бриф E4.

## Tests executed

- `scripts/lint.sh` — без новых замечаний против baseline в изменённых модулях (6 прежних замечаний
  сценария E2 в `ТестыДокументов` исправлены попутно: гейт считает весь модуль).
- `scripts/refcheck.py` — расхождений нет; `scripts/listcheck.py` — расхождений нет.
- Статическая команда брифа — «E4 ГОТОВ К ПРОВЕРКЕ НА БАЗЕ»; XML всех правленых `.mdo/.form/.cmi/.rights` валиден.
- `ai-final-check` — 5/5 PASS.
- **Не запускались:** `smoke.sh`, `doctests.sh` на 2.0.15.9 — нужна полная загрузка конфигурации в `build/ib`
  из EDT (headless `1cedtcli` на машине не работает, `docs/EDT-SETUP.md`). Сценарий `ВидОперацииЗаказа`
  написан, но ни разу не выполнялся.
- 2.0.15.8 на обновлённой копии `build/ib`: CheckConfig чисто, смок 398 форм / 110 на образцах / 53 печатные,
  0 ошибок; 17 сценариев, 0 ошибок.

## Review findings / fixed / rejected

Сводная таблица с причинами — `.ai/reviews/production-2mzl-e4/06-findings-decisions.md`, по плану —
`01b-architecture-critic.md`, `02-plan-review.md`, по данным (Claude) — `04b-database-review-claude.md`.

Исправлено: `fillFromFillingValue`; права на команду (роли с разделом «Производство»); порядок отбора и
пагинации в форме списка; эталонная проверка стандартного представления продаж; `dataHistory`;
привилегированный режим и журнал количества в обработчике; тест не зависит от поведения `СоздатьДокумент()`;
инвариант «продажа = не производство» и шаг переноса drissotex.

Отклонено: индекс по `ВидОперации` (нет прецедента в конфигурации, низкая селективность); NULL в отборе
«Продажа» (у реквизита основной таблицы NULL не бывает); порционный обработчик (схема E2); MagicNumber на
литерале 1 (BSL LS не выдаёт).

## Remaining risks

- Код E4 не исполнялся на базе: CheckConfig, открытие форм, сценарий уровня 3 — после полного обновления
  `build/ib`. Особенно: платформенное поведение `Параметры.Отбор` + фиксированный отбор при произвольном
  запросе, заполнение из отбора при создании из списка, формат стандартного представления продажи.
- Миграция: объектная запись каждого заказа в монопольном режиме, версия в истории данных у каждого
  заказа; длительность на базе drissotex не замерялась.
- Продажникам (`МенеджерПоПродажам`, `Оператор`) раздел «Производство» и команда не открыты — решение владельца.
- Codex-гейты (план, полный круг, verify) не выполнены из-за лимита — перезапустить.
