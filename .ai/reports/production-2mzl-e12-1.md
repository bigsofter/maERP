# Отчёт: E12.1 — рабочее место производства: заказы и продукция (2.0.15.29)

## Task

Взять идеи из расширения drissotex (`build/Extension_Drissotex_14092026.cfe`) в план АРМ производства и реализовать его.
Расширение распаковано во временную базу и выгружено в XML; этап E12 переопределён владельцем 2026-09-15 (бриф
`docs/plans/production-2mzl-e12-arm.md`): рабочее место с вводом прямо в таблицах от заказа, три сборки. Эта сборка — E12.1.

## Plan

`.ai/plans/production-2mzl-e12-1.md`, редакция 2 (§14 — изменения по ревью плана).

## Agents used

| Шаг | Бэкенд | Итог |
|---|---|---|
| architecture-critic | субагент Claude (opus) | approve_with_changes: 1×P1, 8×P2, 6×P3 — все приняты в редакцию 2 |
| **ai-review-plan** | **`opencode/gpt-6-astra`** (временно вместо Codex), `opencode run --agent reviewer` с `plan-critic.md` | reject: 1×P1, 2×P2 — приняты |
| **ai-review** | **`gpt-6-astra`** (`--tier free`, exit 3 засчитан по правилу владельца) | reject: 1×P1, 1×P2 — исправлены |
| ai-review (дополнительно) | субагент Claude с `reviewer.md` | approve_with_changes: 2×P2, 4×P3 |
| **ai-database + ai-security** | субагент Claude с `database.md` и `security.md` (Codex недоступен) | approve_with_changes / approve_with_changes: 2×P2, 5×P3 |
| ai-review --verify | субагент Claude с `reviewer-verify.md` | approve_with_changes: 10 из 15 закрыты, новые 1×P2, 2×P3 — исправлены |
| ai-final-check | скрипт | 6 гейтов PASS; уровни 1–3 и EDT пропущены с причиной |

Отчёты — `.ai/reviews/production-2mzl-e12-1/`.

## Files changed

- `src/cf/src/DataProcessors/АРМПроизводство/ManagerModule.bsl` — новый: строки «Продукции», черновики, «Статус», материалы,
  документы цепочки.
- `src/cf/src/DataProcessors/АРМПроизводство/Forms/Форма/Form.form`, `Module.bsl` — форма переписана декларативно.
- `src/cf/src/DataProcessors/ТестыДокументов/ManagerModule.bsl` — сценарий уровня 3 `АРМПроизводство`.
- `Configuration.mdo` (2.0.15.29), `ОписаниеИзменений` ×4 языка.
- `docs/plans/production-2mzl-e12-arm.md` (бриф, отдельный коммит), `production-2mzl.md`, `production-2mzl-prompt.md`,
  `docs/TESTING.md`, `docs/TESTS.xlsx`, `docs/TECHDEBT.md`, `docs/ROADMAP.md`.

## Tests executed

- `scripts/lint.sh` — по модулям рабочего места 0 замечаний; в `ТестыДокументов` новых нет.
- `scripts/refcheck.py`, `scripts/listcheck.py`, `scripts/queryfields.py` — расхождений нет.
- `scripts/changelog.sh 2.0.15.29` — 4 языка, 24 пункта.
- **Не запускались:** `smoke.sh`, `doctests.sh` (EDT владельца держит `build/ib`, база не обновлена), EDT-проверка формы.

## Review findings / fixed

- План: запрос «Документы» через критерий упал бы у оператора на денежных документах → явное объединение типов по праву
  чтения; упаковка и цена при смене характеристики; цена в контракте; формулы дефицита; контрагент черновика как в форме
  (головной контрагент, запрет отгрузок); проверки после блокировки; ключ строки — номер + версия; групповое удаление;
  «Продукция» только производство; тумблер и параметр списка; копия материалов второй строки той же продукции.
- Код: локальная переменная затирала реквизит-список в «Отменить» (P1); черновик по умолчанию — последний использованный
  с опорой на строки таблицы; ложный конфликт версии у новой строки; перечитывание сохраняет незаписанные строки; «В работу»
  проверяет запрет отгрузок; суммы и цены не уходят на клиент пользователю без сумм; право интерактивной пометки на сервере;
  сообщения до записи не теряются; перечитывание при отказе заказа; копия строки заказа в работе — в новый заказ; срез
  последних с параметрами; материалы и код строки при смене продукции у повторного кода.

## Findings rejected (+why)

- Обрамление `#Если Сервер…` в модуле менеджера — 39 из 40 модулей менеджеров обработок и документов проекта без него.
- Повторные запросы `ГоловнойКонтрагент`/`ПолучитьДанныеКонтрагента` — производительность при малых объёмах.
- Проверка организации черновика против организации контекста в сценарии — ограничение записано в TECHDEBT.

## Remaining risks

- Форма собрана генератором и в EDT не проверялась: возможен отказ импорта — нужен Refresh и проверка у владельца.
- Клиентская логика «Продукции» (окончание редактирования, вставка строк, выбор черновика) проверяется только смоком и ручной
  приёмкой; сервер покрыт сценарием уровня 3, который ещё не прогонялся.
- Обязательные гейты шли не на Codex: ревью плана и кода — `gpt-6-astra` через OpenCode, данные и безопасность — субагент Claude.

## 2.0.15.30 — замечания владельца к 2.0.15.29

- **Двойные строки списка заказов**: причина найдена пробами на копии базы (платформенная копия формы, снимки окна) —
  многострочный комментарий демо-заказов в колонке «Комментарий»; без индикатора и со свойствами таблицы эталона строки
  оставались двойными, без колонки комментария — одинарные. Колонка убрана.
- Высоты таблиц 8/5 строк, свойства таблиц — как у эталона списка реализаций; панель фильтров справа по образцу списка
  реализаций, свёрнута по умолчанию, одни отборы для «Заказов» и «Продукции»; статусы, индикатор, проценты и менеджер в
  «Продукции»; кнопки ввода на основании — панелью над таблицей, видны при выделении заказа в работе.
- Проверено: на копии `build/ib` 2.0.15.29 — 27 сценариев и смок без ошибок; платформенная копия новой формы с новыми
  модулями загружена в копию — смок 398 форм без ошибок, 27 сценариев (с новыми проверками отборов) без ошибок, снимок окна:
  одинарные строки, заказы и нижние закладки на одном экране, свёрнутая панель. EDT-форма платформой не проверялась.
- Ревью: субагент Claude (`05-layout-review-claude.md`) — approve_with_changes, 5×P3: двойная загрузка «Продукции»,
  строки вне отбора после записи, колонки-показатели только для чтения — исправлены; сдвиг таблицы при появлении панели
  кнопок и дублирование среза истории — в TECHDEBT.

## 2.0.15.31 — замечания владельца к 2.0.15.30

- **Проценты → подсказка индикатора**: колонки пяти процентов убраны из обеих таблиц; проценты текущего заказа и легенда —
  в подсказке колонки «Индикатор» (`ПояснениеИндикатора`). Подсказки отдельной ячейки модель формы не даёт (`Form.xcore`:
  у поля только статическая `toolTip`), поэтому подсказка обновляется при активизации строки.
- **Разделитель**: перетаскиваемого разделителя в модели формы нет; значок со стрелками под списком заказов оказался
  навигацией динамического списка. Нижняя цепочка — в сворачиваемой группе `ГруппаЦепочка` (8.5.1,
  `ButtonInParentElement`: кнопка на разделителе), нижние таблицы растягиваются.
- **Кнопки при отметке строк**: пробы на копии базы — кнопки в автокомандной панели таблицы с общими командами
  (`selectedRowsUse` Use без связанной таблицы) рисуются постоянной неактивной панелью и в тёмную панель выделения не
  попадают; с `associatedTableElementId` = таблица — появляются в панели выделения на обеих закладках. Команды статуса и
  ввода на основании заведены на каждую таблицу; заказ основания — из отмеченных строк.
- «Продукция»: статус, стадия, индикатор впереди, заказ последним; новая строка — в начало (вставка в обработчике
  `ПередНачаломДобавления` и ввод обработчиком ожидания); фильтры клиента и продукции — по части наименования
  (`ШаблонВхождения`, `ПОДОБНО … СПЕЦСИМВОЛ "~"`).
- Попутно: мастер первого запуска — переменная `Параметры` в серверной функции формы писала в свойство формы (отдельный
  коммит, правило tiny1C bsl-014).
- **Проверено** на копии `build/ib` 2.0.15.30 (`build/ib-30-probe`) с платформенной копией формы 2.0.15.31
  (`probe31full.py`) и модулями из `src/cf`: 27 сценариев уровня 3 без ошибок (новые проверки отборов по вхождению,
  регистра, экранирования «%», пояснения индикатора), смок 398 форм без ошибок (шаг 1б — прежний ложный сигнал на
  испанских сообщениях платформы, модулей рабочего места в нём нет), `ai-final-check` 6/6.
- **Не проверено глазами**: снимок окна сборки 2.0.15.31 не снят — включён Stage Manager, окно пробного клиента
  не на переднем плане уходит в миниатюру, а фокус у владельца не перехватывался. Панель выделения проверена снимком
  пробы до сборки; вид разделителя и панели без отметок — ручная приёмка.
- Ревью: субагент Claude (`06-feedback31-review-claude.md`) — approve_with_changes, 1×P2, 3×P3: ввод на основании по
  неотмеченному заказу при отметке только незаписанных строк, зависшая вставленная строка, стадия чужого заказа в копии —
  исправлены; ручные сценарии уровня 4 для клиентской логики — в TECHDEBT (пункт «Уровень 4 не покрывает…»).
