# Отчёт: E5 — материалы в заказе клиента на производство (2.0.15.13)

## Task

Бриф `docs/plans/production-2mzl-e5-materialy.md`: табличная часть материалов заказа на производство,
связанная со строками товаров, заполнение по действующим техкартам, пересчёт, очистка, заполнение по КП,
сценарий уровня 3. Решения владельца 2026-09-13: КП заполняет сразу; у заказа не на производство материалы
очищаются при записи; разный состав одной продукции запрещён уже в E5.

## Plan

`.ai/plans/production-2mzl-e5.md`, редакция 3. Отклонения от брифа: колонки `КоличествоЗаЕдиницу` /
`КоличествоУпаковок` / `Количество` (соглашение документов и имя колонки выпуска); общая логика — модуль
менеджера над коллекциями строк (данные формы не теряют дополнительные колонки); в фасаде техкарт —
экспортная `КлючСоставаСтрок`; продукция для запрета разного состава — номенклатура без характеристики.

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| architecture-critic | субагент Claude (opus) | approve_with_changes, 3 × P1 учтены в редакции 2 |
| ai-review-plan | **не Codex** (лимит): OpenCode Go kimi-k3 | approve_with_changes, P2/P3 учтены в редакции 3 |
| ai-test | скрипты | refcheck, listcheck |
| ai-review (полный) | субагент Claude (OpenCode не ответил) + **Codex 18:59** | 3 × P2 + 5 × P3; затем Codex 3 × P2 |
| ai-database | **не Codex**: субагент Claude (OpenCode не ответил, Codex в лимите) | approve_with_changes, 5 × P3 |
| ai-review --verify --tier codex | **не Codex** (лимит до 23:59): субагент Claude | все находки Codex closed, 2 × P3 fixed |
| ai-final-check | скрипты | 5 гейтов PASS |

Гейты, прошедшие не на своём бэкенде: ревью плана (OpenCode вместо Codex), ревью данных и верификация
(субагент Claude вместо Codex). Полный круг ревью кода на Codex прошёл.

## Files changed

`ЗаказПокупателя.mdo` (ТЧ `ТЧМатериалы`, `ТЧТовары.Техкарта`), модули объекта, менеджера и формы
документа, `Form.form` (группа материалов, тумблер, таблица, команды, колонка «Техкарта», обработчики),
функциональные опции `Производство`, `ИспользоватьУпаковку`, `ИспользоватьХарактеристикиНоменклатуры`,
фасад `ТехнологическиеКарты` (`КлючСоставаСтрок`), `ТестыДокументов` (сценарий `МатериалыЗаказа`),
`ОписаниеИзменений`, `Configuration.mdo`, `docs/TESTING.md`, `docs/TESTS.xlsx`, `docs/ROADMAP.md`,
`docs/TECHDEBT.md`, `docs/plans/production-2mzl.md`, `production-2mzl-prompt.md`.

## Tests executed

- lint без новых замечаний против baseline, refcheck и listcheck чистые, `ai-final-check` 5/5.
- На копии обновлённой `build/ib` (18:45, после исправлений по ревью Claude): тесты документов 19/19,
  CheckConfig без замечаний, смок 398 форм / 110 образцов / 53 печатные формы — 0 ошибок (шаг 1б — ложное
  срабатывание на испаноязычных строках, шаг 2 отдельно).
- **Не прогонялось на базе:** последние правки после Codex — клиентский код формы (копия строки, пересчёт,
  отложенный обработчик) и состав опции характеристик. Серверная логика и сценарий уровня 3 не менялись.

## Review findings / fixed / rejected

`.ai/reviews/production-2mzl-e5/06-findings-decisions.md`. Исправлено: потеря материалов после Esc и при
копии строки со сменой продукции, проверка заполнения скрытой таблицы у продажи, вызовы сервера в
клиентских циклах, нормализация кодов строк без карты, упаковка ручного материала, сообщения при очистке,
квадратичные проходы, колонка характеристики вне опции, текст «Что нового». Отклонено с причиной: параметры
выбора колонки «Техкарта» (только просмотр), длина строк НСтр (lint не отмечает), видимость колонки
«Количество» (дублирует упаковки при коэффициенте 1). Отложено в TECHDEBT: ECommerce-форма и ручные
материалы, отрицательный расход, дублированная формула пересчёта, коэффициент упаковки = 1, сообщение по
строке, скрытой отбором.

## Remaining risks

- Клиентская логика формы видна только глазами (список в отчёте владельцу).
- Контекстный вызов сервера из обработчика ожидания после Esc — проверить, что не сбрасывает редактирование
  другой ячейки.
- Черновой CF `build/draft/maERP-2.0.15.13-draft-e5.cf` собран до исправлений по ревью — не для передачи.
