# Отчёт: E11, демо-часть — демо-набор контура 2mzl и торговли (2.0.15.24)

## Task

Бриф `docs/plans/production-2mzl-e11-demo.md`: наполнить тестовую базу записываемыми демо-данными, по которым видны все
варианты работы документов производственного контура 2mzl и торговли — по стадиям и статусам; на них снимаются скриншоты
для инструкций maERP в базе знаний tinycio (следующая задача). Решения владельца 2026-09-14: демо-данные прямо в
`build/ib`; объём — контур 2mzl + торговля; инструкции и кейсы по схемам производства — в прод tinycio сразу, со
скриншотами полностью развёрнутого окна.

## Plan

`.ai/plans/production-2mzl-e11-demo.md`, редакция 2 (§9 — правки `architecture-critic` и ревью плана моделью Codex через
OpenCode). Архитектура: генератор `Обработки.ТестовыеДанные.СоздатьДемоНабор` (одна транзакция, маркер по наименованию,
окно текущего дня, константа ФО до транзакции) и ядро `ЗаполнитьДемоНабор`, которое вызывает и сценарий уровня 3
`ДемоНабор`; помощники `ТестыДокументов` стали экспортными вместо копирования; запуск `scripts/fixtures.sh --demo`.

## Agents used

| Шаг | Бэкенд | Результат |
|---|---|---|
| исследование | главная сессия + `repository-explorer` (Sonnet) + 2× `Explore` (tinycio) | статусы и стадии, помощники, база знаний tinycio |
| architecture-critic | субагент Claude (Opus) | approve_with_changes: 2×P1, 4×P2, P3 — все приняты |
| **ai-review-plan** | **`opencode/gpt-6-astra` (временно вместо Codex)**, `opencode run --agent reviewer` с `plan-critic.md` | reject: P1, 3×P2 → редакция 2; круг проверки плана — approve |
| ai-test | скрипты | refcheck, listcheck — PASS |
| **ai-review** | **`gpt-6-astra`** (`--tier free`, exit 3 засчитан по правилу владельца) | approve_with_changes: P2 — принята |
| **ai-database** | **`gpt-6-astra`** (exit 3 засчитан) + субагент Claude | P2 (параллельный запуск) — принята / approve_with_changes, 4×P3 — приняты |
| ai-security | не запускался | права ролей, привилегированный режим, пароли, интеграции не затронуты |
| **ai-review --verify** | **`gpt-6-astra`**, `opencode run` с `reviewer-verify.md` | approve_with_changes: 5 из 6 закрыты, открыт процессный P3-4 — прогон на обновлённой `build/ib` (после обновления владельцем) |
| ai-final-check | скрипты | 5 гейтов, 4 PASS, 1 FAIL формально (см. ниже) |

**Ни один обязательный гейт не прошёл на основном аккаунте Codex**: план, код, данные и verify — модель Codex через
OpenCode (`gpt-6-astra`); данные — дополнительно субагент Claude.

## Files changed

`DataProcessors/ТестовыеДанные/ManagerModule.bsl` (экспорт `СоздатьДемоНабор`, `НачалоДемоНабора`, `ЗаполнитьДемоНабор`,
область `ДемоНабор`), `DataProcessors/ТестыДокументов/ManagerModule.bsl` (`НовыйКонтекстДанных`, экспорт помощников, поле
`Пометка`, `ШагСекунд`, сценарий `ДемоНабор`), `DataProcessors/СмокТест/Forms/Форма/Module.bsl` (режим `;Демо`),
`scripts/fixtures.sh` (`--demo`), `ОписаниеИзменений`, `Configuration.mdo`; `docs/TESTING.md`, `docs/TESTS.xlsx`,
`docs/TECHDEBT.md`, `docs/ROADMAP.md`, `docs/plans/production-2mzl.md`, `production-2mzl-prompt.md`, бриф E11. Метаданные,
права ролей и uuid не менялись. tiny1C: правило `bsl-012` (локальный коммит `fd898b4`).

## Tests executed

- `scripts/lint.sh` по всей конфигурации: в добавленных строках замечаний нет. Гейт «нет новых замечаний» — **FAIL
  формально**: `YoLetterUsage` в `ТестыДокументов` — имя табличной части `ТЧЗачётАвансов` (строка E8), baseline 2026-08-26
  старше модуля.
- `scripts/refcheck.py`, `scripts/listcheck.py` — PASS; «Что нового» 2.0.15.24 — 3 пункта на ru/fr/en/es.
- **До обновления базы — частичная загрузка модулей в копии `build/ib` 2.0.15.23** (`build/ib-e11`, `-e11b`, `-e11c`):
  - первый прогон: ошибка компиляции «Новый Запрос(...).Выполнить()» (bsl-012) — исправлена; S7 — «Получен от
    субподрядчика» не ставится поступлением на основании передачи (дефект механизма, TECHDEBT), ожидание приведено к
    фактическому поведению;
  - итог: 25 сценариев без ошибок; `fixtures.sh --demo` — 24 кейса, 44 ссылки, 12 с; повтор — `Было=1`; 25 сценариев на
    заполненной копии без ошибок; смок на заполненной копии — CheckConfig чисто, шаг 1б ложно (96 строк известны), 398 форм,
    111 на образцах, 54 печатные формы, ошибок 0.
- **На обновлённой `build/ib` 2.0.15.24** — DB_RESULT.

## Review findings / fixed / rejected

Исправлено: контракт итога с формой запуска (`Было`); P9 — полный ручной выпуск до частичной отгрузки; S5 «Отгружен» через
перепроведение выпуска; граница полуночи — проверка окна до записи и дня после заполнения; ФО до транзакции со сбросом кэша,
сценарий — ПРОПУСК без ФО; P4 переименован (карта регистрируется проведением); помощники экспортированы вместо копирования;
маркер по наименованию, помеченный маркер, повторная проверка под блокировкой; явные материалы и количества; константа
автовыпуска — только при `Ложь`; подсчёт лидов и заявок по значениям прогона. Отклонённых нет.

## Remaining risks

- «Получен от субподрядчика» и «Доставлен» у заказа-продажи штатно не ставятся — TECHDEBT «Найдено в E11».
- Демо-объекты становятся образцами смока и «первыми элементами» справочников; набор включает константы производства и
  автовыпуска в `build/ib`.
- Сценарий уровня 3 и заполнение — под администратором; права ролей на демо-документы не проверялись.
- Французские наименования справочников набора на скриншотах русского интерфейса — осознанно.
