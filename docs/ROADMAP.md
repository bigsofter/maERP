# Планы работ

Разложенный по очереди техдолг. `docs/TECHDEBT.md` — журнал находок: что нашли,
когда и почему отложили. Этот файл — план: в каком порядке разбирать, что чем
блокировано и где нужно решение владельца. Пункт живёт в обоих местах: описание
в TECHDEBT, работа — здесь.

**Ведение.** Новая находка сначала попадает в TECHDEBT. Если из неё следует
работа — строка сюда, в нужную очередь. Закрыли — отмечаю «закрыто `<версия>`» в
обоих файлах; из очереди пункт убирает публикация CF, вместе с переносом секций
в `docs/CHANGELOG.md`.

**Что здесь не планируется.** Раздел «Принято как есть» перечисляет ограничения,
по которым решение уже есть и работа не предполагается: он нужен, чтобы они не
всплывали заново как новые находки.

---

## Развилки владельца

Пока решения нет, соответствующие блоки очереди не начинаются.

| # | Решение | Что блокирует |
|---|---|---|
| Р1 | Бухгалтерский контур: доводить или вырезать | блок 3 целиком (~11 пунктов), полностью зелёный CheckConfig |
| Р2 | Торговое оборудование: нужно ли | блок 4, карантин 5 модулей и ~2100 замечаний BSL LS |
| Р4 | Нужен ли `ЗаказПокупателя` склад отгрузки | колонка «Остаток» в заказе (блок 8) |
| Р5 | `Справочник.Банки`: выпилить загрузку или сменить источник | блок 1 |
| Р6 | `УпаковкиЕдиницыИзмерения`: подчинять владельцу или нет | блок 8 |
| Р7 | Табель: заводить реквизит «Группа» или убрать подпись из макета | блок 7 |
| Р8 | Чек возврата: печатать свой номер или номер основания | блок 7 |
| Р9 | `ПолучитьРеквизитыПоИНН`: заводить константы ИТС или удалить функцию | блок 8 |
| Р10 | Константы, которые не читает код: удалить или наполнить поведением. **Закрыта в части выпуска** (выпуск при продаже — поведение E9, упрощённый выпуск ГП — удалена в E10, 2.0.15.23); открыта по четырём: дата демо-режима, синхронизация, единицы длины и объёма | формы настроек без них с 2.0.14.19; TECHDEBT «Константы, которые не читает код» |

---

## 0. Перед ближайшим релизом

Обязательный минимум, без которого сборка не отдаётся.

- **Полный регресс владельца на тестовой базе** — режим совместимости поднят с
  8.3.23 до 8.5.1 в волну 11: проведение, печать, РМК, обмены. Шаг чек-листа
  `docs/RELEASING.md`. *(TECHDEBT: Платформа)*
- **Проверить глазами шапки печатных форм** — `FactureOld`, чек и
  `FactureSocassif` после ближайшего обновления базы: агрегаты `МАКСИМУМ` в
  запросах с `ИТОГИ ПО` добавлены в 2.0.12.14–15, но прогон видит только
  отсутствие номера. *(TECHDEBT: печатные формы реализации, 2026-08-26)*
- **Проверить счёт-фактуру Socassif**, когда в базе появится образец налоговой
  накладной: колонки `DocumentDate`/`DocumentNumber` разъезжались, исправлено в
  2.0.12.15. *(TECHDEBT: Socassif, 2026-08-26)*
- **Язык интерфейса у клиента** — платформенные строки выходят по-испански при
  французской конфигурации. Не дефект: в списке баз «Язык интерфейса» =
  французский либо ключ `/L fr`. Внести в инструкцию установки.
  *(TECHDEBT: Интерфейс)*

## 1. Тираж и безопасность

- ~~**Пароль скрытого администратора `tinycio-1c`** зашит в `ФормаПервогоЗапуска`~~ —
  закрыто `2.0.14.11` (2026-09-06): в визарде пароль генерируется случайно и показывается
  один раз, в headless-режиме приходит из payload; вторая зашитая учётка в
  `ПроцедурыРегламентныхЗаданий` убрана. Развилка Р3 снята. Аутентификация ОС или
  выделенная роль для поддержки — при необходимости, отдельным решением.
  *(TECHDEBT: Решения)*
- **`Справочник.Банки`: загрузка классификатора с rbc.ru** — виндовые
  разделители путей, `КопироватьФайл` по http; мертво на сервере macOS/Linux и
  бессмысленно вне РФ. Выпилить или сменить источник. **Р5.** *(TECHDEBT: Код)*
- ~~**Свойства конфигурации `Vendor`, `Copyright`, `VendorInformationAddress`** —
  заглушки~~ — закрыто `2.0.14.11` (2026-09-06) значениями по умолчанию `TinyCIO`,
  `© TinyCIO`, `https://tinycio.com` на четырёх языках; если у владельца другие
  формулировки — поправить в `Configuration.mdo`. *(TECHDEBT: сравнение с drissotex)*

## 2. Интерфейс 8.5 и раскладка УНФ

Самая объёмная плановая работа, идёт волнами по подсистемам с 2026-08-27.

- **Проход по ~425 формам** — проставить `preferableInterface`, негодные
  точечно закрепить за «Такси»; заодно привести раскладку к виду УНФ по
  `docs/FORMS-STYLE.md`. Пилот (реализация, карточка и список номенклатуры) — в
  2.0.13.1. *(TECHDEBT: Интерфейс)*
- Хвост из более ранних решений: список `КассоваяСмена` (АРМ кассира) и
  `ОперацияБух` на стандарт форм не переводились — вернуться, когда решится Р1.
- **Меню «Настройка» и «Главное»** — сделано 2.0.14.17–2.0.14.21: основной раздел
  с картинкой-кебабом и справочниками компании, обработка `НастройкиПрограммы`
  (девять форм по блокам, запись сразу при изменении), раздел «Настройка» с
  группами «Настройки программы», «Пользователи и права», «Оборудование».
  Ждёт проверки в EDT: привязка `<mainSectionPicture>` ссылкой на общую картинку.
- **Логотип и заставка не привязаны** — `Logo.jpg`/`Splash.jpg` лежат в
  `Configuration/`, элементы `<logo/>`/`<splash/>` в `Configuration.mdo` пусты.
  Привязать вместе с проверкой картинки главного раздела. *(TECHDEBT: Свойства
  конфигурации)*
- **Часовой пояс базы в headless-развёртывании** — константа `ЧасовойПояс`
  (2.0.14.18) в payload `initial-fill` не передаётся; добавить
  `company.time_zone` и применение при первом запуске. *(docs/DEPLOY.md)*


## 3. Бухгалтерский контур — после Р1

Документ `ОперацияБух` и отчёты достались от предка вместе с кодом
1С:Бухгалтерии. Пункты разбираются вместе: доводить контур или вырезать ветки.

- **Ссылки на отсутствующие объекты в `ОперацияБух`** —
  `ПланыВидовХарактеристик.СтатьиДоходов`, тип `…СтатьиРасходов`,
  `Перечисления.ВидыРасходовНУ`, `РегистрыНакопления.НДСЗаписиКнигиПокупок` и
  `…КнигиПродаж`, `РегистрыСведений.ЖурналУчетаСчетовФактур`. Форма открывается,
  ломается работа с проводками. *(TECHDEBT: автотесты 2026-08-26)*
- **Валютный пересчёт проводок не реализован** — регистра курсов нет,
  `ПересчетСуммыПоКурсу` возвращает сумму без пересчёта; при валюте, отличной от
  базовой, суммы Дт/Кт РУ/УУ/ФО неверны. Вводить только вместе с полноценным
  валютным контуром. *(TECHDEBT: смок 2026-08-25)*
- **Статус отражения в реглучёте всегда пуст** — механизм
  `РеглУчетПроведениеСервер` (БП) не перенесён.
- **Подбор субконто без отборов** — стандартный выбор платформы по типу вместо
  отбора по организации.
- **`БухгалтерскиеОтчеты*` зовут методы, которых нет** в наших
  `ОбщегоНазначения`, `ОбщегоНазначенияКлиент`, `ОбщегоНазначенияКлиентСервер`,
  `БухгалтерскийУчет`; открывают несуществующие общие формы настроек отчётов;
  ссылаются на типы `СправочникСсылка.НоменклатурныеГруппы` и
  `…ПодразделенияОрганизаций`. *(TECHDEBT: refcheck 2026-08-26)*
- **Несуществующие отчёты** `СчетаИсключаемыеИзРасчетаЗадолженностиПокупателей`
  и `…Поставщикам` в `БухгалтерскиеОтчетыКлиентСервер` (строки 1551, 1553).
- **Справка бухобъектов писалась заново** — 55 перекрёстных ссылок сняты
  2026-08-26 ради зелёного CheckConfig, текст сохранён. Если контур доводится —
  писать справку заново.
- **Неиспользуемый реквизит формы**
  `ИсточникСуммыДляПересчетаВВалютуФинОтчетности` — убрать при следующей правке
  формы в EDT (дешёвое, можно и до Р1).
- **Проводки выпуска продукции не подключены** — `Хозрасчетный.ДвижениеВыпускПродукции`
  готова, диспетчер `СформироватьДвижения` закомментирован: ручной выпуск и автовыпуск
  проводок не пишут. *(TECHDEBT: Найдено в E10)*

## 4. Торговое оборудование — после Р2

- **Пять модулей `МенеджерОборудования*` в карантине** с 2026-08-24: ~2100
  замечаний BSL LS, вызовы несуществующих методов БСП. *(TECHDEBT: Решения)*
- **`МенеджерОборудованияВызовСервера` ссылается на
  `Справочники.ШаблоныМагнитныхКарт`** — объекта нет; там же общая форма
  редактора этикеток. *(TECHDEBT: автотесты и refcheck 2026-08-26)*
- **`МенеджерОборудованияКлиент` вне общего механизма расширения** — своя пара
  `НачатьПодключениеРасширенияРаботыСФайламиЗавершение` /
  `НачатьУстановкуРасширенияРаботыСФайлами` с вопросом через `ПоказатьВопрос`
  внутри веток препроцессора. Перевести на
  `РасширениеРаботыС1СПредприятиемКлиент` при ближайшей правке подсистемы:
  проверяется только на живой ККТ. *(TECHDEBT: 2026-08-27)*

## 5. Недостроенные механизмы

- **Присоединённые файлы** — `ПрисоединённыеФайлыКлиент` не содержит
  `ОткрытьФайл`, `ПрисоединитьФайлы`, `СохранитьФайлы`, а их зовут карточка
  номенклатуры и обе формы регистра: кнопки ведут в пустоту. Достраивать вместе
  с механизмом. *(TECHDEBT: refcheck 2026-08-26)*
- **Длительные операции** — `ДлительныеОперацииКлиент` открывает
  `ОбщаяФорма.ДлительнаяОперация`, которой в конфигурации нет.
- **Отложенные обработчики обновления** — все обработчики выполняются при входе,
  монопольные под монопольным режимом. Если появится обработчик на миллионы
  записей, вход будет его ждать; архитектура регистрации отложенность допускает
  (`РежимВыполнения` уже есть). *(TECHDEBT: волна У1)*
- **Константа `НомерВыполненногоЗаполненияДанных`** — пустой объект метаданных,
  живёт до тех пор, пока все базы клиентов не пройдут версию 2.0.12; потом
  удалить вместе с веткой в `СтартоваяВерсияИнформационнойБазы`.

## 6. Покрытие тестами

- **Половина форм объектов не проверяется: нет образцов** — закрыто 2.0.13.1:
  `Обработка.ТестовыеДанные` заводит образцы для всех документов и справочников,
  прогон 2026-09-02 открывает 105 форм на образцах с одним осознанным пропуском
  (`ГруппыПользователей` автотестом не заводятся). *(TECHDEBT: прогон форм 2026-08-27)*
- **Неосновные формы объекта не покрыты** — закрыто 2.0.14.15: явный список
  `ДополнительныеФормыОбъектов()` в `Обработка.СмокТест` (формы роли ECommerce,
  формы единицы и упаковки) открывается вторым проходом на образцах. Новая
  подстановка в `ОбработкаПолученияФормы` — строка в этот список.
- **Уровень 3, жизненный цикл документов** — сделан 2.0.14.15: `Обработка.ТестыДокументов`,
  `scripts/doctests.sh`, девять сценариев в транзакции с откатом. Новый документооборот —
  новый сценарий (правило `docs/TESTING.md`).
- **Уровень 0.7, проверка динамических списков** — сделан 2.0.15.1:
  `scripts/listcheck.py` сверяет колонки форм с текстом произвольного запроса и
  основной таблицей. Осталось разобрать 25 расхождений-наследия из
  `scripts/listcheck-known.txt` — по одной форме, снимая строку из файла.
  *(TECHDEBT: найдено проверкой динамических списков 2026-09-09)*
- **Уровень 4 перестал быть теоретическим** — клиентский обработчик строки
  списка в АРМ производства падал у владельца, а машинная проверка на такое
  невозможна: поле берётся у `ТекущиеДанные`, состав колонок собирается в коде.
  Первый шаг дешевле полного тест-клиента — проход смока, который открывает
  формы списков на клиенте и ставит `ТекущаяСтрока` на первую строку; полный
  уровень 4 (`1cv8 /TESTCLIENT` или Vanessa) — после него.
  *(TECHDEBT: клиентские обработчики строк списков 2026-09-09)*
- **Ложное «новое замечание» расширенной проверки модулей в смоке** — `scripts/smoke.sh`,
  шаг 1б, сравнивает журнал с `build/checkconfig-baseline.txt` через `sort | comm`, не
  нормализуя BOM и порядок сортировки: известные строки считаются новыми, смок помечает
  ошибку компиляции и пропускает «открыть все формы». Нормализовать строки перед сравнением.
  *(TECHDEBT: смок, сравнение с базой замечаний 2026-09-13)*

## 7. Печать

- **Табель учёта рабочего времени**: подпись «Группа: » в макете осталась и
  печатается пустой (реквизита у документа нет). **Р7.**
  *(TECHDEBT: печать по образцам 2026-08-26)*
- **Чек возврата от покупателя** печатает номер основания, а не свой; обе формы
  внесены в `ПечатныеФормыБезНомера()`. **Р8.**
- **Фон и бланк организации в печати не участвуют** — реквизиты
  `ФонПечатныхФорм`, `БланкШапка`, `БланкПодвал` заполняются в карточке, но их
  никто не читает; подставляются только логотип и печать. Довести до единой
  точки вместе с остальным оформлением. *(TECHDEBT: волна У2)*
- **Пакетная печать карточек контрагентов** —
  `Справочники.Контрагенты.ПолучитьКарточку` вызывается по разу на документ и
  организацию (кэш на одну печать). Для массовой печати сотен документов сделать
  `ПолучитьКарточки(Массив, Дата)`. *(TECHDEBT: волна 4)*
- **`ПечатныеФормы.СформироватьТабличныеДанные`** разыменовывает поля через
  точку — 12 замечаний `QueryNestedFieldsByDot`, диагностика подавлена в
  комментарии. Переписать на соединения при ревизии печатных форм реализации.
- **Модуль `ABCАнализ`** перенесён как есть вместе со своими замечаниями BSL LS
  (цикломатика, вложенность, закомментированный код, `Сообщить`, магические
  числа и прочее). Переписывать только вместе с ревизией отчёта
  `ABCАнализПродаж`: без тестов на данных трогать расчёт классов рискованно.
- **`ДобитьСтраницуПустымиСтроками`** упадёт на `ПолучитьОбласть`, если макет
  окажется без области `EmptyLine`; сейчас держится предохранителем в 500 строк.
- **Макет `DevisBL` (A5)** потерял строки «Commandé par» и «Adresse de
  livraison» — вернуть область и параметр `АдресДоставки`, если понадобится
  адрес доставки.
- **Метки IF / PATENTE / RC / CNSS / ICE** — литералы марокканских
  идентификаторов в подвале реквизитов и шапках Devis/BL; вынести в настройку
  организации при выходе на другие рынки.

## 8. Функциональные хвосты по контурам

**Продажи и заказы**

- Склад отгрузки у `ЗаказПокупателя`: реквизита нет, «Остаток» считается по
  складу из настроек пользователя, при пустой настройке — по всем. **Р4.**
- Статус «Доставлен» по-прежнему никем не пишется. *(волна 12)*
- Подбор товара не выбирает серию — добавить шаг по образцу характеристики,
  когда появится серийный учёт. *(волна 9)*
- Динамическая цена от последнего прихода ищет прайс поставщиков без отбора по
  характеристике. *(волна 9)*

**Производство и переработка**

- **Волна «многоуровневые техкарты и переработка»** — план `docs/plans/production-subcontracting.md`
  (редакция 6), промпт волны `docs/plans/production-subcontracting-prompt.md`, промпт первого этапа
  `docs/plans/production-subcontracting-s1-prompt.md` (2026-09-18). Девять этапов S1–S9: способ
  изготовления и процент потерь в техкарте, развёрнутая потребность до четырёх уровней, иерархия
  материалов в рабочем месте, автовыпуск ПФ при передаче в переработку, списание и себестоимость
  поступления из переработки, порядок обеспечения «партия заказа → свободный остаток → производство»,
  настройки, закладка поступлений, подборы номенклатуры. Порядок: S1 → S2 → S3 → (S4 + часть S5) →
  остаток S5 → S9 → S8 → S6 → S7.
  Два разбора плана вернули `reject` с тремя находками уровня P0, все три — об одном: проведение
  заказа сверяет состав материалов с техкартой и при расхождении молча перезаписывает карту, поэтому
  разворачивание в глубину не подменяет `СоставТехкарт`, а новые ресурсы не входят в `КлючСостава`.
  `ai-review-plan` на Codex ещё не проходил — подписка была в лимите до 19.09, разборы выполнены
  субагентами Claude.
  **S1, 2.0.15.39:** способ изготовления (новое перечисление: переработчик / наше производство / оба)
  и процент потерь — два ресурса регистра `СоставТехКарты` и два реквизита `ТЧМатериалы` документа
  карты; видны в документе, в карточке номенклатуры, в списке регистра и на закладке «Материалы»
  рабочего места (показ, из версии карты строки заказа). Путей записи состава оказалось **пять**, а не
  четыре: к фасаду, свёртке, ключу состава и обработчику обновления добавился `ПередЗаписью` самого
  объекта карты — форма документа (у неё нет модуля), копирование и прямая запись шли мимо фасада и
  клали в регистр пустую ссылку перечисления. Перезапись карты проведением заказа переносит способ и
  процент из базовой версии, как уже делалось для упаковки. Обработчик обновления `2.0.15.39` правит
  и документы карт, и движения. Тридцать второй сценарий уровня 3 плюс ключевая проверка «значения не
  обнуляются при перезаписи карты из заказа» в существующем сценарии проведения заказа.
  Обязательные гейты `ai-review-plan`, `ai-review` и `ai-database` **не пройдены**: Codex был в лимите,
  выполнены субагентами Claude; решение владельца 19.09 — не перепрогонять. Отчёт —
  `.ai/reports/production-subcontracting-s1.md`. Тогда же по решению владельца пересоздана базовая
  линия линта (была от 2026-08-26, роняла гейт на чужом коде).
  **S2** — промпт готов: `docs/plans/production-subcontracting-s2-prompt.md` (2026-09-19). Открытая
  развилка этапа: где живёт разовая проверка карт базы на циклы и глубину — от неё зависит и
  содержимое окна «Что нового», потому что сама функция разворачивания пользователю невидима.

- **Волна «производственный контур 2mzl»** — план `docs/plans/production-2mzl.md`, промпт
  `docs/plans/production-2mzl-prompt.md` (2026-09-09). Пятнадцать этапов E1–E15: технологические
  карты в регистр с историей, способ пополнения запаса, коммерческое предложение, вид операции и
  материалы в заказе покупателя, регистр потребностей производства, стадии заказа, автовыпуск при
  отгрузке, АРМ производства, тесты и демо-данные, совместимость с базой drissotex, кейс клиента.
  Закрывает развилку **Р10** в части двух констант выпуска. Идёт этапами, каждый — свой коммит и
  своя сборка `2.0.15.Y`.
  **E1, 2.0.15.6:** реализованы и приняты регистр и фасад техкарт, карточка номенклатуры,
  перенос прежних составов, выбор дня версии, расчёт показателей и пять сценариев
  уровня 3. История проверки: `docs/plans/production-2mzl-e1-check.md`.
  **E2, 2.0.15.7:** реализован способ пополнения запаса (Закупка / Производство / Оба),
  умолчания по типу номенклатуры, серверная страховка, миграция существующих элементов
  и шестнадцатый сценарий уровня 3. Статические проверки, смок и 16 сценариев уровня 3
  прошли на обновлённой `build/ib`; остаётся ручная приёмка поля в карточке.
  **E3, 2.0.15.8:** реализован отдельный документ «Коммерческое предложение» со
  статусами, формами 8.5, печатью Devis A4, вводом заказа на основании,
  автоматическим статусом «Выигран», связями, меню, правами и семнадцатым
  сценарием уровня 3. Прежнее решение R12 «документ не переносим» отменено.
  Удаление старой ТЧ номенклатуры отложено до подтверждённого переноса всех баз
  клиентов; см. `docs/TECHDEBT.md`.
  **E4, 2.0.15.9:** реализован вид операции заказа клиента («Продажа» / «Производство»):
  значение заполнения и серверная страховка, «Производство» по коммерческому предложению с
  производимой номенклатурой, представление и заголовок «Заказ клиента на производство», общая
  команда «Заказы клиентов на производство» в разделе «Производство» для ролей с доступом к разделу,
  тумблер и колонка в списке заказов, обработчик обновления и восемнадцатый сценарий уровня 3.
  Смена вида у записанного заказа разрешена до E6. Прогон на базе ждёт полного обновления `build/ib`.
  **E5, 2.0.15.13:** у заказа на производство табличная часть материалов, связанная со строками
  товаров по коду строки, и колонка «Техкарта»: заполнение по действующим картам (строка и все
  строки), заказ по КП заполняется сразу, пересчёт и согласование при записи, очистка у продаж,
  отказ записи при разном составе одной продукции (решение владельца — уже в E5), девятнадцатый
  сценарий уровня 3. Прогон на базе ждёт обновления `build/ib`; клиентская часть — ручная приёмка.
  **E6, 2.0.15.14:** заказ клиента на производство проводится (продажа — нет, кнопок проведения у неё
  нет): проведение регистрирует версию технологической карты с датой проведения, когда состав
  продукции отличается от карты, и не регистрирует при совпадении или неизменном с прошлого проведения
  составе; право на изменение карты через заказ — только у ролей с правом на карты; отказы по составу
  и будущей карте; запрет смены вида проведённого заказа; проверка заполнения при записи из формы
  сохранена на сервере; блокировка карт продукции во всех путях записи карты; двадцатый сценарий уровня 3. Роли не менялись (решение владельца: `Кладовщик`
  не проводит). Прогон на базе ждёт обновления `build/ib`; формы — ручная приёмка.
  **E7, 2.0.15.15:** регистр `ПотребностиПроизводства`: проведённый заказ на производство пишет «Потребность» по
  продукции, заказ поставщику — «Заказано» (поле «Заказ клиента», кнопка «Заполнить по потребности», ввод на
  основании, скрытая колонка «Продукция»), поступление — «Пришло» (ввод на основании заказа клиента, колонка в
  списке), возврат поставщику — сторно; распределение по продукции в порядке строк, сверх потребности — фактическое
  (решения владельца 2026-09-13, одна сборка); отчёт «Потребности производства» в разделах «Производство» и «Закупки»;
  права чтения ролям закупок и производства; двадцать первый сценарий уровня 3. Обязательные ревью — субагенты
  Claude (Codex в лимите). Исправления по прогону на базе — 2.0.15.16–2.0.15.19; смок и 21 сценарий уровня 3 на
  обновлённой `build/ib` без ошибок (E6 и E7); формы — ручная приёмка.
  **E8, 2.0.15.20:** стадии заказа клиента на производство в регистре истории статусов: семь новых значений
  `СтатусыЗаказовПокупателя`, доли выполнения и индикатор `○◐●`, общий модуль `ПроизводственныеЗаказы` (каскад с
  поздней вехой — решение владельца 2026-09-14), пересчёт из заказа, закупок, выпуска, реализации, возврата от
  покупателя и шести денежных документов, в том числе при отмене проведения (явная очистка движений под блокировками
  проведения); стадия и индикатор в шапке заказа, колонка и оформление списка, «Детали», дашборд, АРМ, отбор «Заказы к
  отгрузке» в списке реализаций; индексы `Основание` у реализации, выпуска и возврата; двадцать второй сценарий
  уровня 3. Одна сборка; обязательные гейты — субагенты Claude (Codex в лимите до 2026-09-19). Смок и 22 сценария
  уровня 3 на обновлённой `build/ib` 2.0.15.20 без ошибок; формы — ручная приёмка.
  **E9, 2.0.15.21:** автовыпуск при отгрузке — общий модуль `Производство`: проведение реализации по заказу на
  производство создаёт и проводит один выпуск на секунду раньше (одинаковые позиции сводятся, серии сохраняются),
  материалы по составу заказа или по техкарте, партии закупок под заказ, затем общий остаток по сериям с «Из остатка»;
  к выпуску за вычетом выпущенного и не отгруженного по заказу; все проверки до записи (закрытый период по датам
  автовыпуска, проба заполнения и списания на самом объекте); реализация списывает продукцию по ФИФО; отмена проведения,
  удаление и пометка переносятся; запрет ручной правки, форма только для просмотра; «Из остатка» в стадиях, дефиците,
  поступлении и отчёте; переключатель в настройках «Производство»; двадцать третий сценарий уровня 3. Одна сборка;
  обязательные гейты — модель Codex через OpenCode (`gpt-6-astra`, временно), данные и безопасность — дополнительно
  субагенты Claude. Прогон на копии полностью обновлённой `build/ib` нашёл переменную `Документы` (имя свойства
  глобального контекста) и ошибку теста — исправлено в 2.0.15.22; 23 сценария уровня 3 и смок без ошибок; формы —
  ручная приёмка.
  **E10, 2.0.15.23:** самостоятельный выпуск и настройки — из формы ручного выпуска убран мёртвый код предка (проверка
  веса, доли стоимости, пустой серверный вызов цены на каждую строку) и пустая группа «Доля стоимости», колонка «Цена»
  оставлена; заполнение материалов по техкартам — процедура модуля менеджера `ВыпускПродукции` через фасад, одним
  серверным вызовом; константа «Упрощённый выпуск ГП» удалена (реструктуризация) — **Р10 закрыта в части выпуска**;
  совет перезаполнить выпуски при смене карты не учитывает автовыпуски; проводки выпуска записаны в блок 3 (Р1);
  двадцать четвёртый сценарий уровня 3. Одна сборка; обязательные гейты — модель Codex через OpenCode (`gpt-6-astra`,
  временно), данные и безопасность — дополнительно субагенты Claude. Смок и 24 сценария уровня 3 на копии полностью
  обновлённой `build/ib` 2.0.15.23 без ошибок с первого прогона; форма выпуска — ручная приёмка.
  **E11 (демо-часть), 2.0.15.24:** демо-набор контура 2mzl и торговли — `scripts/fixtures.sh --demo`: заказы клиента на
  производство по одиннадцати схемам (под заказ без остатка, из своего остатка, ручной выпуск, состав в заказе, частичная
  закупка и отгрузка, черновик, производство на склад, новая версия карты) на каждой стадии, КП, заказы-продажи, договоры,
  лиды и заявки во всех статусах; идемпотентно, одной транзакцией; двадцать пятый сценарий уровня 3. Найдено: «Получен от
  субподрядчика» не ставится поступлением на основании передачи (TECHDEBT). Решения владельца: демо-данные прямо в
  `build/ib`, объём — контур и торговля; инструкции maERP в базу знаний tinycio со скриншотами — следующим шагом (форма документа — скриншот на каждую закладку: шапка и табличные части, у каждого скриншота своё описание). Смок и
  25 сценариев на обновлённой `build/ib` до и после засева без ошибок; `build/ib` засеяна.
  **E12 (переопределён 2026-09-15 после разбора расширения drissotex, бриф `production-2mzl-e12-arm.md`):** рабочее место
  производства с вводом в таблицах от заказа, три сборки. **E12.1, 2.0.15.29:** закладки «Заказы» (тумблер «Производство /
  Все», стадия, индикатор, пять долей) и «Продукция» (строка создаёт заказ-черновик, выбор черновика клиента, правка и
  удаление строк с пометкой пустого черновика), кнопка «Статус» (в работу, в черновик, отменить), нижние закладки
  «Материалы» и «Документы» (цепочка второго уровня, автовыпуск виден), кнопки ввода на основании; двадцать седьмой
  сценарий уровня 3. Ожидает полного обновления `build/ib`, смока и прогона сценариев. **2.0.15.30–31** — правки владельца
  (панель фильтров, панели отмеченных строк, сворачиваемая цепочка, фильтры по вхождению). **E12.2, 2.0.15.32:**
  закладки «Заказы поставщику» и «Поступления» с вводом строк, проведением сразу, строками заказа поставщику,
  «Статусом» и удалением; двадцать восьмой сценарий уровня 3. **E12.3a, 2.0.15.33:** закладки «Выпуск» и «Отгрузки»
  (материалы по картам, цена заказа, автовыпуск, запрет правки реализации после позднего выпуска), «Выпуск по заказу»,
  фильтр нулевых цен прайса; двадцать девятый сценарий уровня 3. **E12.3b, 2.0.15.34:** закладка «Переработка»,
  «Поступление из переработки», продукция переработчика как выпуск в стадии и автовыпуске, рабочее место на начальной
  странице оператора; тридцатый сценарий уровня 3. **E12.4, 2.0.15.35:** косметика по замечаниям владельца —
  счётчики строк в заголовках нижних закладок, кнопки ввода на основании в панелях своих закладок, ширины колонок,
  движение продукции и материалов с итогами на закладке «Документы», количества в подсказке индикатора. E12 закрыт;
  дальше — вынос движка строк рабочего места в общий модуль (TECHDEBT) и приёмка владельцем.
- **Меню раздела «Производство» и отчёт себестоимости, 2.0.15.36** (план `docs/plans/production-menu.md`,
  ревью плана — `.ai/reviews/production-menu/02-plan-review-claude.md`): подсистемы `ПроизводственныеОперации`
  и `Справочники` переименованы в `Переработка` и `Нормативы`, «Выпуск продукции» вынесен в первый список
  раздела, регистр `ТоварыВПереработке` убран из меню, техкарты переехали в «Нормативы», номенклатура
  открывается тремя отборами, панель «Создать» оставлена только у техкарты, мёртвая команда
  `Document.ВыпускПродукции.Command.ВыпускПродукции` удалена; новый отчёт `СебестоимостьПродукции` по приходу
  регистра `ТоварыВНаличии` с вычетом НДС, права — `ОператорПроизводства`; тридцать первый сценарий уровня 3.
  Обязательный гейт `ai-review-plan` не прошёл (Codex в лимите до 19.09) — выполнен субагентом Claude.
- **Переработчики отдельной командой в разделе** — у `Catalog.Контрагенты` нет признака `Переработчик`
  (есть `Покупатель`, `Поставщик`, `Перевозчик`), отбирать не по чему. Нужен новый реквизит: реструктуризация
  справочника, который переносится из базы drissotex, — решение владельца. *(волна 12)*
- **Расшифровка себестоимости по материалам** — отчёт `СебестоимостьПродукции` показывает приход продукции, но не
  расход материалов того же документа. Отдельная группировка по расходу регистра. *(волна 12)*
- **Себестоимость единицы в общем итоге отчёта** — средневзвешенная по разной номенклатуре, смысла не имеет.
  Ограничение ресурса группировками в схеме компоновки в конфигурации не встречается ни разу, выдумывать XML
  не стали (tiny1C `workflow-005`). Смотреть показатель на уровне продукции. *(волна 12)*
- **Автовыпуск на границе суток** — `CommonModules/Производство/Module.bsl:767-771` ставит выпуск на секунду
  раньше реализации. Реализация ровно в 00:00:00 (импорт, ECommerce, перенос drissotex) уводит выпуск на
  предыдущие сутки, а 1-го числа — в предыдущий месяц: месячный отчёт себестоимости разойдётся с отгрузкой.
- Распределение себестоимости на несколько строк ГП в
  `ПоступлениеИзПереработки` — по весу или количеству; услуги переработчика в
  себестоимость ГП не входят. Добавить ТЧ услуг или связь с
  `ПоступлениеДополнительныхРасходов`. *(волна 7)*
- Услуга переработчика: долг формируется прямым движением, без договора, авансов
  и зачётов. Полный контур — через хозяйственную операцию документа. *(волна 12)*
- Розничная цена выпуска назначается только при единственной позиции продукции;
  для мультивыпуска поле скрыто. *(волна 12)*
- `ВыпускПродукции.Основание` — связь выбора только по организации.

**Зарплата и премии**

- Плановая ветка `РассчитатьПоказатель` осталась кодом предка: для показателей
  без анализа продаж премия всегда 0; ветки `Оплаты`/`ВыручкаОтОплат`/
  `Рентабельность` делают по запросу на показатель и сотрудника. Разбирать
  отдельно. *(волна 6)*
- «Чистая прибыль» по продавцам: затраты регистра `Убытки` не распределяются, по
  сотруднику чистая прибыль равна валовой. *(волна 6)*
- Праздники с плавающей датой (исламские, Пасха) в макет `ПраздничныеДни` не
  входят, переноса праздника с выходного нет — как и у предка. *(волна 6)*

**Взаиморасчёты и налоговый учёт**

- Отказ проведения налоговых документов по складу без признака «Налоговый учёт»:
  исторические документы drissotex перепроведутся только после простановки
  флага. Нужен массовый перенос на налоговый склад или снятие отказа — решение
  владельца при миграции клиента. *(волна 5)*
- Печать долга и оплат для `НалоговаяНакладная`: `ОплатыИДолгПоДокументу` и
  `ПолучитьЗадолженностьКлиентовПоКлассификации` считают по
  `ВзаиморасчётыСПокупателями`; при появлении такой печати нужна ветка по
  налоговому регистру. *(волна 5)*
- Долг в интерфейсе (списки РТУ и заказов, `Дашборд`, `СверкаВзаиморасчётов`)
  показывается без налоговых накладных — объединить с налоговым регистром, если
  понадобится. *(волна 5)*
- «Главная книга» сделана только по покупателям; вариант по поставщикам — при
  необходимости. *(волна 10)*

**Прочее**

- `УпаковкиЕдиницыИзмерения` не подчинён, а `ФормаЭлементаУпаковка` и
  `ОбработкаПолученияФормы` обращаются к `Владелец` (наследие предка). Форма
  защищена попыткой. **Р6.** *(смок 2026-08-25)*
- `Справочники.Контрагенты.ПолучитьРеквизитыПоИНН` падает на первой строке:
  берёт `ИТСЛогин`/`ИТСПароль`, которых в конфигурации нет; вызовов у функции
  нет. **Р9.** *(автотесты 2026-08-26)*
- Общая картинка `Взаимодействие16` осталась от удалённого
  `Документ.Взаимодействие` — убрать, если не понадобится. *(волна 8)*
- Колонка «Поставщик» в `ТоварыВНаличии.ВыборПартииПоОстаткам` берётся как
  `Партия.Контрагент` через составной тип: для оприходования, выпуска,
  пересортицы и акта разбора пусто. Отдельные представления партий по типу
  документа — при необходимости. *(волны 3 и 8)*
- Скрытому администратору `tinycio-1c` физлицо не создаётся: при интерактивном
  редактировании карточки его придётся заполнить. *(волна 8)*

## 9. Производительность

Этап производительности, отдельной волной.

- **27 виртуальных таблиц без параметров** и **86 вызовов
  `НайтиПоНаименованию`/`НайтиПоКоду` на сервере**. *(TECHDEBT: Код)*
- Пакетная печать карточек контрагентов — см. блок 7.

## 10. Чистота кода и именование

- **~100 устаревших вызовов после поднятия режима до 8.5.1** —
  `БлокироватьОкноВладельца` (61), `БлокироватьВесьИнтерфейс` (17),
  `КраткоеПредставлениеОшибки` (13), `ПодробноеПредставлениеОшибки` (8) и
  одиночные. Работают, но устарели. *(TECHDEBT: Платформа)*
- **105 имён со смесью алфавитов** — элементы стиля `BottonBackgroundColor`,
  `BottonFont`, `ButonTextColor` и прочие. Переименование через рефакторинг EDT,
  отдельными коммитами. *(TECHDEBT: Код)*
- **NFC/NFD в путях git** — часть кириллических путей хранится в NFD (наследие
  macOS). Привести к NFC одним переименовывающим коммитом.
- **Удалить общий реквизит `УдалитьАвторОбщий`** (шаг 2 `docs/plans/avtor-otvetstvennyi.md`) — после того как
  все базы клиентов пройдут 2.0.15.28 и владелец решит по записям журнала «Автор не перенесён». Вместе с ним —
  обработчик `ПеренестиАвтораВОтветственного` и сценарий `АвторВОтветственного`. *(TECHDEBT: Реквизит «Автор»)*
- **Свои реквизиты «Автор» у контрагентов и номенклатуры — объединить с «Ответственным»** (решение владельца
  2026-09-15): переименование без потери данных, перенос обработчиком, поле «Автор» убрать с форм. *(TECHDEBT:
  Реквизит «Автор»)*
- **Английская терминология**: коллизия `Товар = Product` и
  `Продукция = Finished product`, предложено `Goods` для «Товар».
  *(TECHDEBT: Платформа)*

## 11. Этапы 12 и 13

Крупные работы, объявленные в `docs/MERGE-DRISSOTEX.md`.

- **Этап 12 — интеграция с хабом tinycio**: Product Hub, публикация релизов и
  оповещений; сюда же свойства конфигурации из блока 1 и формат из
  `docs/UNF-ADOPTION.md`.
  - **Онбординг клиента через хаб** — план на стороне tinycio:
    `../tinycio/docs/plans/feature-maerp-onboarding.md` (2026-09-06). Хаб ведёт
    витрину с модулями и ценами, регистрацию, стадии объекта обслуживания и
    очередь заданий; на 1С-сервере их забирает PULL-раннер и запускает скрипты
    развёртывания (вне git). Промпты: хаб —
    `docs/hub/tinycio-start-prompt.md`, maERP (этап 12а) —
    `docs/hub/stage12a-headless-first-run-prompt.md`. На стороне maERP (§14 плана):
    - скрипты `create_base` / `publish_web` / `create_users` / `initial_fill` /
      `healthcheck` по контракту раннера: payload из `JOB_PAYLOAD`, результат
      в `JOB_RESULT_FILE`, `publish_web` возвращает `login_url`;
    - ~~**headless-первый запуск**~~ — сделано `2.0.14.11` (2026-09-06, этап 12а):
      обработка `НачальноеЗаполнение` (`/C "НачальноеЗаполнение;payload;result"`),
      общий модуль `НачальноеЗаполнениеВызовСервера`, контракт `docs/DEPLOY.md`,
      dev-скрипт `scripts/initial-fill.sh`, тест `scripts/initial-fill-test.sh`.
      Серверный `initial-fill.sh` для раннера — владелец, по контракту.
      *(TECHDEBT: Онбординг через хаб)*
    - язык интерфейса `/L` и веб-публикация базы — из блока 0; шаги описаны в
      `docs/DEPLOY.md`, серверные скрипты `publish_web`/`healthcheck` — владелец;
    - ~~`Vendor` / `Copyright` / `VendorInformationAddress`~~ — заполнены умолчаниями
      `2.0.14.11`, см. блок 1;
    - манифест модулей для лендинга не нужен: каталог живёт в хабе (14 модулей
      sales-каталога), `scripts/changelog.sh` остаётся для оповещений.
- **Этап 13 — рефакторинг регистра `Продажи`**: себестоимость уходит из
  регистра, прибыль собирается в отчётах из `Продажи` и профильных регистров
  себестоимости. Отсюда же закрывается вопрос «чистой прибыли» в «Ведомости по
  продажам» (решение 2026-08-24: НДС-ресурсы в `Продажи` не добавлять).

---

## Принято как есть

Решения приняты, работа не планируется. Если пункт всплывает снова — это не
новая находка.

- **Опечатка `ВзаиморасчётыСПоcтавщикамиНалоговый`** (латинская «c») — не
  переименовываем: регистр хранит данные клиентов, переименование только при
  большой реструктуризации с конвертацией (волна 11).
- **Константы `ImprimerBL` и `ImprimerFactureVentesEnDetail`** латиницей — не
  переименовываем (решение 2026-08-23).
- **uuid команд `ПечатьТоварнаяНакладная` / `…Old`** у нас и drissotex поменяны
  местами — объекты не перенумеровывались, учитывать при сравнении ролей по uuid.
- **`YoLetterUsage`** — в базе линтера остаются 58 имён метаданных с «ё» и
  производные, плюс `ПланыВидовХарактеристик.ПоказателиРасчётаПремий`. Правило не
  отключаем, база работает фильтром.
- **`CommonModuleNameWords` на `ОбновлениеПрограммыОбработчики`** — имя точнее
  описывает назначение, замечание принято как известное.
- **Комплекты**: количество в регистре `Продажи` — сумма единиц составляющих
  (количественные колонки отчётов завышены, суммы верны); комплекты с
  характеристиками не поддерживаются; прибыль по возврату комплекта не
  сторнируется; оприходование комплекта даёт двойной учёт. Ограничения Rayssouni,
  приняты (волна 10).
- **Решение R12 отменено в E3, 2.0.15.8:** отдельный
  `Документ.КоммерческоеПредложение` реализован; печать из заказа сохранена и
  общий механизм Devis A4 теперь обслуживает оба документа.
- **`СуммаНДС` в партиях**: исторические движения имеют НДС = 0 до
  перепроведения; приходы без НДС в ТЧ дают нулевой НДС по определению.
- **Запросы в циклах** — из 13 исходных осталось 7, все холодные (печать чека
  возвратов, обновление розничных цен, регистрация изменений обмена, клиент-банк).
  В горячих местах их нет.
- **Шапка `ВыпускПродукции` дублирует первую строку ТЧ** — осознанная
  денормализация ради списков, печати и совместимости, синхронизируется в
  `ПередЗаписью`.
- **Стандартные картинки платформы** (`БиблиотекаКартинок.Справочник`, `.Отчет`,
  `.РегистрНакопления`, `.РегистрСведений`, `.РегистрБухгалтерии`,
  `.УдалитьНепосредственно`) в конфигурации не заведены, но работают — внесены в
  известные расхождения `scripts/refcheck-known.txt`.
- **`ДополнительныеРасходы.РаспределитьНаСебестоимость`** — база пропорции НДС
  издержек проверена в волну 11, бага Rayssouni у нас нет.

## Правила, которые надо помнить при доработках

Не задачи, а условия: нарушишь — получишь дефект.

- **Видимость разделов по ролям** выведена из прав и проставлена в
  `Configuration/CommandInterface.cmi`. Меняешь права роли — пересматриваешь
  видимость.
- **Остатки при пакетной загрузке строк** заполнять одним вызовом
  `ТоварыВНаличии.ЗаполнитьОстаткиСтрок` после заполнения ТЧ, а не по строке.
- **Автозакрытие периода**: добавляешь денежный документ — пополняешь список
  имён в `РаботаСДокументами.ДокументПодлежитАвтозакрытию`.
- **Статус оплаты налоговой накладной** обновляется движениями
  регистратора-накладной: оплата после накладной попадает в статус только после
  её перепроведения (модель волны 5).
- **Комплекты не приходовать как товар** (кроме возвратов) — иначе двойной учёт.
