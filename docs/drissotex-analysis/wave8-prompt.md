# Промпт: волна 8 — закрытие переноса drissotex (дефекты, решения, техдолг)

Ты работаешь в репозитории maERP (`~/Dev/maERP`): тиражная конфигурация
1С:Предприятие 8.5.1, исходники — EDT-проект `src/cf` (`.mdo`, `.form`,
`.bsl`, `.dcs`, макеты `Templates/*/Template.mxlx` — XML, побайтно равный
`Template.xml` конфигуратора). Сначала прочитай `CLAUDE.md`,
`docs/MERGE-DRISSOTEX.md` (разделы «Побочные дефекты на нашей стороне»,
«Решения владельца» № 5, 6, 10, 11, «Не переносим» → «Их откаты», «Волна 2»
п. 2.1), `docs/TECHDEBT.md` целиком, `docs/rules-1c/README.md`. Промпты
прошлых волн — `wave4-prompt.md` … `wave7-prompt.md`: источники, правила,
формат отчёта те же. Волны 1–7 выполнены и запушены (`origin/develop`,
последний коммит `e4b309b`); в коммиты **не добавлять** трейлер
`Co-Authored-By`.

Это не перенос функционала, а зачистка: всё, что всплыло при сравнении с
drissotex и при переносе, но откладывалось. Большая часть — S; каждый пункт
— отдельный коммит, после каждого `scripts/lint.sh` (база —
`build/bsl-json.json` на момент старта, пересоздать
`build/bsl-json-baseline.json`; сравнение по парам (файл, код)) и
XML-проверка правленых `.mdo`/`.form`/`.mxlx`.

## Решения владельца

**Нужны до начала соответствующих пунктов** (спросить, если не даны в этом
же сообщении; где есть умолчание — применить его и отметить в отчёте):

- **№ 12 — `СуммаНДС`** (п. 8.5): ресурс в `ТоварыВНаличии`,
  `ДополнительныеРасходы`, `ТоварыВПереработке`, `СебестоимостьНДС` в
  выпуске и поступлении из переработки. Реструктуризация трёх регистров +
  перепроведение истории (исторические партии получат НДС = 0). Умолчания
  нет — без ответа пункт не делать.
- **№ 5 — список номенклатуры**: добавить колонки «Цена закупки» /
  «Розничная цена» рядом с «Продано за 12 мес.» (умолчание — добавить,
  запрос переписать: срез цен одним запросом по видимым строкам, не по
  всему регистру).
- **№ 6 — откаты РМК** (`PaiementDeCredit`/`Rembourser`,
  `ПолучитьДатуПоследнейПродажи`, BL с ценами, `МинимальнаяЦена = 0`,
  `ТолькоПросмотр` прошлых дней в пересчёте): умолчание — **не переносить**
  (уже действует), пункт только подтверждает и закрывает решение.
- **№ 10 — роли**: Оператор — полные права на `ПеремещениеТоваров`,
  `СписаниеТоваров`, `ОприходованиеТоваров`; Кладовщик теряет
  `ОтгрузкаТовара`; `Пользователи.ФизическоеЛицо` обязательное. Умолчание —
  первые два **перенести** (расширение прав безвредно; снятие у
  Кладовщика — только при подтверждении), обязательность физлица — **не
  переносить** (ломает служебных пользователей первого запуска).
- **№ 11 — `build/Rayssouni.zip`**: выгрузка 21.08, не анализировалась.
  Умолчание — распаковать в `build/rayssouni/`, сравнить с drissotex тем
  же методом (`cmp.py`/`three.py` из `docs/MERGE-DRISSOTEX.md` «Метод»;
  скрипты писать заново) и дать отчёт `docs/drissotex-analysis/rayssouni.md`
  без переноса.
- **Свойства конфигурации** (п. 8.1.6): `BriefInformation`,
  `DetailedInformation`, `Copyright`, `VendorInformationAddress`, `Vendor`,
  `Version` — нужны реальные значения (поставщик, адрес, версия). Без них —
  заполнить «maERP», «Tinycio» и версию `1.0.0.1`, отметить как заглушку.
- **Печать актов переработки** (п. 8.3): нужны ли «Акт передачи в
  переработку» и «Акт приёмки из переработки» сейчас. Умолчание — сделать
  по образцу `СписаниеТоваров/АктСписанияТоваров` с нашими хелперами
  `ВыводПечатныхФорм` (S–M).
- **Константы `ImprimerBL`/`ImprimerFactureVentesEnDetail`** — переименовать
  в `ПечататьНакладнуюВРознице`/`ПечататьЧекВРознице`? Умолчание — не
  переименовывать (данные клиента), пункт в TECHDEBT остаётся.

## Задача

8.1 → 8.2 → 8.3 → 8.4 → 8.5 (только при решении № 12) → 8.6 (проверка).

## Источники

- Наш код — `src/cf`; эталон для откатов «Наименование → Товар» и для
  сверки форматов макетов — XML drissotex `build/drissotex/` (пути NFD —
  искать через python с `unicodedata.normalize('NFC', …)`), диффы
  `build/drissotex-diffs/*.diff`. Правим **только `src/cf`**.
- Список побочных дефектов с местами: `docs/MERGE-DRISSOTEX.md` →
  «Побочные дефекты на нашей стороне» (1–9); техдолг по волнам —
  `docs/TECHDEBT.md` (разделы «Найдено при переносе волны 1…7»).
- Результаты сверок владельца на `build/ib` по `wave5-check.md`,
  `wave6-check.md`, `wave7-check.md` — если владелец прислал замечания,
  они идут первыми (п. 8.0 — багфиксы по сверке, по одному коммиту).

## Правила

- Те же, что в волнах 4–7: `НСтр` ru/fr/en/es, директивы явно, экспортные
  методы документировать, запросы не в цикле, блокировки по
  `docs/rules-1c/locks-and-transactions.md`, `Отказ = Истина` + сообщение
  через `ОбщегоНазначенияКлиентСервер.СообщитьПользователю`.
- Переименования метаданных — только через список в отчёте и отдельными
  коммитами (EDT-рефакторинг делает владелец); текстом в `.mdo` имена
  объектов не переименовывать.
- `CommonModuleInvalidType` — ложное; `YoLetterUsage` на именах метаданных
  с «ё» — в базу; в новых идентификаторах «ё» не использовать.
- Git: ветка `develop`, один коммит — один пункт, сообщения на русском,
  без `Co-Authored-By`; `build/`, `DT-INF` не трогать. Пуш в
  `origin/develop` — по явной просьбе владельца.

## Пункты

### 8.0. Багфиксы по сверкам владельца (если есть замечания) — S каждый

По каждому замечанию из прогонов `wave5/6/7-check.md`: воспроизвести
логику по коду, исправить, добавить контрольный запрос в соответствующий
`*-check.md`.

### 8.1. Побочные дефекты на нашей стороне — S каждый

1. `CommonForms/ИнтерфейсКлиент/Module.bsl:13` — `ПараметрыСеанса.Контрагенты`
   → `Контрагент` (проверить, что дальше по коду ожидается одно значение, а
   не список; если список — собрать из `Контрагент`).
2. Синоним `СтатьяДДС` = «Артикул» → «Статья ДДС» (fr «Poste de
   trésorerie», en «Cash flow item», es «Partida de flujo de caja») в
   `ПриходныйКассовыйОрдер`, `РасходныйКассовыйОрдер`,
   `ПоступлениеБезналичныхДенежныхСредств`, `СписаниеБезналичныхДенежныхСредств`,
   `ПрочиеРасходы`, `ПрочиеРасходыВозврат`, `ПоступлениеДополнительныхРасходов`,
   регистре `ДвиженияДенежныхСредств`; заголовки полей в формах, где
   заголовок задан явно, — тоже.
3. Строки формата в макетах: `CDT=2`/`BDC=2` → `ЧДЦ=2`, `DF = JJ. MM. yyyy`
   → `ДФ=dd.MM.yyyy` в `ПКО/Ticket`, `ПКО/ПриходныйКассовыйОрдер`,
   `РКО/Ticket`, `РКО/РасходныйКассовыйОрдер`, `РКО/LettreDePaiement`,
   `КассоваяСмена/EtatZ`, `РеализацияТоваровУслуг/Чек`,
   `НалоговаяНакладная/Чек`, `СверкаВзаиморасчётов/АктСверки(БезНДС)`.
   Найти все вхождения grep-ом по `Template.mxlx` (`Формат`/`<f>` в
   ячейках и параметрах), сверить с drissotex, где у них верно.
4. Откат «Наименование» → «Товар» в ~18 формах, где речь не о товаре
   (`Хозрасчетный/ФормаСчета`, `РегламентныеИФоновыеЗадания`,
   `РегистрацияИзмененийДляОбменаДанными`, `Банки/ЗагрузкаБанков`,
   `РабочиеМеста/ФормаВыбора`, `КлиентБанк`,
   `ФормаНастройкиУниверсальныйДрайвер`, `УпаковкиЕдиницыИзмерения` и др.):
   пройти grep-ом по `<value>Товар</value>` в заголовках `.form`, сравнить с
   drissotex (`<v8:content>`), вернуть «Наименование» там, где объект — не
   номенклатура. Список правленых форм — в отчёт.
5. `ЗаказПокупателя/ПечатьКоммерческоеПредложениеА4`: вызов `PrintDevisBL`
   с пятым аргументом — привести к сигнатуре (или добавить параметр, если
   он осмысленный: см. волну 4, `ВыводПечатныхФорм`).
6. Свойства конфигурации (`Configuration.mdo`) — по решению выше.
7. `ОперацияБух/ФормаДокумента`: `ХозрасчетныйПодразделениеДт/Кт` без
   `dataPath` — привязать к `Объект.…` по образцу drissotex.
8. Дубли без потребителей: локальные копии функций хранилищ настроек в
   `ГрупповоеИзменениеРеквизитов/Форма` заменить вызовами
   `ОбщегоНазначения`; `ДлительныеОперации.ЗапуститьВыполнениеВФоне`,
   `ОбщегоНазначенияКлиентСервер.СкопироватьРекурсивно` и соседи — удалить,
   если `grep` по `src/cf` не находит вызовов (список удалённого — в отчёт).
9. `ОбщегоНазначенияВызовСервера.ПолучитьИмяПринтераЧековНаСервере` —
   убрать `&НаСервере` (общий серверный модуль), проверить вызовы с клиента.

### 8.2. Техдолг волн 1–7 без решений — S каждый

- **JSON НДС платёжного документа**: читатели `РаботаСДокументами:615` и
  `CommonForms/РедактированиеНДСПлатёжногоДокумента` → `XMLЗначение`, как в
  `РассчитатьСуммуНДСПлатёжногоДокумента` (волна 1).
- **`ВыплатаЗарплаты/ManagerModule.bsl:56`** — имя файла «Relevé de paie»
  через `НСтр` (волна 1).
- **`ОбщегоНазначения.ЗначенияРеквизитовОбъекта`** — заменить на
  актуальную реализацию БСП (проверка `ЗначениеЗаполнено(Ссылка)`,
  псевдонимы `КАК`, `СокрЛП` имён, поддержка `ТЧ.(…)` не нужна): закрывает
  заметки волн 2, 6, 7. После замены пройти вызовы с
  `Номенклатура.ТипНоменклатуры`-подобными путями (ключи без точек
  остаются).
- **`.bslls.json`**: отключить `CommonModuleInvalidType`; `YoLetterUsage` —
  исключение на имена метаданных (если правило поддерживает) либо оставить
  в базе (волны 3, 5, 6).
- **РМК `VentesEnDetail`: локальная `PrintFacture`** — удалить мёртвый код
  (волна 4).
- **`ДенежныеСредстваОрганизаций.ПолучитьОплаты`**: использовать `&ДатаДок`
  в запросе (граница по дате документа), четыре `ИЛИ` по `ЕСТЬNULL` →
  `ВЫБОР`/соединение (волна 5). Поведение по ветке `ЗаказПокупателя` не
  менять.
- **`НалоговаяНакладнаяПокупка.mdo`** — убрать
  `ВзаиморасчётыСПокупателямиНалоговый` из `registerRecords` (волна 5;
  движений нет, реструктуризации не требует — проверить).
- **Серии готовой продукции в `ПоступлениеИзПереработки`** — для
  `ТЧГотоваяПродукция` выбор серии из справочника `СерииНоменклатуры` по
  владельцу (стандартный выбор), а не «по остаткам» (волна 7).
- **Выбор партии в `ПередачаВПереработку`** и `ВыпускПродукции`:
  форма `ВыборПартииПоОстаткам` показывает «Поставщика» только для ПТУ —
  представление партии по типу документа (волна 3), S.

### 8.3. Печать актов переработки — S–M (по решению)

Команды `ПечатьАктПередачи` / `ПечатьАктПриёмки` (новые uuid) в
`ПередачаВПереработку` / `ПоступлениеИзПереработки`, макеты по образцу
`СписаниеТоваров/АктСписанияТоваров` с логотипом и подвалом из
`ВыводПечатныхФорм` (`УстановитьЛоготип`, `РеквизитыОрганизацииДляПодвала`,
`ДобитьСтраницуПустымиСтроками`), валюта из
`Константы.ВалютаРегламентированногоУчета`, без хардкода локализации; в
акт приёмки — две таблицы (сырьё списано / продукция получена) с
себестоимостью из движений. Права на команды — `ОператорПроизводства`,
`Кладовщик`.

### 8.4. Решения № 5, 6, 10, 11 — S–M

- № 5: колонки цен в `Номенклатура/ФормаСписка` (динамический список:
  срез `ЦеныНоменклатуры`/`ЦеныНоменклатурыКонтрагентов` соединением в
  произвольном запросе списка, а не запросом при открытии); «Продано»
  оставить. Сверить с `Catalogs__Номенклатура__Forms__ФормаСписка*.diff`.
- № 6: зафиксировать в `docs/MERGE-DRISSOTEX.md` как принятое «не
  переносить» (если владелец не решил иначе); кода нет.
- № 10: права Оператора по решению; снятие `ОтгрузкаТовара` у Кладовщика
  — только при подтверждении; обязательность `ФизическоеЛицо` — нет.
- № 11: анализ `Rayssouni.zip` → `docs/drissotex-analysis/rayssouni.md`
  (что это за выгрузка, отличия от drissotex и от нас, есть ли что
  переносить); без переноса.

### 8.5. `СуммаНДС` — M, только при решении № 12

По плану волны 2 п. 2.1 и `production.md` § 2.4: ресурс `СуммаНДС` в
`ТоварыВНаличии`, `ДополнительныеРасходы`, `ТоварыВПереработке`;
`ДобавитьПриход/ДобавитьРасход(…, СуммаНДС = 0)` и все вызовы
(возвраты, оприходование, акт разбора, пересортица, переработка — пройти
`grep`), `СписатьНоменклатуру` (`СуммаНДСОстаток` пропорционально
списанию), `РаспределитьНаСебестоимость`, `СебестоимостьНДС` в
`СформироватьОприходованиеПоВыпускуПродукции` и
`…ПоПоступлениюИзПереработки`, `ОтразитьПередачу`; отчёты по
себестоимости — колонка НДС. Отметить в отчёте: реструктуризация трёх
регистров и необходимость перепроведения истории (`wave8-check.md`).

### 8.6. Проверка

- `docs/drissotex-analysis/wave8-check.md`: список форм с откатом
  заголовков для визуальной проверки; печать каждого макета из п. 8.1.3 с
  суммой и датой (формат `1 234,56` / `dd.MM.yyyy`); открытие
  `ИнтерфейсКлиент`; `ОперацияБух` с подразделениями; коммерческое
  предложение А4; при 8.3 — оба акта; при 8.5 — перепроведение и сверка
  НДС по партиям.
- `docs/MERGE-DRISSOTEX.md`: решения № 5, 6, 10, 11, 12 перевести в
  «принято/отклонено», раздел «Побочные дефекты» — пометить закрытые;
  `docs/TECHDEBT.md` — убрать закрытое, добавить новое.

## Результат

Отчёт в чате: что исправлено (с хешами), что отложено и почему, решения,
принятые по умолчанию, что проверить в EDT (Refresh, синтаксический контроль
правленых модулей, формы с откатом заголовков, макеты) и на данных
(`wave8-check.md`), новые находки для `docs/TECHDEBT.md`; отчёт по часам —
xlsx в `~/Downloads` по шаблону «Шаблон - отчёт по задаче.xlsx», без
итоговой строки.
