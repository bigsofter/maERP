# Промпт: волна 11 — стабилизация после переноса (данные, техдолг, подготовка релиза)

Ты работаешь в репозитории maERP (`~/Dev/maERP`): тиражная конфигурация
1С:Предприятие 8.5.1, исходники — EDT-проект `src/cf` (`.mdo`, `.form`,
`.bsl`, `.dcs`, макеты `Templates/*/Template.mxlx`). Сначала прочитай
`CLAUDE.md`, `docs/TECHDEBT.md` (целиком — это источник задач волны),
`docs/MERGE-DRISSOTEX.md` («Решения владельца», «Технические правила
переноса»), `docs/drissotex-analysis/wave9-check.md` и `wave10-check.md`.
Промпты и порядок прошлых волн — `wave4-prompt.md` … `wave10-prompt.md`:
правила, стиль коммитов и формат отчёта те же. Волны 1–10 выполнены и
запушены (`origin/develop`, последний коммит `89c2c8e`); перенос drissotex
и Rayssouni (приоритеты 1–2) закрыт. В коммиты **не добавлять** трейлер
`Co-Authored-By`; пуш в `origin/develop` — после завершения.

Это волна уборки: новых переносов нет, источник задач — `docs/TECHDEBT.md`
и обязательные разовые шаги из wave9/wave10-check. Каждый пункт — отдельный
коммит; после каждого `scripts/lint.sh` (пересоздать
`build/bsl-json-baseline.json` перед началом; сравнение по парам
(файл, код); `YoLetterUsage` на именах метаданных с «ё» — в базу) и
XML-проверка правленых файлов. Выполненные пункты TECHDEBT — удалять из
файла в том же коммите, что и правка.

## Задача

Порядок: 11.1 → 11.2 → 11.3 → 11.4. Блок 11.5 — только вопросы владельцу,
кода по нему не писать.

### 11.1. Разовые заполнения данных — M

Обязательные шаги из wave9/wave10-check сделать кодом, а не инструкцией:

- **`ЦенаВключаетНДС = Истина` историческим документам продаж**
  (wave9-check п. 2, сейчас ручной шаг). Разобраться, какой механизм
  однократных обработчиков есть в конфигурации: модуль
  `ОбновлениеИнформационнойБазыБПО` (если это рабочий механизм версионных
  обновлений — повесить шаг туда) или `CommonForms/ФормаПервогоЗапуска`
  (там уже есть создание физлиц пользователям, волна 8). Если ни один не
  подходит для существующих баз — сделать серверный шаг при первом входе
  администратора с константой-флагом «выполнено». Заполнять пустой флаг
  только у документов с датой до момента обновления, без перепроведения
  (прямое изменение реквизита, `ОбменДанными.Загрузка = Истина`).
- **`ОбъектРасчётов` в исторических движениях налоговых регистров**
  (wave10-check п. 3.3): тем же механизмом заполнить измерение по значению
  реквизита `ДокументДвижения` наборами записей, без перепроведения
  документов.
- Обе обработки идемпотентны (повторный запуск ничего не портит), пишут
  в журнал регистрации итог (сколько объектов обновлено).

### 11.2. Техдолг кодом — мелочи из TECHDEBT — M суммарно

Каждый пункт — свой коммит, TECHDEBT чистится по мере выполнения:

1. **Мёртвые копии печати** в менеджере `НалоговаяНакладная`
   (`PrintFacture`, `PrintTicket`, `PrintBonDeLivtaison`) — удалить,
   убедившись по поиску ссылок, что вызовов нет.
2. **Отладочный вывод** в `Reports/КарточкаСубконто/ManagerModule.bsl:543`
   — удалить.
3. **Отчёт «Зарплата сотрудника»**: собранные, но не выводимые
   предупреждения (переменная `Предупреждение`) — вывести в результат или
   убрать переменные.
4. **`ДополнительныеРасходы.РаспределитьНаСебестоимость`** — проверить у
   нас базу пропорции НДС издержек (у Rayssouni перепутана:
   `СтрДопРасход.СуммаНДС / СуммаТоваров × Стр.СуммаНДС` вместо базы
   `Стр.Сумма`); если у нас так же — исправить с сохранением копеечной
   сходимости (остаток на последнюю строку).
5. **Задание «Запрет отгрузок» без дедупликации**: не слать оповещение
   менеджеру повторно, пока по контрагенту не изменилось состояние
   (хранить последнюю отправку — `СистемныеОповещения` уже хранит записи,
   проверять наличие неотображённого оповещения с тем же текстом; без
   нового регистра, если можно обойтись).
6. **Остаток при массовом подборе** (`ДобавитьНоменклатуруПоШтрихкоду` —
   остаток запрашивается на каждую строку): пакетно, по образцу
   `ЗаполнитьОстаткиСтрок`.
7. **Подбор без характеристики/серии**: в `ФормаПодбора` для товара с
   характеристиками открывать выбор характеристики (по образцу
   автоподбора), а не добавлять строку с пустой.
8. **Справочные цены для динамических видов цен** (`ЦенаРозничная`,
   `ЦенаМинимальная`, `ЦенаПоТипуЦен` в строках показывают 0, если вид
   переведён на динамический расчёт): считать через
   `ПолучитьЦенуНоменклатуры`/`ДанныеЦенДляСписка`, батчем на табличную
   часть, без запросов в цикле.
9. **Запросы в циклах (13 мест по TECHDEBT)**: пройти список, исправить
   те, что в горячих местах (проведение, формы документов); по остальным —
   краткая оценка «можно оставить» с причиной в TECHDEBT.

Пункты 5–9 ранжируй по риску: если правка тянет за собой заметную
перестройку — не делать, оставить в TECHDEBT с пометкой «оценено в волну
11, отложено: причина».

### 11.3. Гигиена конфигурации — S

- **`НДСПродажи`** — регистр-сирота (менеджерный модуль есть, движений
  никто не пишет): подтвердить поиском по коду и удалить регистр с модулем
  (вопрос владельцу не нужен, если ссылок нет; если ссылки есть —
  в 11.5).
- **`YoLetterUsage` в базе** — свериться, что в
  `build/bsl-json-baseline.json` только имена метаданных с «ё»
  (`ВзаиморасчётыСПокупателямиНалоговый`, `ОбъектРасчётов`,
  `СпособРасчётаОтсрочки` и т. п.), случайные «ё» в комментариях и строках
  — исправить.
- **Опечатка `ВзаиморасчётыСПоcтавщикамиНалоговый`** (латинская «c») — НЕ
  переименовывать (данные клиентов), но зафиксировать в TECHDEBT решение:
  жить с ней или переименовать при следующей реструктуризации с
  конвертацией.

### 11.4. Подготовка релиза — S

- Свежая сборка: `scripts/lint.sh` чистый, XML-проверка, затем
  `scripts/dist-build.sh` (сборка .cf пакетным режимом) — убедиться, что
  собирается без ошибок. Сам релиз (merge `develop` → `main`, тег,
  GitHub Release с .cf) — **только после** прогона владельцем
  `wave5-check.md` … `wave10-check.md` на тестовой базе; в отчёте дать
  короткий чек-лист релиза.

### 11.5. Вопросы владельцу (кода не писать, собрать в отчёте)

1. **`ОбменССайтом`** — выпилить или оставить (2 плана обмена, обработка)?
2. **`МенеджерОборудования*`** — 5 модулей, ~2100 замечаний BSL LS:
   заморозить как есть или обновить с актуальной БПО?
3. **Хардкод пароля скрытого администратора** — вынести в параметр
   первого запуска / переменную окружения?
4. **Режим совместимости 8.3.23** — поднимать ли до актуального (отдельная
   волна с полным регрессом)?
5. **Свойства конфигурации** (`Vendor`, `Version`, описание, копирайт) —
   отложено с волны 8; заполнить к релизу?
6. **Видимость разделов по ролям** — на паузе с 2026-08-21; возвращаемся?
7. **«Главная книга» по поставщикам** — нужна ли парная к покупательской?
8. **«Чистая прибыль» без НДС себестоимости** — переносить ли ресурсы
   `СебестоимостьНДС`/`ДополнительныеРасходыНДС` в регистр `Продажи`
   (отдельная волна: `СформироватьДанные`, возвраты, корректировки)?

## Правила

Как в прошлых волнах: модули не копировать, писать по нашим образцам и
`docs/rules-1c` (запросы не в цикле, `НСтр` на 4 языка, директивы явно,
экспортные методы документировать, обработчики форм в `<handlers>`
Form.form); один коммит — одно осмысленное изменение, сообщения на русском.
`build/`, `DT-INF`, генерируемые XML не трогать.

## Результат

Отчёт в чате: сделано (с хешами), отложено с причинами, список вопросов
11.5 владельцу одним блоком, что проверить в EDT (Refresh; удаление
`НДСПродажи` — реструктуризация) и на данных (разовые заполнения 11.1 на
копии базы drissotex); TECHDEBT актуализирован; обновить
`docs/MERGE-DRISSOTEX.md`; отчёт по часам — xlsx в `~/Downloads` по шаблону
прошлых волн, без итоговой строки.
