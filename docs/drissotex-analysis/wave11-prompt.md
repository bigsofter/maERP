# Промпт: волна 11 — стабилизация после переноса (данные, техдолг, решения владельца)

Ты работаешь в репозитории maERP (`~/Dev/maERP`): тиражная конфигурация
1С:Предприятие 8.5.1, исходники — EDT-проект `src/cf` (`.mdo`, `.form`,
`.bsl`, `.dcs`, макеты `Templates/*/Template.mxlx`). Сначала прочитай
`CLAUDE.md`, `docs/TECHDEBT.md` (целиком — это источник задач волны),
`docs/MERGE-DRISSOTEX.md` («Решения владельца», «Технические правила
переноса»), `docs/drissotex-analysis/wave9-check.md` и `wave10-check.md`.
Промпты и порядок прошлых волн — `wave4-prompt.md` … `wave10-prompt.md`:
правила, стиль коммитов и формат отчёта те же. Волны 1–10 выполнены и
запушены (`origin/develop`); перенос drissotex и Rayssouni (приоритеты 1–2)
закрыт. В коммиты **не добавлять** трейлер `Co-Authored-By`; пуш в
`origin/develop` — после завершения.

Это волна уборки: новых переносов нет, источник задач — `docs/TECHDEBT.md`,
обязательные разовые шаги из wave9/wave10-check и решения владельца от
2026-08-24 (ниже). Каждый пункт — отдельный коммит; после каждого
`scripts/lint.sh` (пересоздать `build/bsl-json-baseline.json` перед началом;
сравнение по парам (файл, код); `YoLetterUsage` на именах метаданных с «ё» —
в базу) и XML-проверка правленых файлов. Выполненные пункты TECHDEBT —
удалять из файла в том же коммите, что и правка.

## Решения владельца (2026-08-24)

1. **`ОбменССайтом` — выпилить** (2 плана обмена, обработка, задание).
2. **`МенеджерОборудования*` — заморозить** как есть (не обновлять с БПО;
   замечания BSL LS остаются в базе линтера).
3. **Хардкод пароля скрытого администратора — оставить**, значение сменить
   на пароль администратора тестовой базы drissotex (согласовано устно,
   в документации не публикуется).
4. **Режим совместимости — поднимать** с 8.3.23 до актуального для
   платформы 8.5.
5. **Свойства конфигурации — заполнять**: описание конфигурации и картинку
   заставки исполнитель делает сам; **`Vendor` и копирайт пока не
   заполнять**. Система релизов/оповещений и стыковка с хабом tinycio —
   отдельный этап 12 (не в этой волне).
6. **Видимость разделов по ролям — возобновить** (пауза от 2026-08-21
   снята).
7. **«Главная книга» — только по покупателям**, парную по поставщикам не
   делать.
8. **В регистре `Продажи` не должно быть себестоимости**: чистая прибыль
   собирается на уровне отчётов и дашбордов из регистра `Продажи` (выручка)
   и профильных регистров себестоимости. Это рефакторинг — отдельный
   этап 13 (не в этой волне); НДС-ресурсы в `Продажи` НЕ добавлять,
   соответствующий пункт TECHDEBT переписать под это решение.

## Задача

Порядок: 11.1 → 11.2 → … → 11.7.

### 11.1. Разовые заполнения данных — M

Обязательные шаги из wave9/wave10-check сделать кодом, а не инструкцией:

- **`ЦенаВключаетНДС = Истина` историческим документам продаж**
  (wave9-check п. 2, сейчас ручной шаг). Разобраться, какой механизм
  однократных обработчиков есть в конфигурации: модуль
  `ОбновлениеИнформационнойБазыБПО` (если это рабочий механизм версионных
  обновлений — повесить шаг туда) или `CommonForms/ФормаПервогоЗапуска`
  (там уже есть создание физлиц пользователям, волна 8). Если ни один не
  подходит для существующих баз — серверный шаг при первом входе
  администратора с константой-флагом «выполнено». Заполнять пустой флаг
  только у документов с датой до момента обновления, без перепроведения
  (прямое изменение реквизита, `ОбменДанными.Загрузка = Истина`).
- **`ОбъектРасчётов` в исторических движениях налоговых регистров**
  (wave10-check п. 3.3): тем же механизмом заполнить измерение по значению
  реквизита `ДокументДвижения` наборами записей, без перепроведения.
- Обе обработки идемпотентны, пишут в журнал регистрации итог (сколько
  объектов обновлено).

### 11.2. Техдолг кодом — мелочи из TECHDEBT — M суммарно

Каждый пункт — свой коммит, TECHDEBT чистится по мере выполнения:

1. **Мёртвые копии печати** в менеджере `НалоговаяНакладная`
   (`PrintFacture`, `PrintTicket`, `PrintBonDeLivtaison`) — удалить,
   убедившись по поиску ссылок, что вызовов нет.
2. **Отладочный вывод** в `Reports/КарточкаСубконто/ManagerModule.bsl:543`
   — удалить.
3. **Отчёт «Зарплата сотрудника»**: собранные, но не выводимые
   предупреждения (переменная `Предупреждение`) — вывести в результат или
   убрать переменные.
4. **`ДополнительныеРасходы.РаспределитьНаСебестоимость`** — проверить у
   нас базу пропорции НДС издержек (у Rayssouni перепутана:
   `СтрДопРасход.СуммаНДС / СуммаТоваров × Стр.СуммаНДС` вместо базы
   `Стр.Сумма`); если у нас так же — исправить с сохранением копеечной
   сходимости (остаток на последнюю строку).
5. **Задание «Запрет отгрузок» без дедупликации**: не слать оповещение
   менеджеру повторно, пока по контрагенту не изменилось состояние
   (проверять наличие неотображённого оповещения с тем же текстом; без
   нового регистра, если можно обойтись).
6. **Остаток при массовом подборе** (`ДобавитьНоменклатуруПоШтрихкоду` —
   остаток запрашивается на каждую строку): пакетно, по образцу
   `ЗаполнитьОстаткиСтрок`.
7. **Подбор без характеристики/серии**: в `ФормаПодбора` для товара с
   характеристиками открывать выбор характеристики (по образцу
   автоподбора), а не добавлять строку с пустой.
8. **Справочные цены для динамических видов цен** (`ЦенаРозничная`,
   `ЦенаМинимальная`, `ЦенаПоТипуЦен` в строках показывают 0, если вид
   переведён на динамический расчёт): считать через
   `ПолучитьЦенуНоменклатуры`/`ДанныеЦенДляСписка`, батчем на табличную
   часть, без запросов в цикле.
9. **Запросы в циклах (13 мест по TECHDEBT)**: пройти список, исправить
   те, что в горячих местах (проведение, формы документов); по остальным —
   краткая оценка «можно оставить» с причиной в TECHDEBT.

Пункты 5–9 ранжируй по риску: если правка тянет заметную перестройку — не
делать, оставить в TECHDEBT с пометкой «оценено в волну 11, отложено:
причина».

### 11.3. Гигиена конфигурации по решениям — M

- **Выпилить `ОбменССайтом`** (решение 1): оба плана обмена, обработку,
  регламентное задание `ЗаданиеОбменССайтом`, ссылки в подсистемах, ролях и
  Configuration.mdo; поиском по коду убедиться, что живых вызовов нет
  (`ECommerce`-контур не задеть — он отдельный). Отдельный коммит.
- **`МенеджерОборудования*` — заморозить** (решение 2): код не трогать,
  в TECHDEBT переформулировать пункт как «заморожено решением 2026-08-24».
- **Сменить пароль скрытого администратора** (решение 3) во всех местах
  хардкода (поиск по старому значению), значение — пароль администратора
  тестовой базы drissotex; в docs/коммитах значение не цитировать.
- **`НДСПродажи`** — регистр-сирота: подтвердить поиском по коду отсутствие
  ссылок и удалить регистр с модулем.
- **`YoLetterUsage` в базе** — свериться, что в базе линтера только имена
  метаданных с «ё»; случайные «ё» в комментариях и строках — исправить.
- **Опечатка `ВзаиморасчётыСПоcтавщикамиНалоговый`** (латинская «c») — НЕ
  переименовывать (данные клиентов); зафиксировать в TECHDEBT: жить с ней,
  переименование — только при большой реструктуризации с конвертацией.

### 11.4. Свойства конфигурации и оформление — S

По решению 5 (без хаба — он в этапе 12):

- Заполнить `Configuration.mdo`: синонимы/описание конфигурации
  (`briefInformation`, `detailedInformation` — краткий и развёрнутый текст
  о maERP на ru/fr/en/es, написать самому: торгово-производственная ERP
  для малого и среднего бизнеса, продажи/закупки/склад/партии/производство/
  зарплата/налоговый контур/РМК, 4 языка). `Vendor` и копирайт НЕ трогать.
- `Version` — привести к схеме `1.0.X.Y` и зафиксировать правило
  версионирования в `docs/RELEASING.md` (см. 11.7).
- Картинки: `Configuration/Splash.jpg`, `Logo.jpg` и пустая
  `MainSectionPicture.png` (пункт TECHDEBT «Интерфейс») — сделать самому
  простую аккуратную заставку/логотип (текстовая композиция «maERP» на
  спокойном фоне, сгенерировать программно, например PIL; фирменных цветов
  не выдумывать — нейтральная гамма), корректных размеров для 8.5.

### 11.5. Видимость разделов по ролям — M

Решение 6, пауза снята. Настроить видимость подсистем верхнего уровня по
ролям (командный интерфейс подсистем): кладовщику — склад и закупки без
продаж/зарплаты/бухгалтерии, продажнику — без зарплаты и производства,
бухгалтеру — без склада и производства, кассиру (PDV) — только РМК, и т. п.
Перед работой составить матрицу «роль × раздел» из текущего состава ролей
(права уже выданы — видимость должна им соответствовать: раздел виден, если
у роли есть права хотя бы на один его объект), приложить матрицу в отчёт.
Реализация — через `CommandInterface.cmi`/видимость по ролям в подсистемах,
без изменения прав.

### 11.6. Режим совместимости — S, отдельным коммитом

Решение 4: поднять `compatibilityMode` с 8.3.23 до актуального для
платформы 8.5 (посмотреть допустимые значения схемы EDT). Отдельный коммит
без других изменений; в отчёте — явное предупреждение, что владельцу нужен
полный регресс на тестовой базе (проведение, печать, РМК, обмены) до
релиза.

### 11.7. Подготовка релиза — S

- `docs/RELEASING.md`: схема версионирования, чек-лист релиза (lint чистый,
  XML-проверка, `scripts/dist-build.sh`, прогон wave5–wave10-check + регресс
  режима совместимости у владельца, merge `develop` → `main`, тег `vX.Y`,
  GitHub Release с .cf).
- Прогнать сборку `scripts/dist-build.sh` — убедиться, что .cf собирается.
  Сам релиз — только после прогона проверок владельцем.

## Дальше (не в этой волне, зафиксировать в плане)

- **Этап 12 — интеграция с хабом tinycio** (решение 5, кросс-репо
  `~/Dev/tinycio`): maERP как продукт Product Hub (`services/product-service`
  — вебхуки HMAC, каталог модулей, зеркала), система релизов и оповещений об
  изменениях, схема документации для портала и выкатка версий с
  одновременной публикацией на сайт (`sites/tinycio-landing`, роут
  `/products/maerp`). Промпт писать после изучения обоих репозиториев.
- **Этап 13 — рефакторинг регистра `Продажи`** (решение 8): убрать
  себестоимость/допрасходы/валовую прибыль из регистра; отчёты, дашборды и
  премии собирают прибыль соединением `Продажи` (выручка, НДС) с
  профильными регистрами себестоимости (`ТоварыВНаличии`,
  `ДополнительныеРасходы`) по партиям. Затрагивает `СформироватьДанные`,
  возвраты, корректировки, `ВедомостьПоПродажам`, показатели премий и
  планов — отдельная волна с полным регрессом.

## Правила

Как в прошлых волнах: модули не копировать, писать по нашим образцам и
`docs/rules-1c` (запросы не в цикле, `НСтр` на 4 языка, директивы явно,
экспортные методы документировать, обработчики форм в `<handlers>`
Form.form); один коммит — одно осмысленное изменение, сообщения на русском.
`build/`, `DT-INF`, генерируемые XML не трогать. Пароли в файлы и логи не
писать (кроме самого места хардкода по решению 3).

## Результат

Отчёт в чате: сделано (с хешами), отложено с причинами, матрица
«роль × раздел», что проверить в EDT (Refresh; удаление `НДСПродажи` и
`ОбменССайтом`, смена режима совместимости — реструктуризация/регресс) и на
данных (разовые заполнения 11.1 на копии базы drissotex); TECHDEBT
актуализирован (решения 2026-08-24 вписаны); обновить
`docs/MERGE-DRISSOTEX.md`; отчёт по часам — xlsx в `~/Downloads` по шаблону
прошлых волн, без итоговой строки.
