# Техдолг

Журнал находок: что отложено, почему и чем грозит. Порядок разбора,
зависимости и развилки, ждущие решения владельца, — в `docs/ROADMAP.md`.
Новая находка сначала сюда, работа из неё планируется там.

## Интерфейс

- **Видимость разделов по ролям** — сделано в волну 11: матрица выведена из
  прав (раздел виден, если у роли есть View/Use хотя бы на один документ,
  журнал, отчёт или обработку раздела), уточнения владельца: PDV — только
  Продажи (РМК), MobileClient — разделы скрыты (мобильный интерфейс свой);
  проставлено в `Configuration/CommandInterface.cmi`. При изменении прав
  ролей пересматривать и видимость.
- **Язык интерфейса платформы — испанский** (найдено 2026-08-31 по скриншоту владельца).
  Строки конфигурации выходят по-французски, а платформенные — по-испански: заголовки
  стандартных групп командного интерфейса («Otro», «Informes», «Servicio»), строка
  состояния («Llamadas actuales»), баннер учебной версии. Это не дефект конфигурации:
  так настроен клиент. Лечится в списке баз — «Язык интерфейса» = французский, либо
  ключом запуска `/L fr`. Проверить у клиентов при установке: половина интерфейса на
  чужом языке выглядит как брак решения.
- **Проход по ~425 формам в интерфейсе 8.5** — формы без
  `preferableInterface`; негодные точечно закреплять за «Такси». С 2026-08-27
  идёт заодно с приведением раскладки к виду УНФ: правила — `docs/FORMS-STYLE.md`,
  пилот (форма реализации, карточка и список номенклатуры) в 2.0.13.1, дальше
  волнами по подсистемам.

## Решения, которые надо принять

- **`МенеджерОборудования*` — заморожено (решение 2026-08-24)** — 5 модулей, ~2100 замечаний BSL LS,
  вызовы несуществующих методов БСП. Нужно ли торговое оборудование.
- ~~**Хардкод пароля** скрытого администратора `tinycio-1c` в
  `ФормаПервогоЗапуска`~~ — **закрыто 2026-09-06 (2.0.14.11)**: пароль генерируется
  случайно в визарде (показ один раз на последнем шаге) либо приходит из payload
  headless-заполнения (`docs/DEPLOY.md`); зашитая учётка `AddAuthentication` в
  `ПроцедурыРегламентныхЗаданий.ЗакрытиеСеансов` убрана. Аутентификация ОС /
  выделенная роль для поддержки — отдельным решением, если понадобится.

## Код

- **`Справочник.Банки`: загрузка классификатора с rbc.ru** — виндовые
  разделители путей, `КопироватьФайл` по http; мертво на macOS/Linux
  сервере и бессмысленно вне РФ. Выпилить или заменить источник.
- **Запросы в циклах** — оценено в волну 11: BSL LS находит 7 мест из 13
  исходных (остальные закрыты волнами 4–10), все холодные, оставлены как
  есть: печать чека возвратов (`ВозвратОтПокупателя(Налоговый)`,
  `PrintTicket`) — цикл по одному документу (отбор `Ссылка = &Ref`), запрос
  оплаты выполняется один раз на печать; `ОбновлениеРозничныхЦен` — цикл по
  товарным группам правил ценообразования (единицы, регламентное задание);
  `РегистрацияИзмененийДляОбменаДанными` (2 места) и `КлиентБанк` — редкие
  административные обработки; `ОбменССайтом` — выпиливается решением
  2026-08-24. В горячих местах (проведение, формы документов) запросов в
  циклах не осталось.
- **Виртуальные таблицы без параметров** (27),
  **`НайтиПоНаименованию`/`НайтиПоКоду` на сервере** (86) — этап
  производительности.
- **Опечатки в именах метаданных**: `ВзаиморасчётыСПоcтавщикамиНалоговый`
  (латинская «c») — решение волны 11: НЕ переименовывать, живём с ней
  (регистр хранит данные клиентов, переименование — только при большой
  реструктуризации с конвертацией). Остальные: элементы стиля
  `BottonBackgroundColor`, `BottonFont`, `ButonTextColor`; всего 105 имён
  со смесью алфавитов — переименование через рефакторинг EDT, отдельными
  коммитами.
- **NFC/NFD в путях git** — часть кириллических путей хранится в NFD
  (наследие macOS). Привести к NFC одним переименовывающим коммитом.

## Найдено при сравнении с drissotex (2026-08-22)

Подробности и план переноса — `docs/MERGE-DRISSOTEX.md`, раздел
«Побочные дефекты на нашей стороне».

- **Свойства конфигурации**: описание (`BriefInformation`,
  `DetailedInformation` на 4 языках), `Version` (схема `1.0.X.Y`) заполнены
  волной 11. Заставка и логотип лежат файлами в `Configuration/` (`Logo.jpg`,
  `Splash.jpg`, `MainSectionPicture.png`), но в `Configuration.mdo` элементы
  `<logo/>` и `<splash/>` пусты — файлы ни к чему не привязаны; картинка
  главного раздела с 2.0.14.17 задаётся общей картинкой `Главное128`
  (`<mainSectionPicture>`), `MainSectionPicture.png` после проверки в EDT
  удалить. `Vendor`, `Copyright`, `VendorInformationAddress` заполнены
  этапом 12а (TinyCIO).

## Найдено при переносе волны 2 (2026-08-22)

- **Остаток при массовом подборе** — оценено в волну 11: пакетных вызовов
  не осталось. `ДобавитьНоменклатуруПоШтрихкоду` и
  `УстановитьПараметрыНоменклатуры` вызываются только одиночно из
  интерактивных обработчиков (скан, подбор, «при изменении»), данные товара,
  цена и остаток приходят одним серверным вызовом
  `ПолучитьДанныеНоменклатуры`; единственный цикл — корзина ЛКП, где остаток
  не запрашивается (в ТЧ нет колонки «Остаток»). Если появится пакетная
  загрузка строк — остатки заполнять одним вызовом
  `ТоварыВНаличии.ЗаполнитьОстаткиСтрок` после заполнения ТЧ.
- **`ЗаказПокупателя`: остаток по складу пользователя** — у заказа нет
  реквизита `МестоХранения`, колонка «Остаток» считается по складу из
  настроек пользователя; при пустой настройке — по всем складам. Решить,
  нужен ли заказу склад отгрузки.

## Найдено при переносе волны 3 (2026-08-22)

- **Форма `ТоварыВНаличии.ВыборПартииПоОстаткам`**: колонка «Поставщик»
  берётся как `Партия.Контрагент` через составной тип — для
  `ОприходованиеТоваров`/`АктРазбора`/`ПересортицаТоваров` будет пусто.
  При необходимости — отдельные представления партий по типу документа.
## Найдено при переносе волны 4 (2026-08-23)

- **Константы `ImprimerBL` и `ImprimerFactureVentesEnDetail`** — имена
  латиницей; решение владельца 2026-08-23: не переименовывать.
- **Карточки контрагентов в печати** — `Справочники.Контрагенты.ПолучитьКарточку`
  вызывается по разу на документ и организацию (кэш на время одной печати),
  а не одним запросом на пакет документов. Для массовой печати сотен
  документов стоит сделать пакетный вариант `ПолучитьКарточки(Массив, Дата)`.
- **Метки IF / PATENTE / RC / CNSS / ICE** в подвале реквизитов организации
  и шапках Devis/BL оставлены литералами (марокканские идентификаторы,
  одинаковые на всех языках); при выходе на другие рынки вынести в
  настройку организации.
- **`ДобитьСтраницуПустымиСтроками`** ограничена высотой страницы через
  `ПроверитьВывод`, предохранитель 500 строк — на случай области нулевой
  высоты. Если макет окажется без области `EmptyLine`, вывод упадёт на
  `ПолучитьОбласть`.
- **uuid команд РТУ** `ПечатьТоварнаяНакладная` / `ПечатьТоварнаяНакладнаяOld`
  у нас и drissotex поменяны местами (`a3159cc9…`/`71341228…` против
  `19b3a458…`/`a3159cc9…`) — существующие объекты не перенумеровывались;
  при сравнении ролей по uuid учитывать.
- **Макет `DevisBL` (A5) drissotex** потерял строки «Commandé par» и
  «Adresse de livraison» предыдущей версии — при необходимости адреса
  доставки вернуть область в макет и параметр `АдресДоставки` в код.

## Найдено при переносе волны 5 (2026-08-23)

- **Отказ проведения налоговых документов по складу без признака
  «Налоговый учёт»** — исторические документы drissotex перепроведутся
  только после простановки флага (см. `wave5-check.md` п. 3); если у
  клиента налоговые накладные шли по обычным складам, нужен массовый
  перенос на налоговый склад или снятие отказа (решение владельца).
- **Печать долга и оплат для налоговых документов**: `ОплатыИДолгПоДокументу`
  и `ПолучитьЗадолженностьКлиентовПоКлассификации` считают по
  `ВзаиморасчётыСПокупателями`; для `НалоговаяНакладная` печатные формы
  с оплатами не вызываются, но при появлении такой печати нужна ветка по
  налоговому регистру.
- **Потребители долга в интерфейсе** (списки РТУ/заказов, `Дашборд`,
  `СверкаВзаиморасчётов`) показывают долг без налоговых накладных —
  ожидаемое следствие решения № 2; при необходимости объединить с
  налоговым регистром.
- **`YoLetterUsage`** — аудит в волну 11: в базе линтера остаются только
  имена метаданных с «ё» (58 имён: регистры взаиморасчётов, `ОбъектРасчётов`,
  `БанковскийСчёт` и т. п.) и производные от них идентификаторы; случайные
  «ё» в текстах сообщений исправлены. Правило не отключаем, база — фильтр.

## Найдено при переносе волны 6 (2026-08-23)

- **Плановая ветка премий (`РассчитатьПоказатель`)** осталась кодом предка:
  для показателей без анализа продаж премия всегда 0 (формула
  `Стоимость / 100 * ВыполнениеВЗачёт` закомментирована в предке), ветки
  `Оплаты`/`ВыручкаОтОплат`/`Рентабельность` по-прежнему делают по запросу
  на показатель и сотрудника (кэш покрывает только `Выручка`/`Прибыль`/
  `ЧистаяПрибыль`); вариант `ПоПодразделению` получает список сотрудников
  подразделения отдельным запросом. Не трогалось намеренно (решение № 8,
  «результат по плану не менять») - разобрать отдельно.
- **`ПолучитьСуммыПоказателяПродажПоПродавцам` и «Чистая прибыль»**: затраты
  регистра `Убытки` по продавцам не распределяются, для варианта «по
  сотруднику» чистая прибыль = валовая прибыль продавца; при необходимости
  распределять пропорционально выручке.
- **Праздники с плавающей датой** (исламские праздники Марокко, Пасха) в
  макет `ПраздничныеДни` не входят - добавляются вручную в форме календаря;
  переноса праздника, попавшего на выходной, на рабочий день нет (как и в
  предке).
- **`YoLetterUsage`** на обращениях к `ПланыВидовХарактеристик.ПоказателиРасчётаПремий`
  - как и для регистров с «ё», до переименования или исключения в
  `.bslls.json`.

## Найдено при переносе волны 7 (2026-08-23)

- **Распределение себестоимости на несколько строк ГП** в
  `ПоступлениеИзПереработки` - по весу `Количество × Σ компонентов техкарты`
  либо по количеству; стоимость услуг переработчика (счёт подрядчика) в
  себестоимость ГП не включается - при необходимости добавить ТЧ услуг или
  связь с `ПоступлениеДополнительныхРасходов`.
- **`ВыпускПродукции.Основание`**: связь выбора только по организации;
  отбора по контрагенту/договору нет (в выпуске нет таких реквизитов).
## Найдено при закрытии переноса, волна 8 (2026-08-23)

- **`Документ.Взаимодействие`** удалён 2026-08-23 (наследие СПА-конфигурации,
  не компилировался); общая картинка `Взаимодействие16` осталась — убрать,
  если не понадобится.
- **`СуммаНДС` в партиях**: исторические движения регистров
  `ТоварыВНаличии`/`ДополнительныеРасходы` имеют НДС = 0 до перепроведения
  документов; приходы без НДС в ТЧ (оприходование, пересортица по цене,
  акт разбора без НДС в компонентах) дают партии с нулевым НДС — по
  определению. Отчёт «Товары в наличии» и отчёт по переработке НДС пока не
  показывают.
- **Первый запуск**: скрытому администратору `tinycio-1c` физлицо не
  создаётся (пишется программно); при интерактивном редактировании его
  карточки потребуется заполнить физлицо.
- **Форма `ТоварыВНаличии.ВыборПартииПоОстаткам`**: колонка «Поставщик» —
  `Партия.Контрагент` через составной тип; для партий без контрагента
  (оприходование, выпуск, пересортица, акт разбора) пусто — ожидаемо,
  отдельные представления по типу документа не делались.

## Платформа

- **Режим совместимости** — поднят с 8.3.23 до 8.5.1 в волну 11; до релиза
  обязателен полный регресс владельца на тестовой базе (проведение, печать,
  РМК, обмены).
- **Устаревшие вызовы после поднятия режима до 8.5.1** — BSL LS показал
  ~100 мест `DeprecatedMethodCall` (в базе линтера): `БлокироватьОкноВладельца`
  (61), `БлокироватьВесьИнтерфейс` (17), `КраткоеПредставлениеОшибки` (13),
  `ПодробноеПредставлениеОшибки` (8) и одиночные. Вызовы работают, но
  устарели — заменить на актуальные аналоги отдельной задачей.
- **Английская терминология**: коллизия `Товар = Product` и
  `Продукция = Finished product`; предложено `Goods` для «Товар».

## Найдено при переносе волны 9 (2026-08-23, Rayssouni часть 1)

- **«Чистая прибыль» в «Ведомости по продажам»** считается как выручка без
  НДС минус полная себестоимость (входной НДС не выделен). Решение
  2026-08-24: НДС-ресурсы в `Продажи` НЕ добавлять — наоборот, этапом 13
  себестоимость уходит из регистра, прибыль собирается в отчётах из
  `Продажи` и профильных регистров себестоимости.
- **Автозакрытие периода**: список исключений — строка с именами документов
  в `РаботаСДокументами.ДокументПодлежитАвтозакрытию`; при добавлении новых
  денежных документов список нужно пополнять.
- **Подбор товара** не выбирает серию (характеристика выбирается с
  волны 11); при появлении серийного учёта в подборе добавить шаг выбора
  серии по образцу характеристики.
- **Динамическая цена от последнего прихода** берёт прайс поставщиков без
  отбора по характеристике (регистр хранит характеристику, поиск в
  `ДинамическаяЦена` — только по номенклатуре, как у среза `ПоследнийПрайс`).
- **Решение R12 отменено в E3, 2.0.15.8:** отдельный документ
  `КоммерческоеПредложение` реализован. Печать Devis из заказа сохранена;
  предложение подключено к тому же механизму печати без копирования внешнего
  модуля Rayssouni.

## Найдено при переносе волны 10 (2026-08-24, Rayssouni часть 2)

- **Количество комплекта в регистре «Продажи»** — сумма единиц составляющих,
  а не количество комплектов (свёртка группирует партии составляющих под
  комплектом, количества складываются). Суммы корректны; количественные
  колонки отчётов по продажам для комплектов завышены.
- **Комплекты с характеристиками не поддерживаются**: составляющие
  раскладываются без характеристики, и строка продажи комплекта с
  характеристикой не найдёт свои партии при свёртке (себестоимость нулевая).
  Комплекты заводить без характеристик.
- **Возврат комплекта от покупателя** приходует комплект как товар (партия
  комплекта), уменьшение прибыли по возврату не находит партии составляющих —
  прибыль по возврату комплекта не сторнируется. Ограничение Rayssouni, принято.
- **Оприходование/инвентаризация комплекта** создаёт складской остаток самого
  комплекта, который продажей не списывается (списываются составляющие) —
  двойной учёт. Комплекты не приходовать как товар (кроме возвратов).
- **Статус оплаты налоговой накладной** в списке обновляется движениями
  регистратора-накладной: оплата, проведённая после накладной, попадает в
  статус после перепроведения накладной (модель волны 5).
- **«Главная книга» — только по покупателям** (по образцу «Расчеты с
  покупателями»); вариант по поставщикам — при необходимости.
- **`ДополнительныеРасходы.РаспределитьНаСебестоимость`**: проверено в
  волну 11 — у нас база пропорции НДС издержек корректна (`Стр.Сумма`,
  остаток на последнюю строку); бага Rayssouni (`Стр.СуммаНДС` в базе) нет.

## Найдено в волне 12 (2026-08-25, производственный контур)

- **Шапка `ВыпускПродукции` дублирует первую строку `ТЧГотоваяПродукция`**
  (номенклатура, характеристика, серия, упаковка, количества) — денормализация
  для списков, печатных форм, розничной цены и обратной совместимости;
  синхронизируется в `ПередЗаписью`. Если дубль надоест — убирать вместе со
  списковыми колонками и печатью.
- **Розничная цена выпуска** пишется только при единственной позиции
  продукции; для мультивыпуска цены не назначаются (поле скрыто).
- **Услуга переработчика — упрощённые взаиморасчёты**: долг формируется
  прямым движением (без договора, авансов и зачётов); объект расчётов — сам
  документ поступления. Если понадобится полный контур (авансы, сверка) —
  вводить хозяйственную операцию документу.
- **Статус «Доставлен»** по-прежнему никем не пишется (существующее
  поведение продажного контура, волной 12 не менялось).

## Найдено смок-тестами (2026-08-25, первый прогон CheckConfig)

- **Валютный пересчёт проводок `ОперацияБух` не реализован**: регистра курсов
  валют в maERP нет, `ПересчетСуммыПоКурсу` возвращает сумму без пересчёта
  (пересчёт был на `РаботаСКурсамиВалют` из БП). Валютные суммы Дт/Кт при
  валюте, отличной от базовой, дадут неверные суммы РУ/УУ/ФО. Вводить регистр
  курсов — только вместе с полноценным валютным контуром.
- **Подбор субконто без отборов**: `ОбработатьНачалоВыбораСубконто` в
  `ОперацияБух` работал через `ОбщегоНазначенияБПКлиент` (БП) — теперь
  стандартный выбор платформы по типу субконто, без отборов по организации.
- **Статус отражения в реглучёте всегда пуст**: механизм
  `РеглУчетПроведениеСервер` (БП) не перенесён, форма `ОперацияБух` показывает
  «Документ не проведён» вместо статусов отражения.
- **Битые ссылки в справке бухобъектов** — закрыто 2026-08-26: 55 перекрёстных
  ссылок сняты, текст справки сохранён, CheckConfig с тех пор зелёный. Если
  бухгалтерский контур будет доводиться, справку писать заново. Было: отчёты АнализСубконто,
  АнализСчета, КарточкаСубконто, КарточкаСчета, ОСВ, ОСВ по счёту,
  ОборотыСчета, ОтчётПоПроводкам; ПВХ ВидыСубконтоХозрасчетные, ПланСчетов и
  РегистрБухгалтерии Хозрасчетный, справка формы ОперацияБух, картинка в
  справке ГрупповоеИзменениеРеквизитов, цвет в макете FactureSocassif
  (РеализацияТоваровУслуг). На рантайм не влияет; чистить справки предка
  отдельной волной, до тех пор CheckConfig не будет полностью зелёным.
- **`УпаковкиЕдиницыИзмерения` не подчинён, а код рассчитан на владельца**:
  `ФормаЭлементаУпаковка` и `ОбработкаПолученияФормы` менеджера обращаются к
  `Владелец`, которого у справочника в maERP нет (наследие предка, где
  упаковки были подчинены). Форма защищена попыткой (единица владельца
  остаётся пустой в наименовании); решить судьбу подчинённости при ревизии
  упаковок.

## Найдено в волне У1 (2026-08-26, механизм обновления)

- **Константа `НомерВыполненногоЗаполненияДанных` живёт лишний релиз**: её
  читает конвертер `ОбновлениеПрограммыОбработчики.СтартоваяВерсияИнформационнойБазы`,
  превращая номер выполненного шага прежнего механизма в стартовую версию базы.
  Удалить константу можно только после того, как все базы клиентов пройдут
  версию 2.0.12 (после обновления запись версии есть в `ВерсииПодсистем`, и
  конвертер больше не вызывается). Пока висит пустой объект метаданных.
- **Отложенные обработчики обновления не реализованы** (решение этапа У1):
  все обработчики выполняются в момент входа, монопольные — под монопольным
  режимом. Если появится обработчик, перебирающий миллионы записей, вход в
  программу будет ждать его; тогда добавлять отложенное выполнение
  (архитектура регистрации это допускает: у обработчика уже есть
  `РежимВыполнения`).
- **`CommonModuleNameWords` на `ОбновлениеПрограммыОбработчики`**: BSL LS
  требует, чтобы имя общего модуля состояло из слов своего словаря, и ругается
  на «Обработчики» (тот же случай, что у `ПроцедурыРегламентныхЗаданий`).
  Имя оставлено — оно точнее описывает назначение модуля; замечание принято
  как известное.

## Найдено в волне У2 (2026-08-26, подсистема печатных форм)

- **Модуль `ABCАнализ` — перенесённое наследие.** При разборе
  `ВыводПечатныхФорм` две функции ABC-классификации (200 строк) выделены в
  отдельный модуль: они не относятся к печати и вызываются только из выражений
  схемы компоновки отчёта `ABCАнализПродаж`. Код перенесён как есть, вместе со
  своими замечаниями BSL LS (`CyclomaticComplexity`, `NestedStatements`,
  `CommentedCode`, `MissingSpace`, `SpaceAtStartComment`, `DeprecatedMessage`
  — устаревший `Сообщить`, `LatinAndCyrillicSymbolInWord`, `MagicNumber`,
  `DuplicateStringLiteral`, `IfElseIfEndsWithElse`, `CanonicalSpellingKeywords`).
  Переписывать вместе с ревизией самого отчёта: без тестов на данных трогать
  расчёт классов рискованно. Baseline линтера обновлён под новое имя файла.
- **Запрос `ПечатныеФормы.СформироватьТабличныеДанные` разыменовывает поля
  через точку** (12 замечаний `QueryNestedFieldsByDot`): перенесён из
  `ВыводПечатныхФорм` без изменений, диагностика подавлена в комментарии
  метода. Переписать на соединения при ревизии печатных форм реализации.
- **Фон и бланк организации в печати не участвуют.** Реквизиты
  `ФонПечатныхФорм`, `БланкШапка`, `БланкПодвал` заполняются в карточке
  организации, но ни одна печатная форма их не читает (единственная попытка —
  закомментирована в налоговой накладной). Подсистема подставляет только
  логотип и печать организации; довести до единой точки вместе с остальным
  оформлением печатных форм.

## Найдено прогоном автотестов 2026-08-26 (версия 2.0.12.10)

- **Бухгалтерский контур `ОперацияБух` ссылается на объекты, которых в maERP
  нет.** Документ достался от предка вместе с кодом 1С:Бухгалтерии. После
  правки формы (валюты учёта и пересчёт в валюту финотчётности) остались
  обращения к отсутствующим объектам в путях, которые смок-тест не проходит —
  они сломаются при работе с проводками, а не при открытии формы:
  `ПланыВидовХарактеристик.СтатьиДоходов` и тип
  `ПланВидовХарактеристикСсылка.СтатьиРасходов`
  (`Forms/ФормаДокумента/Module.bsl`, `ОпределитьНеПринимаемыеДоходы`,
  `ОпределитьНеПринимаемыеРасходы`), `Перечисления.ВидыРасходовНУ` (там же),
  `РегистрыНакопления.НДСЗаписиКнигиПокупок`,
  `РегистрыНакопления.НДСЗаписиКнигиПродаж`,
  `РегистрыСведений.ЖурналУчетаСчетовФактур` (список регистров движений).
  Решать вместе с судьбой самого документа: либо переносить налоговый учёт
  доходов и расходов целиком, либо вырезать эти ветки.
- **Неиспользуемый реквизит формы `ИсточникСуммыДляПересчетаВВалютуФинОтчетности`**
  (`Документы.ОперацияБух.Форма.ФормаДокумента`): константы за ним нет, код
  больше к нему не обращается. Убрать при следующей правке формы в EDT.
- **`Справочники.Контрагенты.ПолучитьРеквизитыПоИНН` не работает**: функция
  берёт логин и пароль ИТС из констант `ИТСЛогин` и `ИТСПароль`, которых в
  конфигурации нет, и падает на первой же строке обращения к сервису
  `orgregister.1c.ru`. Вызовов у функции сейчас нет. Либо заводить пару
  констант (и настройку в интерфейсе), либо удалять функцию.
- **`БухгалтерскиеОтчетыКлиентСервер` обращается к несуществующим отчётам**
  `СчетаИсключаемыеИзРасчетаЗадолженностиПокупателей` и
  `...Поставщикам` (строки 1551 и 1553). Наследие того же бухгалтерского
  контура.
- **`МенеджерОборудованияВызовСервера` ссылается на
  `Справочники.ШаблоныМагнитныхКарт`** — объекта нет. Модуль в карантине
  (решение 2026-08-24), правится вместе со всем менеджером оборудования.
- **Справка бухгалтерских объектов потеряла перекрёстные ссылки.** 55 ссылок
  вида `<a href="../id<uuid>/...">` вели на страницы справки объектов, которых
  в maERP нет (`CheckConfig` считал их неразрешёнными ссылками). Ссылки сняты,
  текст сохранён. Если бухгалтерский контур будет доводиться, справку писать
  заново.

## Найдено сверкой ссылок 2026-08-26 (`scripts/refcheck.py`)

Полный список принятых расхождений — `scripts/refcheck-known.txt`. Смысловые
группы:

- **Присоединённые файлы: кнопки ведут в пустоту.** `ПрисоединённыеФайлыКлиент`
  не содержит методов `ОткрытьФайл`, `ПрисоединитьФайлы` и `СохранитьФайлы`,
  а их зовут карточка номенклатуры и обе формы регистра `ПрисоединённыеФайлы`.
  Механизм присоединённых файлов не достроен — доделывать вместе с ним.
- **Бухгалтерский контур не перенесён целиком.** `БухгалтерскиеОтчеты*` и
  `ОперацияБух` зовут методы, которых нет в наших `ОбщегоНазначения`,
  `ОбщегоНазначенияКлиент`, `ОбщегоНазначенияКлиентСервер`,
  `БухгалтерскийУчет` (наследие БСП и 1С:Бухгалтерии), открывают несуществующие
  общие формы настроек отчётов и ссылаются на типы `СправочникСсылка.
  НоменклатурныеГруппы` и `СправочникСсылка.ПодразделенияОрганизаций`.
  Решается вместе с судьбой бухгалтерского контура.
- **Стандартные картинки платформы** (`БиблиотекаКартинок.Справочник`, `.Отчет`,
  `.РегистрНакопления`, `.РегистрСведений`, `.РегистрБухгалтерии`,
  `.УдалитьНепосредственно`) в конфигурации не заведены, но работают: их даёт
  платформа. Внесены в известные расхождения как рабочие.
- **Карантин менеджера оборудования** (`Справочники.ШаблоныМагнитныхКарт`,
  общая форма редактора этикеток) — правится вместе со всем менеджером.
- **`ОбщаяФорма.ДлительнаяОперация`** — `ДлительныеОперацииКлиент` открывает
  форму, которой в конфигурации нет: длительные операции в maERP не достроены.

## Печатные формы реализации: шапка берётся из итоговой строки запроса (2026-08-26)

Пять запросов модуля менеджера `РеализацияТоваровУслуг` (`ТекстЗапросаDevisBL`,
`ТекстЗапросаFacture`, а также запросы внутри `PrintFactureOld`,
`PrintFactureSocassif`, `PrintTicket`) заканчиваются `ИТОГИ ПО Ссылка` без
агрегатных функций, а код обходит выборку `ПоГруппировкам` и читает из
итоговой строки реквизиты шапки: организацию, клиента, номер, дату, валюту,
суммы. В итоговой записи таких полей нет — прогон печати показал это ошибками
приведения типов (номер, признак «без НДС», суммы итогов).

Сделано в 2.0.12.14: в `ТекстЗапросаDevisBL` и `ТекстЗапросаFacture` добавлены
агрегаты `МАКСИМУМ(...)` по полям шапки — итоговая строка теперь их содержит.

Сделано в 2.0.12.15: агрегаты добавлены в оставшиеся запросы того же вида —
`PrintFactureOld`, `PrintFactureSocassif`, `PrintTicket` в реализации, а также в
печать `ВозвратОтПокупателя`, `ВозвратОтПокупателяНалоговый` и
`НалоговаяНакладнаяПокупка`: там дефект тот же, скопированный вместе с кодом
печати, и в возврате он приводил бы не к пустой шапке, а к падению
(`Selection.TotalSomme - TotalSommePaiement` по NULL). Ни одна из этих форм
прогоном не проверяется — образцов таких документов в тестовой базе нет.

Остальные запросы с `ИТОГИ ПО` проверены: `ЗаказПокупателя`, `Номенклатура`,
`ПроизводственныйКалендарь`, оборотно-сальдовая ведомость читают на уровне
группировки только само поле группировки — там дефекта нет.

Проверить глазами (прогон этого не видит): шапка `FactureOld`, чека и
`FactureSocassif` после ближайшего обновления базы. Прогон теперь пишет строку
`ПОДОЗРЕНИЕ`, если в печатной форме нет номера документа, — это ловит пустую
шапку, но только её.

## Счёт-фактура Socassif: колонки объединения разъехались (2026-08-26)

В `PrintFactureSocassif` первая ветка `ОБЪЕДИНИТЬ ВСЕ` (`НалоговаяНакладная.ТЧТовары`)
выдавала в колонку `DocumentDate` дату документа, а в `DocumentNumber` — дату инвойса;
вторая ветка (`ТЧУслуги`) в тех же позициях выдавала дату инвойса и номер. То есть у
накладной с товарами в печать вместо номера уходила дата, а номер не выводился вовсе.
Исправлено в 2.0.12.15 по образцу второй ветки. Проверить глазами, когда в базе появится
образец налоговой накладной (план покрытия печати, `docs/TESTING.md`).

## Печать, найденная после заполнения образцами (2026-08-26)

Прогон печати на заполненной базе (`scripts/fixtures.sh`) впервые дошёл до 51 печатной формы
вместо 18 и нашёл три дефекта. Все три исправлены в 2.0.12.17, но два оставили след:

- **Табель учёта рабочего времени.** Печать обращалась к `Док.Группа` и
  `Стр.ВариантУчётаРабочегоВремени` — таких реквизитов у документа и его табличной части
  нет (наследие drissotex). Реквизит «вариант учёта» заменён на график работы сотрудника —
  это единственные подходящие данные в конфигурации. Подпись «Группа: » в макете осталась и
  печатается пустой. Решить: заводить реквизит группы у табеля или убирать подпись из макета.
- **Чек возврата от покупателя** печатает номер документа-основания, а не свой собственный.
  Проверка шапки по номеру для него неприменима, поэтому обе формы (`ВозвратОтПокупателя.Чек`,
  `ВозвратОтПокупателяНалоговый.Чек`) внесены в `ПечатныеФормыБезНомера()`. Если так и
  задумано — это нормально; если нет, в чеке должен печататься номер возврата.

Третий дефект следа не оставил: в макетах акта передачи в переработку и акта приёмки из
переработки ячейка подзаголовка таблицы была текстовой, хотя код писал в неё параметр.
Ячейке проставлено заполнение «Параметр».

## Прогон форм открывает только пустые объекты (2026-08-27, закрыто)

`ПолучитьФорму(Имя)` в проходе «открыть все формы» зовётся без параметров, поэтому
`ПриСозданииНаСервере` каждой формы объекта отрабатывал на **новом** объекте, и ветки
кода, работающие только с записанным объектом, прогоном не проверялись. Так мимо
прогона прошла ошибка в `Справочники.Номенклатура.КомплектИспользованВДокументах`:
запрос с полем-псевдонимом `Есть` (зарезервированное слово языка запросов) не
компилировался, но для пустой ссылки функция возвращает `Ложь` первой строкой и до
запроса не доходит. Владелец поймал её вручную, открыв карточку товара из документа.

Закрыто в 2.0.12.18 вторым проходом смока: основная форма объекта и основная форма
группы открываются с параметром `Ключ` по образцу из базы. Прогон 2026-08-27 —
53 формы на образцах, ошибок нет.

Осталось два хвоста:

- **Неосновные формы объекта не покрыты** — закрыто 2.0.14.15 явным списком
  `ДополнительныеФормыОбъектов()` в модуле менеджера `СмокТест`: формы роли
  ECommerce (номенклатура, контрагенты, заказ покупателя) и формы единицы и упаковки
  открываются вторым проходом на образце; образец для форм единиц подбирается по виду
  измеряемой величины.
- **Половина форм объектов пропускается: в базе нет образцов** — закрыто 2.0.13.1:
  `Обработка.ТестовыеДанные` расширена с 25 объектов печати на все документы и
  справочники (у иерархических — ещё и группа). Прогон 2026-09-02 — 105 форм на
  образцах, ошибок нет; единственный пропуск — `ГруппыПользователей`, их автотест
  не заводит осознанно (пользователи ИБ — дело администратора).

## Константы, которые не читает ни одна строка кода (2026-09-08, 2.0.14.19)

При переносе настроек с формы «Параметры» на формы обработки `НастройкиПрограммы`
сверил каждую константу с кодом (`grep` по модулям, формам, СКД). Не читаются нигде:
`ИспользоватьУпрощенныйВыпускГП`, `ИспользоватьВыпускПродукцииПриПродаже`,
`ДатаДемоРежима`, `ИспользоватьСинхронизацию`, `ЕдиницаИзмеренияДлины`,
`ЕдиницаИзмеренияОбъема`. На формы настроек они не вынесены — показывать
переключатель, который ничего не переключает, вреднее, чем не показывать. Сами
константы оставлены в метаданных: удалять их или наполнять поведением — решение
владельца (**Р10** в `docs/ROADMAP.md`). `ЕдиницаИзмеренияВеса` читается одной СКД
(отчёт «Движения товара») и вынесена на форму «Номенклатура».

**Закрыто в части выпуска (волна 2mzl):** `ИспользоватьВыпускПродукцииПриПродаже` наполнена
поведением автовыпуска и вынесена на форму «Производство» (E9, 2.0.15.21);
`ИспользоватьУпрощенныйВыпускГП` удалена из метаданных (E10, 2.0.15.23, решение владельца
2026-09-14). Не читаются по-прежнему четыре: `ДатаДемоРежима`, `ИспользоватьСинхронизацию`,
`ЕдиницаИзмеренияДлины`, `ЕдиницаИзмеренияОбъема` — Р10 в их части открыта.

## Расширение для работы с 1С:Предприятием: остался один самодельный сценарий (2026-08-27)

Предложение установить расширение сведено в `ОбщийМодуль.РасширениеРаботыС1СПредприятиемКлиент`
и общую форму `ВопросОбУстановкеРасширения` — по образцу БСП (`ФайловаяСистемаКлиент.
ПодключитьРасширениеДляРаботы1СПредприятия` в УНФ). На него переведены печать и сохранение
печатной формы, звонок из карточки лида и открытие карточки товара.

Вне механизма остался `ОбщийМодуль.МенеджерОборудованияКлиент`: там своя пара
`НачатьПодключениеРасширенияРаботыСФайламиЗавершение` / `НачатьУстановкуРасширенияРаботыСФайлами`
с вопросом через `ПоказатьВопрос`, внутри веток препроцессора подключения оборудования. Трогать
её заодно с печатью было рискованно — подключение оборудования проверяется только на живой ККТ.
Перевести на общий механизм при ближайшей правке подсистемы оборудования.

## Дополнительные реквизиты (2026-09-07, 2.0.14.13)

- **Два механизма свойств номенклатуры.** Старая ТЧ `СвойстваНоменклатуры` со
  справочниками `ВидыСвойствНоменклатуры`/`ЗначенияСвойствНоменклатуры` (наследие drissotex,
  закладка «Свойства», в коде не используется) осталась рядом с новым механизмом
  `УправлениеСвойствами` (ПВХ `ДополнительныеРеквизиты`, закладка «Дополнительно»). Перенести
  данные старой ТЧ в дополнительные реквизиты обработчиком обновления и удалить старую —
  после того как владелец подтвердит, что у клиентов она пуста.
- **Механизм подключён только к номенклатуре.** Наборы, ТЧ `ДополнительныеРеквизиты` и
  измерение регистра `ДополнительныеСведения` заведены под `Справочник.Номенклатура`;
  для контрагентов и документов нужны те же три шага плюс вызовы в формах. Группы
  номенклатуры реквизитов не имеют (ТЧ только для элементов): резиденция/титр для
  недвижимости — отдельной волной по разбору Ахмеда.
- **Реквизиты не видны в отчётах и списках.** Свойство `Характеристики` у справочника не
  описано — СКД отчёты и динамические списки дополнительных реквизитов не видят. Описать
  через ТЧ набора, когда появится первый отчёт по ним.

## Онбординг через хаб tinycio (2026-09-06)

План работ на стороне хаба — `../tinycio/docs/plans/feature-maerp-onboarding.md`; здесь —
что в maERP этому мешает. Очередь — `docs/ROADMAP.md`, блок 11, этап 12.

- ~~**Первый запуск несовместим с headless-развёртыванием.**~~ — **закрыто 2026-09-06
  (2.0.14.11)**: обработка `НачальноеЗаполнение` и общий модуль
  `НачальноеЗаполнениеВызовСервера` заводят организацию, пользователей, физлица и
  служебного администратора из JSON (`/C "НачальноеЗаполнение;payload;result"`, контракт
  `docs/DEPLOY.md`); визард зовёт тот же модуль. Пароль администратора — случайный /
  из payload (Р3).
- **В репозитории нет скриптов развёртывания.** `scripts/` — только dev-цепочка на macOS
  (`ib-create`, `ib-restore`, `cf-load`, `dist-build`, теперь и `initial-fill`): файловые
  базы, без `Srvr=`, без веб-публикации. `IBCMD` в `env.sh` объявлен и нигде не
  используется. Продовые скрипты живут на 1С-сервере вне git (решение владельца
  2026-09-06) — контракт к ним задаёт план хаба (§5.4) и `docs/DEPLOY.md`; серверный
  `initial-fill.sh` пишется по образцу `scripts/initial-fill.sh`.
- **Инструкции установки для клиента нет** — шаги сервера (CF → заполнение → публикация
  → `/L`) описаны в `docs/DEPLOY.md` с 2026-09-06, но инструкции для ручной установки у
  клиента (список баз, язык интерфейса, веб-клиент) по-прежнему нет.
- **Контакты владельца и пользователей некуда записать.** Payload `initial_fill`
  приносит `owner.email/phone` и `users[].email`, а у `Организации` и `Пользователей`
  реквизитов для контактов нет — поля принимаются и игнорируются. Появятся оповещения
  клиенту из базы (этап 12) — завести реквизиты и заполнять их здесь.
- **`ЗакрытиеСеансов` работает только на Windows.** Регламентное задание снимает сеансы
  через `COMОбъект("v83.COMConnector")` — на сервере Linux/macOS `Новый COMОбъект` падает
  и гасится пустым `Попытка`. С 2026-09-06 задание разрывает только сеансы (через агента
  кластера), а не соединения: для `Disconnect` нужна аутентификация администратора базы у
  рабочего процесса, зашитая учётка убрана, хранить пароль администратора базы в константе
  не стали. Для тиража на Linux переписать на `rac` либо удалить задание.
- **Каталог модулей для витрины** в конфигурации не описан: 13 подсистем и 4
  константные функциональные опции (`Бухгалтерия`, `Зарплата`, `ИспользоватьПроизводство`,
  `ТендерыПоставщиков`) не совпадают с 14 модулями продажного каталога хаба. Решено:
  мастер — хаб, конфигурация манифест не отдаёт; при появлении платных модулей
  включение функциональных опций по покупке — отдельная задача этапа 12.


## Найдено проверкой динамических списков (2026-09-09, `scripts/listcheck.py`)

Проверка появилась после регрессии списка поступления товаров и услуг:
в 95e510d форма сохранилась из устаревшего состояния EDT, и текст запроса
основного списка подменился текстом соседнего списка заказов поставщику —
список выбирал заказы, а тринадцать колонок формы остались без полей. Ни
CheckConfig, ни EDT, ни смок такого не видят: запрос валиден сам по себе, а
данные списка читаются только при отрисовке таблицы у пользователя. Восстановлено
в 2.0.15.1, проверка заведена уровнем 0.7 (`docs/TESTING.md`).

Первый прогон нашёл 25 расхождений-наследия — они записаны в
`scripts/listcheck-known.txt` и ждут разбора:

- **Колонки на удалённые реквизиты.** Контрагенты (`ГруппаКомпаний` в четырёх
  формах выбора и списка, `ЗапретОтгрузки` в форме ECommerce), продажи
  (`ТорговаяТочка`, `СтатусНакладной`, `ВтораяХодка` в форме выбора реализации),
  заказ покупателя (`ТорговаяТочка`), журнал безналичных платежей
  (`ТипПлатежногоДокумента`), план счетов (`НалоговыйУчет`,
  `УчетПоПодразделениям`, `УчетПоНаправлениямДеятельности`), подключаемое
  оборудование (`Используется`). Реквизитов с такими именами у объектов нет —
  формы пришли из конфигураций клиентов, а реквизиты по дороге не приехали.
- **Поля есть у объекта, но не выбраны запросом.** Регистр присоединённых
  файлов (`Объект`, `Размер`, `Внешний`, `ID`, а `Раздел` и `IDСвязь` — уже
  удалённые реквизиты), форма выбора упаковок номенклатуры
  (`ТипНоменклатуры`), обработка регистрации изменений обмена
  (`Узел`, `ИндексКартинки`, `СсылкаПредставление`).
- **Мобильная форма списка налоговой накладной** выбирает из
  `Документ.РеализацияТоваровУслуг` при `mainTable` налоговой накладной. Мобильный
  интерфейс из конфигурации выключен (`usePurposes = PersonalComputer`), поэтому
  форма не открывается ни у кого — но при включении упадёт.

Разбирать по одной форме: колонку либо убрать, либо добавить поле в запрос,
и снимать строку из `listcheck-known.txt`. Очередь — `docs/ROADMAP.md`, блок
«Покрытие тестами».

## Клиентские обработчики строк списков не покрыты (2026-09-09)

`Обработка.АРМПроизводство.Форма.Форма` падала с «Поле объекта не обнаружено
(Ссылка)» при щелчке по заказу: таблица строится программно, а колонки `Ссылка`
в ней не было — у динамического списка в `ТекущиеДанные` попадают только
выведенные колонками поля. Исправление 2.0.15.1 невидимой колонкой **не сработало**:
журнал регистрации 2026-09-13 на 2.0.15.9 снова показал ту же ошибку в строке
активации — скрытая колонка поле в текущие данные не кладёт. В 2.0.15.10 ссылка
берётся из `ТекущаяСтрока` (ключ строки списка с основной таблицей), колонка убрана.

Машинной проверки на это нет и статически её не построить: обработчик читает
поле у `ТекущиеДанные`, а состав колонок может собираться в коде. Ловится
уровнем 4 (сценарные UI-тесты) — по правилу `docs/TESTING.md` это первый
случай клиентской логики, которая реально ломается, и потому заявка на уровень
4 перестала быть теоретической. Дешёвый промежуточный вариант — проход смока,
который открывает формы списков на клиенте и ставит `ТекущаяСтрока` на первую
строку.

**E12.1 (2.0.15.29):** форма рабочего места стала декларативной — `listcheck.py` видит список заказов; клиентская логика
закладки «Продукция» (ввод строк, выбор черновика, удаление) по-прежнему покрывается только сценарием уровня 3 на модуль
менеджера и ручной приёмкой.

## Карточка ECommerce не показывает техкарту (2026-09-10)

`ОбработкаПолученияФормы` справочника `Номенклатура` отдаёт пользователям с ролью
`ECommerce` упрощённую `ФормаЭлементаECommerce` — техстраницы там нет и не было.
Поймано приёмкой E1 (коммит 5): с включённой опцией «Производство» и типом «Продукция»
страница «Техкарта» не появлялась — открывалась ECommerce-карточка, у которой есть
своё поле типа, но нет ни таблицы состава, ни кнопки истории.
Этап E1 по брифу переводит только `ФормаЭлемента`.
Решено владельцем 2026-09-10: карточка номенклатуры одна для всех ролей,
подстановка ECommerce-форм по роли убрана из `ОбработкаПолученияФормы`.
Сами ECommerce-формы остались в метаданных (смок открывает их вторым проходом
напрямую по именам), ветка мобильного клиента сохранена — это не роль.

## Старая табличная часть технологической карты (E1, 2026-09-10)

В 2.0.15.2 добавлен обработчик `ПеренестиТехнологическиеКарты`: прежние составы
из `Номенклатура.ТехнологическаяКарта` переносятся в документы и регистр. Читатели
и карточка номенклатуры уже используют фасад `ТехнологическиеКарты`.

E1 принят владельцем на обновлённой базе 2026-09-11; совместимостный долг по старой
табличной части при этом остаётся до подтверждения обновления всех клиентских баз.

Старую ТЧ с uuid `3083d1a4-2a84-4b02-a6a5-c91b846772ca` и её реквизит `ЕИ`
сохраняем без изменений для совместимости с drissotex. Удаление — отдельный
более поздний релиз, только после подтверждения, что все базы клиентов прошли
версию с переносом. При удалении нужно решить путь обновления баз, которые
пропустили промежуточную версию, и убрать обработчик и тестовые обращения к ТЧ.
Очередь — `docs/ROADMAP.md`, блок производства; приёмка —
`docs/plans/production-2mzl-e1-check.md`.

## Смок: сравнение с базой замечаний расширенной проверки (2026-09-13)

Прогон смока на копии `build/ib` 2.0.15.8 упал на шаге 1б: 18 строк «новых замечаний»
расширенной проверки модулей оказались старыми замечаниями бухгалтерских модулей из
`build/checkconfig-baseline.txt` — после удаления BOM и нормализации Unicode новых строк
ноль. Сравнение `sort -u | comm -13` в `scripts/smoke.sh` зависит от BOM первой строки
журнала (урок tiny1C workflow-002) и порядка сортировки, поэтому скрипт ставит
`COMPILE_ERROR=1` и пропускает шаг «открыть все формы». Шаг 2 пришлось запускать отдельно
(398 форм, 0 ошибок). Исправление — нормализация строк журнала и базы (BOM, NFC,
`LC_ALL=C` для обоих `sort`) перед `comm`. Очередь — `docs/ROADMAP.md`, блок «Покрытие тестами».

## Найдено в E5 (2026-09-13, материалы заказа на производство)

- **ECommerce-форма заказа не знает о материалах.** Бриф E5 запрещает её менять. Замена номенклатуры
  строки там не очищает материалы, заполненные вручную (без колонки «Техкарта»): серверу нечем
  заметить смену продукции. Материалы, заполненные по карте, очищаются при записи (карта другой
  продукции). ECommerce-заказы на производство на практике не заводятся; при появлении такого
  сценария — очистка в `ТЧТоварыНоменклатураПриИзменении` ECommerce-формы или запрет производства
  у ECommerce-заказа.
- **Формула количества материала продублирована** в модуле менеджера заказа
  (`ПересчитатьСтрокуМатериала`) и в модуле формы: у документа нет клиент-серверного модуля.
  Две строки; при изменении формулы — править обе.
- **Коэффициент упаковки всегда 1** (`ОбщегоНазначенияВызовСервера.РассчитатьКоэффициентЕдиницы`,
  тело закомментировано), поэтому `КоличествоУпаковок` и `Количество` материалов совпадают, а
  сценарий уровня 3 не проверяет упаковку с коэффициентом.
- **Отрицательный расход материала не поддерживается.** `КоличествоЗаЕдиницу` материалов заказа — знаковый
  `DefinedType.Количество` (как в карте), а `КоличествоУпаковок` и `Количество` — неотрицательные: возвратные
  отходы с отрицательным расходом молча обнулятся. У `ВыпускПродукции` та же пара типов; решать вместе.
- **Ошибка заполнения материала в режиме «по строке»** может относиться к строке материалов другой продукции,
  скрытой отбором: переход из сообщения её не покажет, нужно переключиться на «Все материалы».

## Найдено в E6 (2026-09-13, проведение заказа на производство)

- **«Техкарта» строки — ссылка на документ карты дня, а не снимок состава.** `ЗаписатьТехкарту`
  перезаписывает документ дня, поэтому любая запись карты продукции в тот же день (карточка
  номенклатуры, форма карты, другой заказ) меняет состав за ссылкой у всех заказов, сославшихся на
  эту карту сегодня. Следствие решений владельца 2026-09-13 (версия заказа — текущая карта, дата —
  день проведения). Состав заказа — только его `ТЧМатериалы`; этапы E7–E9 не должны сравнивать
  `Техкарта` с материалами для решений. Перепроведение с неизменным составом карту не переключает.
  Снять можно только версиями внутри дня в фасаде карт (реструктуризация периодичности регистра).
- **Блокировка карт продукции — только управляемая, параллельную запись не проверяет автотест.** Все пути
  записи карты (фасад `ЗаписатьТехкарту` в своей транзакции, проведение заказа, `ПередЗаписью` документа
  карты) берут `ТехнологическиеКарты.ЗаблокироватьКартыПродукции` до записи документа — одна сессия
  уровня 3 гонку не воспроизводит. Проверка — вручную двумя сеансами.
- **Удаление документа карты не берёт блокировку карт продукции**: у `ТехнологическаяКарта` нет
  `ПередУдалением`. Физическое удаление проведённой карты одновременно с проведением заказа той же
  продукции даст заказу устаревший срез. Удаление карт на практике редкое (помечаются); при необходимости —
  `ПередУдалением` с `ЗаблокироватьКартыПродукции`.
- **Перенос заказов из баз клиентов в режиме загрузки** не выполняет проведение и проверки E6: при
  переносе (drissotex) ставить `Проведен = Ложь` и перед выкладкой сверять запросом, что проведённых
  заказов не на производство нет.
- **Обработка «Проведение документов» и групповое проведение из списка** обходят скрытые команды:
  продажи получат отказ с сообщением на каждый заказ, непроведённые заказы на производство E4/E5
  проведутся и зарегистрируют версии карт сегодняшней датой.
- **Роль `ECommerce` имеет права на технологические карты** и теперь меняет их и через проведение
  заказа. Путь по правам не новый (форма карты уже доступна) — нужны ли ей карты, решает владелец.
- **«Провести» у продаж вне форм заказа.** «Связанные документы», таблица заказов в кабинете клиента
  (`CommonForms/ИнтерфейсКлиент`, `ДСЗаказыКлиента`) и закладка заказов в списке реализаций
  (`СписокЗаказы`) заполняют командные панели автоматически и покажут команды проведения. Проведение
  продажи отклоняется с сообщением. Явные скрытые кнопки там не ставились: таблица списка реализаций
  лежит на закладке, кнопка внутри такой таблицы роняет клиент 8.5 (урок forms-009).
- **Продукция со способом пополнения «Оба» без материалов проводится.** Сознательно: её можно
  закупить под заказ. Если E9 будет выпускать «Оба» без состава, вернуться к отказу.
- **Замечания BSL LS в модуле объекта заказа:** `EventHandlerOutsideEventRegion` у
  `ОбработкаПроведения` — модуль целиком без стандартных областей (все обработчики, в том числе
  E5); `SetPrivilegedMode` в регистрации карт — намеренно, право пользователя проверяется до
  включения режима.

## Найдено в E7 (2026-09-13, потребности производства)

- **Заказ поставщику по заказу на продажу основание не ставит.** Ввод на основании продажи (как и обработка
  «Формирование заказов поставщику») оставляет `Основание` пустым — связь «заказ клиента → закупка» есть только у
  заказов на производство (E7) и у заказов по предложению поставщика. Поведение продаж сознательно не менялось.
- **Итоговый заказ поставщику из предварительных.** Строки с заполненной колонкой `ЗаказПоставщику` (подбор из
  предварительных заказов) и соответствующие им строки поступлений движений потребности не дают: «Заказано» пишет
  предварительный заказ, а поступление по итоговому заказу «Пришло» не отмечает. Сценарий «предварительный заказ под
  заказ на производство → итоговый → поступление» не поддержан; при появлении — заказ клиента строки брать из основания
  предварительного заказа.
- ~~**Отмена проведения заказа клиента со связанными закупками.**~~ Закрыто в E8 (2.0.15.20): отмена разрешена
  (решение владельца 2026-09-14), потребности очищаются под блокировкой проведения, стадия — «Черновик»; «Заказано» и
  «Пришло» закупок видны в отчёте.
- **Распределение по продукции зависит от порядка проведения закупок** и не пересматривается при изменении
  потребности: излишек ложится на последнюю продукцию, явная продукция пределом не ограничена. Для стадий E8 — сумма
  по материалу и характеристике заказа, а не строки продукции (обязательство в плане волны).
- **Фоновое перепроведение последовательности `Основная`** включает поступления и возвраты: старые поступления по
  заказам поставщику, чьё основание — заказ на производство, проведённый в 2.0.15.15, начнут писать «Пришло» при
  восстановлении последовательности. Исторических таких документов нет (проведение заказов появилось в 2.0.15.14).
- **Смена коэффициента упаковки материала** пересчитает базовое количество: запись проведённого заказа без
  перепроведения получит отказ «проведите заказ» с сообщением про материалы. Коэффициент сейчас всегда 1 (E5).
- **Колонка «Заказ клиента» списка поступлений** у ролей без права чтения заказов поставщику (`Оператор`, `Логист`)
  скрыта и убрана из запроса — поступления на основании заказа клиента напрямую им тоже не видны в этой колонке. У
  списка включено получение представлений невидимых полей: у ролей с правом соединение с заказами поставщику
  выполняется и при скрытой колонке (выключенная опция «Производство») — нагрузка, не доступ.
- **Ввод поступления на основании заказа клиента** заполняет строки без поставщика и цен последнего прайса без учёта
  поставщика: поставщик выбирается вручную.
- **Организация закупки не сверяется с организацией заказа клиента.** Поступление организации B с основанием —
  заказом клиента организации A — отметит «Пришло» у чужой организации. Разграничения доступа по организациям в
  конфигурации нет; при его появлении — проверка совпадения в проведении.
- **«Создать на основании → Поступление товаров и услуг»** видна у любого заказа клиента, в том числе у продажи и при
  выключенной опции «Производство», и там отклоняется сообщением. Скрыть команду у продаж — вместе с командами
  проведения (см. «Найдено в E6»).

## Найдено в E8 (2026-09-14, стадии заказа на производство)

- ~~**Переработка выпуском не считается.**~~ — закрыто в 2.0.15.34 (E12.3b): продукция поступления из переработки по
  заказу и его передачам — «Выпущено» в стадии и автовыпуске.
- **Аванс «на основании заказа» до отгрузки.** ПКО, введённый на основании заказа, кладёт аванс на объект «сам ПКО», а
  не на заказ: до зачёта в реализации доля оплаты индикатора его не показывает. Аванс по строке оснований на заказ
  учитывается сразу.
- **Оплата другим контрагентом.** Долг заказа читается по контрагентам заказа и его реализаций (индексированное
  измерение): оплата по объекту заказа от другого контрагента в долг не попадёт.
- **Гонка перепривязки.** Заказы документа определяются до блокировки истории статусов: перепривязка реализации к
  другому заказу одновременно с оплатой по ней может оставить новый заказ без учёта платежа до следующей записи.
- **Фоновое восстановление последовательности** стадии не пересчитывает: если оно изменит «Пришло» старых поступлений
  (см. «Найдено в E7»), стадия догонит при следующей записи заказа или связанного документа.
- **Подписка `ПослеПроведенияДокументаОплаты`** перепроводит налоговые накладные после записи истории статусов в
  транзакции ПКО и безналичных — «история последней» для четырёх денежных документов не строго; налоговые накладные
  историю не пишут и заказ не блокируют.
- **`КорректировкаРегистров`** пишет `ВзаиморасчётыСПокупателями` вручную и стадию не пересчитывает.
- **Исторические заказы на производство** стадию не получают до следующей записи (решение 2 волны); колонка «Индикатор»
  у них пустая, статус — прежний.
- **Смок и уровень 3 идут под администратором**: пересчёт стадии из-под ролей `PDV`, `Казначей`, `Кладовщик`
  (привилегированный режим общего модуля) подтверждается ручной приёмкой.
- **Отмена проведения ПКО, РКО и безналичных** с перепроведением налоговых накладных подпиской
  `ПослеОтменыПроведенияДокументаОплаты` (событие «при записи») берёт складские блокировки раньше взаиморасчётов — порядок
  не совпадает с проведением; было и до E8.
- **Прямое `Удалить()` проведённого документа** кодом вызывает `ОбработкаУдаленияПроведения` без `ПередЗаписью`: движения
  удаляет платформа, стадия заказа устаревает до следующей записи. Интерактивно удаляются только помеченные (непроведённые).
- **Права на историю статусов.** Ролям, читающим заказ клиента, в E8 выдано чтение регистра истории; запись статусов
  продаж (`ЗаписатьСтатусПродажи`, `ЗаписатьСтатусЗаказаПродажи`) по-прежнему идёт под пользователем и требует `Update`,
  который есть не у всех ролей, пишущих заказы и реализации. `ЛичныйКабинет` и `ОператорПроизводства` могут вручную
  добавить запись истории и убрать заказ из «Заказы к отгрузке». Снять `Update`/`Edit` — после перевода записи статусов
  продаж в узкий привилегированный режим. Форма заказа читает статус в привилегированном `ПриСозданииНаСервере` (было до E8).
- **`ОператорПроизводства` видит оплату по индикатору** (пятый кружок, стадия «Ожидает оплаты»), хотя суммы в интерфейсе
  ему скрыты; права на взаиморасчёты у роли есть. Скрывать ли пятый кружок — решение владельца.

## Найдено в E9 (2026-09-14, автовыпуск при отгрузке)

- **Константа автовыпуска в базах клиентов не сброшена** (решение владельца 2026-09-14). `ИспользоватьВыпускПродукцииПриПродаже`
  до E9 ничего не делала; если в базе клиента она включена, перепроведение исторических реализаций по заказам на
  производство создаст автовыпуски задним числом. Перед обновлением базы клиента — проверить значение константы.
- **Себестоимость между продукциями многопродуктового автовыпуска** распределяется по весам техкарт механизма выпуска
  (решение владельца: выпуск строго один на реализацию); итог отгрузки верен, по позициям — приблизительно. Реализация
  берёт продукцию по ФИФО и может списать партию другого выпуска той же даты или раньше.
- **Партия под заказ — весь документ поступления.** Поступление, где часть строк под заказ, а часть на склад, даёт одну
  партию: автовыпуск считает её «под заказ» целиком и может занизить «Из остатка». Разделение — партиями по строкам или
  продукцией в партии.
- **Серии прошлых отгрузок с дробным коэффициентом упаковки.** История «выпущено и отгружено ранее» разворачивает
  `ТЧСерии` реализаций и возвратов с округлением до 0,001 без отдачи остатка последней серии, как делает
  `СерииИРазмещение.ОбъединитьТоварыССериями`: при дробном пересчёте упаковки расхождение до 0,001 на строку.
- **Материал-услуга в составе** попадает в автовыпуск строкой без списания с нулевой себестоимостью: колонку
  `Себестоимость` у ручного выпуска вводит пользователь, у автовыпуска её источника нет.
- **Выпуск по техкарте работает только для непроведённого заказа.** Проведение заказа (E6) отклоняет строку продукции со
  способом «Производство» без материалов, а у типов «Продукция» и «Полуфабрикат» это способ по умолчанию (E2): решение
  владельца 4 (без состава — по техкарте) на проведённом заказе на производство не встречается.
- **Продукция по техкарте без материалов в заказе** выпускается автоматически, но стадии E8 считают производимыми только
  строки с материалами: в долю выпуска индикатора она не входит, «Из остатка» по ней не пишется (потребности нет).
- **«Из остатка» не учитывают пределы распределения закупок** (`ПотребностиПроизводства.ПределыПоОстаткам`): закупка после
  автовыпуска из общего остатка распределяется по продукции без учёта взятого со склада. Дефицит, «к поступлению» и
  отчёт «Из остатка» учитывают.
- **Смена основания проведённой реализации** с заказа на производство на другой заказ блокирует потребности старого
  заказа только через движения «Из остатка» автовыпуска: без них параллельная реализация по старому заказу может в узком
  окне посчитать «выпущено ранее» со старым автовыпуском.
- **Автовыпуск в предыдущем закрытом дне.** Реализация в 00:00:00 ставит автовыпуск на 23:59:59 предыдущего дня: при
  закрытом периоде проведение, отмена проведения и пометка реализации получают отказ; разовое разрешение на
  редактирование реализации на автовыпуск не распространяется.
- **Где автовыпуск не виден или советует невозможное.** ~~`ТехнологическиеКарты.ЕстьВыпускиПослеДаты` при смене карты
  советует «перезаполнить выпуски» и для автовыпусков, которые вручную не правятся~~ — закрыто в E10 (2.0.15.23): совет
  учитывает только ручные выпуски; ~~в АРМ производства (критерий
  `СвязанныеДокументы` заказа) автовыпуск не виден — у него основание реализация~~ — закрыто в E12.1 (2.0.15.29): закладка
  «Документы» показывает второй уровень цепочки, автовыпуск — со статусом «автовыпуск»; дашборд считает автовыпуски вместе
  с ручными выпусками.
- **Фоновое перепроведение последовательности** автовыпуски не трогает, а выпуски в последовательность не входят:
  себестоимость автовыпуска не догоняет пересчёт партий материалов, как и у ручных выпусков.
- **Смок и уровень 3 под администратором**: проведение реализации с автовыпуском из-под `PDV`, `Казначей`, `Оператор` и
  форма автовыпуска у ролей без чтения реализации — ручная приёмка.
- **Замечания BSL LS**: `SetPrivilegedMode` в модуле `Производство` (шесть процедур, намеренно, с комментариями);
  `EventHandlerOutsideEventRegion` у нового `ПередУдалением` реализации — модуль объекта без стандартных областей.

## Найдено в E10 (2026-09-14, самостоятельный выпуск и настройки)

- **Бухгалтерские проводки выпуска не подключены (Р1).** `РегистрыБухгалтерии.Хозрасчетный.ДвижениеВыпускПродукции` готова,
  но диспетчер `СформироватьДвижения` того же модуля закомментирован целиком: проведение ручного выпуска и автовыпуска
  проводок в `Хозрасчетный` не пишет. Подключать — только вместе с решением по бухгалтерскому контуру (блок 3 `docs/ROADMAP.md`).
- **Смена карты задним числом при одних автовыпусках — без сигнала** (решение владельца 4, ревью данных E10). Если после
  дня новой версии у продукции только автовыпуски, `ЕстьВыпускиПослеДаты` возвращает Ложь и предупреждения нет: автовыпуски
  хранят материалы и себестоимость прежней карты, пока реализации не перепроведут. Вариант на будущее — отдельная строка
  «перепроведите реализации».
- **Процедура заполнения материалов на данных формы** (`Документы.ВыпускПродукции.ЗаполнитьМатериалыПоТехкартам`) уровнем
  3 проверяется только на объекте документа; вызов из формы — ручная приёмка («Проверка E10» в `docs/TESTING.md`).
- **Коэффициент единицы при заполнении не проверяется**: `ОбщегоНазначенияВызовСервера.РассчитатьКоэффициентЕдиницы`
  всегда возвращает 1 (тело закомментировано у предка), поэтому `Количество` строки материалов равно количеству упаковок
  при любой упаковке.
- **Подписи формы ручного выпуска только на русском**: команда «Заполнить техкарту», колонка «Стоимость» (en/es), реквизит
  формы «Несколько позиций продукции» — нарушение gf-002, в E10 раскладка и подписи не менялись.

## Найдено в E11 (2026-09-14, демо-набор)

- **«Получен от субподрядчика» у заказа-продажи не ставится.** Поступление из переработки, введённое на основании передачи,
  получает основанием передачу; в критерий `СвязанныеДокументы` заказа попадает только документ с основанием-заказом, и
  `РаботаСДокументами.ОбновитьСтатусЗаказаПоСвязанным` оставляет «Передан субподрядчику». Статус достижим, только если в
  поступлении вручную выбрать основанием заказ. Исправление — искать родительский заказ поступления через передачу (как
  `ОбновитьПроизводственныйСтатусЗаказа`) или включать поступления по передачам заказа в выборку.
- **«Доставлен» у заказа-продажи штатно никем не пишется** (волна 12, `docs/ROADMAP.md`) — в демо-набор не входит.
- **«Отгружен» у заказа-продажи пересчитывается только записью документов переработки и выпуска**: сама реализация ставит
  «Собран»; «Отгружен» появляется, когда после реализации перезаписывают выпуск или передачу по тому же заказу.
- **Демо-набор в `build/ib`** включает константы «Использовать производство» и «Автоматический выпуск при продаже» и
  становится образцами смока (первый объект вида): `ПервыйЭлемент` выбирает без упорядочивания, будущие образцы
  `fixtures.sh` могут взять демо-справочники.
- **Помощники `ТестыДокументов` экспортные** (`НовыйКонтекстДанных`, `НовыйДокумент`, `Провести`, …) — ими пользуется
  демо-набор; менять их поведение — с прогоном и тестов, и `fixtures.sh --demo`.

## Реквизит «Автор» (2026-09-14, 2.0.15.28)

- **Общий реквизит `УдалитьАвторОбщий` живёт до шага 2** (`docs/plans/avtor-otvetstvennyi.md`): в 2.0.15.28 «Автор»
  переименован без потери данных, заполненные значения перенесены обработчиком `ПеренестиАвтораВОтветственного` в пустой
  `Ответственный`. Удалять общий реквизит (он есть у 172 объектов, 87 из них — по автоиспользованию) можно только
  когда все базы клиентов пройдут 2.0.15.28; до удаления владелец решает по записям журнала «Автор не перенесён»:
  у объектов без `Ответственного` или с другим `Ответственным`. Релиз удаления обновляет только с 2.0.15.28 и выше.
- **Подписка `ПослеОтменыПроведенияДокументаОплаты` перепроводила налоговые накладные при записи оплаты в режиме
  загрузки** (групповое изменение реквизитов, обработчики обновления) — исправлено в 2.0.15.28 (находка ревью плана).
- **Переход на 2.0.15.28 перестраивает 42 таблицы** (проверено полной загрузкой на копии, контрольная загрузка той же
  версии — 0): оба справочника с новым `Ответственным` и 40 документов и справочников, в том числе реализация,
  поступление, заказы и кассовые документы. Причина выборки не установлена; перед публикацией CF — замер на копии базы
  клиентского размера. Перенос автора на копии — 431 мс.
- **Сценарий `АвторВОтветственного` оставляет в журнале регистрации строки «Автор не перенесён»** по своим тестовым
  объектам: запись журнала не откатывается транзакцией сценария. Только тестовые базы.
- **Свои реквизиты «Автор» у `Контрагенты` и `Номенклатура`** (тип «Пользователь», есть на формах элементов) — не
  общий реквизит и в 2.0.15.28 не тронуты. Решение владельца 2026-09-15: **объединить с `Ответственный`**, как общий
  реквизит, — отдельной сборкой по той же схеме: переименование в `УдалитьАвтор` без потери данных (реквизиты, вероятно,
  пришли из drissotex — проверить uuid и наличие значений в базе клиента), обработчик переноса в пустой `Ответственный`
  с журналом неперенесённых значений, поле «Автор» убрать с форм и из кода, удаление — вместе с шагом 2
  `docs/plans/avtor-otvetstvennyi.md`. **Типы не совпадают у контрагентов:** `Номенклатура.Ответственный` — «Пользователь»,
  перенос прямой; `Контрагенты.Ответственный` — «Физическое лицо», а «Автор» — «Пользователь», поэтому перед переносом
  нужно решение владельца: сопоставлять пользователя с физическим лицом (через справочник пользователей) или оставить
  автора контрагента в журнале без переноса.

## Найдено сверкой полей запросов (2026-09-14, `scripts/queryfields.py`)

- **Панели «Детали» списков читали несуществующие поля** — исправлено в 2.0.15.25: выпуск продукции и акт разбора —
  хозяйственная операция; уценка товаров и поступление дополнительных расходов — количество строк (у табличных частей его
  нет; вместо него — новая цена и сумма товара). Ошибка появлялась только при активации строки списка — смок её не видит.
- **`КассовыеСменыВызовСервера`** обращается к полям кассовой смены ККТ (`НачалоКассовойСмены`, `ОкончаниеКассовойСмены`,
  `ФискальноеУстройство`, `НомерСменыККТ`), которых у документа нет — код торгового оборудования предка, до решения Р2
  (`scripts/queryfields-known.txt`).
- **Формы документов вне маршрута демо** (возвраты, кассовые и банковские документы, переработка, заявка на поиск товара) ещё
  не переведены на закладки и заголовки слева (`docs/FORMS-STYLE.md`, правила 4–5).

- **Критерий «Связанные документы»** не включал в тип значения выпуск продукции и возврат поставщику, а команда — выпуск:
  дерево связей не показывало документы на основании выпуска, из формы выпуска команды не было — исправлено в 2.0.15.25.
  Дерево строится от первого родителя (КП или заказ) по реквизитам-основаниям: закупки по потребности, поступления по
  заказу поставщику, реализации и автовыпуск под ними, оплаты по строкам оснований; документы, связанные только движениями
  регистров (потребности), в нём не видны.
- **«от» в представлениях печатных форм** сверки взаиморасчётов и списания безналичных осталось русской строкой.

## Найдено в E12.1 (2026-09-15, рабочее место производства: заказы и продукция)

- **Предел «Продукции» — 300 последних невыполненных заказов на производство.** Старшие заказы в таблицу не попадают,
  пользователь получает сообщение; поиск и отбор по клиенту на закладке не сделаны.
- **Перенос строки между заказами и смена клиента записанной строки** — только в форме заказа: в рабочем месте колонки
  клиента и заказа у записанной строки только для просмотра.
- **Организация черновика без организации у пользователя** — единственная организация базы; при нескольких организациях
  запись черновика отказывает проверкой заполнения, выбора организации в рабочем месте нет.
- **Смена упаковки на пустую** в строке рабочего места игнорируется (пустая упаковка значит «единица номенклатуры» только у
  новой строки).
- **Отбор отчёта «Потребности производства» по заказу** передаётся параметром `Отбор` типовой формы отчёта — в проекте
  приём используется впервые, проверяется ручной приёмкой.
- **Удаление строки с повторным ненулевым кодом** (данные загрузки): материалы удалённой строки остаются у другой строки
  с тем же кодом, даже если продукция у неё другая; при смене продукции рабочее место код уже переназначает.
- **Высота строк списка и многострочные поля** (2.0.15.30): колонка с многострочным значением (комментарий заказа)
  делает двойной высоту всех строк динамического списка в интерфейсе 8.5; ни смок, ни `listcheck` этого не видят —
  проверка только снимком окна. Эту же колонку в других списках документов стоит проверить глазами.
- **EDT пишет в платформу явные значения свойств таблицы**, которых нет в `Form.form` (выключенные полосы прокрутки,
  нулевой подвал, начальный вид «с начала»): свойства таблиц рабочего места заданы явно по эталону списка реализаций.
- **Срез истории статусов в двух местах**: `АРМПроизводство.ПоказателиЗаказа` читает стадию, индикатор и проценты тем же
  срезом, что `ПроизводственныеЗаказы.ДанныеСтадии`; при смене хранения стадии править оба.
- ~~**Панель кнопок ввода на основании сдвигает таблицу**~~ — закрыто в 2.0.15.31: кнопки — командами таблиц в панели
  выделенных строк 8.5.1, видимость не переключается.
- **Подсказка индикатора — по текущему заказу, а не по строке под курсором** (2.0.15.31): у колонки таблицы одна
  подсказка, данных подсказки по строке модель формы не даёт; подсказка обновляется при активизации строки.
- **Разделитель между заказами и цепочкой не перетаскивается** (2.0.15.31): в управляемых формах такого элемента нет;
  нижняя группа сворачивается кнопкой на разделителе, высоты таблиц — в строках (8 и 5) и растягиваются по окну.
- **Фильтры рабочего места ищут только по наименованию** клиента и номенклатуры (не по коду, ИНН, артикулу).
- ~~**Нулевая цена поступления пишет прайс поставщика**~~ — закрыто 2026-09-15 решением владельца: `ЦеныНоменклатурыПоставщиков.
  СформироватьДвижения` не записывает нулевые цены (проверка в сценарии `АРМЗакупки`).
- **`Основание` заказа поставщику и поступления не индексировано** (E12.2, ревью данных P2-3): закладки закупок и выбор
  строк заказа поставщику отбирают документы по основанию — при десятках тысяч документов переход по заказам замедлится.
  Индекс — реструктуризация без потери данных; отложено до замера на базе drissotex.
- **Причина отказа проведения в статусе строки закупки** держится до перечитывания закладок, дальше — «Черновик»;
  причина остаётся в сообщениях (ревью кода P3-1).
- **Возвраты поставщику в выборе строки заказа поставщику** (E12.2, проверка ревью P3-1): остаток строки для выбора
  считается по строкам поступлений без вычета возвратов, а запреты — по регистру «Заказы поставщикам» за вычетом
  возвратов; после возврата строка может пропасть из выбора. Сценарий проверяет, что «Пришло» легло на оба изделия, но не
  сверяет количества по изделиям.
- **Приход сверх заказа поставщику не ограничен** документом поступления (прежнее поведение): рабочее место гарантирует
  только, что на момент своей записи заказанное не меньше пришедшего.
- **Тексты рабочего места продублированы** в модуле формы и модуле менеджера (без заказа поставщику, только заказ в
  работе, правка в форме документа) — переводы сопровождаются дважды (ревью кода P3-4).
- **Пустой документ закупки помечается со строками** (E12.2): удаление последней строки ставит пометку непрочитанному
  заново объекту — платформа не помечает изменённый объект; в помеченном документе остаётся последняя строка.
- **Стадии в строках «Продукции» после записи закупок** (E12.2) обновляются кнопкой «Обновить»: запись строки закупки
  перечитывает список заказов и нижние закладки, но не таблицу «Продукция».
- **Коэффициент упаковки в конфигурации всегда 1** (`ОбщегоНазначенияВызовСервера.РассчитатьКоэффициентЕдиницы`
  закомментирована): пересчёт цены основания к упаковке строки в рабочем месте написан, но проверен только с единицей.
- **Клиентская логика закладок закупок** (E12.2) — ручная приёмка: подстановка документа и строки заказа поставщику,
  количество по умолчанию, вставка в начало, «Статус» по отмеченным строкам; сервер покрыт сценарием `АРМЗакупки`.
- **Модуль менеджера рабочего места ~6500 строк** (E12.3a–b, риск планов): движок строк пяти нижних закладок остался в
  модуле менеджера обработки; вынос в общий модуль — отдельной задачей после приёмки E12 (регресс пяти закладок).
- **Материалы ручного выпуска — по технологической карте, автовыпуска — по составу заказа** (E12.3a, как в форме выпуска
  E10): если состав заказа правили вручную, ручной выпуск спишет по карте.
- **Ручной выпуск не пишет «ИзОстатка» и не берёт партии под заказ** (прежнее поведение формы): дефицит на закладке
  «Материалы» после ручного выпуска не уменьшается.
- **Уменьшение и распроведение выпуска с уже отгруженной продукцией** рабочим местом не ограничено (как в форме выпуска):
  автовыпуск реализации пересчитается только её перепроведением.
- **«К выпуску» стадии E8 — только по строкам с материалами заказа** (E12.3a, критик P2): продукция с картой, но без
  материалов в заказе в «Выпуск по заказу» не попадает; строкой её можно выпустить, количество по умолчанию — от заказанного.
- **Правило порядка выпуска и отгрузки — только в рабочем месте** (E12.3a, ревью данных DB-1, DB-2): проведение ручного
  выпуска и реализации заказа из рабочего места повторяет проверку в своей транзакции под блокировкой истории статусов
  заказа; форма выпуска и форма реализации правило не проверяют — выпуск задним числом перед проведённой реализацией
  (или реализация перед поздним выпуском) в форме по-прежнему даёт двойной выпуск автовыпуском.
- **Черновик реализации, после которого проведён ручной выпуск** (E12.3a, DB-5), в рабочем месте не проводится и не
  правится строками: пометить его «Статусом» и ввести отгрузку заново.
- **Налоговые накладные на каждое обновление закладки «Отгрузки»** (E12.3a, DB-3): проверка читает шапку и две табличные
  части налоговых накладных по `Основание` и `ОтгрузкаТовара` без индексов — замер на базе drissotex.
- **`ВыполнениеПоПродукции` — пакет из восьми запросов** (E12.3a, ревью кода P3-5) при выборе продукции строки и после
  записи выпуска (предупреждение сверх заказа); отдельный лёгкий запрос плана не делался — работает верно, замер на
  большой базе.
- **Черновик после отказа проведения нового выпуска и реализации не покрыт уровнем 3** (E12.3a, astra P2): сценарии идут в
  транзакции, отказ проведения портит её, и запись черновика свежим объектом в ней невозможна; сценарий проверяет, что
  строка до записи проходит проверки и не проводится.
- **«Выпуск по заказу» не проверяет технологические карты** (E12.3a, ревью кода P3-1): продукция без карты заполняется без
  материалов, как в форме выпуска; строчный ввод такую продукцию отклоняет.
- **Отказ проведения выпуска и реализации не хранится**: статус строки после перечитывания — «Черновик», причина — в
  сообщении (как в E12.2).
- **Клиентская логика закладок «Выпуск» и «Отгрузки»** (E12.3a) — ручная приёмка: выбор продукции заказа с количеством по
  умолчанию, подстановка документа, «Статус» по отмеченным строкам, «Выпуск по заказу»; сервер покрыт сценарием
  `АРМВыпускОтгрузка`.
- **Коэффициент упаковки продукции в выпуске** (E12.3a, astra P1): материалы считаются от количества в базовых единицах,
  но коэффициент в конфигурации всегда 1 — сценарий с упаковкой продукции невозможен.
- **Стадии старых заказов с поступлениями из переработки** (E12.3b, решение владельца 2026-09-15 «не пересчитывать»):
  обработчика пересчёта нет — стадия меняется при следующей записи любого документа контура заказа; перепроведение старой
  реализации уменьшит её автовыпуск на поступившее от переработчика.
- **Стоимость услуг переработчика** в поступлении из переработки скрыта от оператора без сумм (форма документа): кнопка
  рабочего места открывает форму как есть, стоимость вносит пользователь с суммами (решение владельца 2026-09-15).
- **Поступление из переработки задним числом** из формы после проведённой реализации даёт двойной выпуск автовыпуском:
  правило порядка выпуска и отгрузки проверяет только рабочее место (E12.3b, критик плана P3).
- **Продукция и выпуском, и из переработки** по одному заказу считается дважды, ограничения нет (как выпуск сверх заказа).
- **Форма передачи в переработку не контролирует остаток партии** (E12.3b, ревью данных DB-2, прежнее поведение): после
  поступления в форме можно уменьшить количество или отменить проведение передачи, остаток у переработчика уйдёт в
  минус; рабочее место такую передачу не меняет и советует новую. Проверка смены переработчика в форме — без блокировки.
- **Старые заказы с поступлениями из переработки после реализации** (E12.3b, ревью данных): рабочее место не даёт править
  и проводить такую реализацию и не предлагает её в выборе документа отгрузки — правило порядка выпуска и отгрузки.
- **Оператор без сумм читает регистры взаиморасчётов с поставщиками и дополнительных расходов** (ревью безопасности SEC-1,
  не из E12.3b): долг переработчику за услуги доступен отчётам без привилегий; права ролей — решение владельца.
- **Черновики поступлений из переработки не запрещают правку передачи** (E12.3b, решение по astra P2-2): их проведение
  проверит остаток у переработчика и откажет, если передачу уменьшили.
- **Закладка «Переработка» и начальная страница оператора** (E12.3b) — ручная приёмка: смок начальную страницу не
  открывает; сервер покрыт сценарием `АРМПереработка`.
- **Итог движения на закладке «Документы» считается по документам целиком** (E12.4, ревью плана P2-4): документ второго
  уровня может содержать строки других заказов — они входят в «поступило/списано/остаток» полностью. Предупреждение —
  в подсказке колонок; точный итог по строкам заказа требует разбора строк каждого документа.
- **Движение суммирует разные номенклатуры в одну цифру** (E12.4): количество приводится к базовым единицам, но
  материалы в килограммах и в штуках складываются вместе — как и в остальных колонках количества рабочего места.
- **Кнопка ввода на основании остаётся видимой после снятия флажка** (E12.4, замечание владельца 3): видимость
  пересчитывается при активизации строки и смене верхней закладки; снятие отметки с единственной строки без смены
  текущей события не даёт. Нажатие такой кнопки даёт прежнее сообщение «Выберите заказ».
- **Число строк в заголовке нижней закладки собирает модуль формы** (E12.4): платформенный `ПутьКДаннымЗаголовка`
  = «<таблица>.RowsCount» (FORMS-STYLE, правило 4) работает только для табличной части объекта — для реквизита формы
  типа ТаблицаЗначений платформа отвечает «Неверный путь к данным» (проверено загрузкой 2026-09-16). Базовые заголовки
  закладок продублированы в модуле и в `gen7.py`: при переименовании закладки править оба места.
- **Запрос движения документов выполняется на каждое переключение строки заказа** (E12.4, ревью плана P2-6): ещё одно
  объединение поверх девяти прежних запросов `ДанныеТекущегоЗаказа`. Встраивание в запрос списка документов левым
  соединением — отдельной задачей вместе с выносом движка строк.
- **Сценарий `НастройкиПрограммы` падает на текущем часовом поясе** (найдено 2026-09-16): `ТекущийЧасовойПояс()` даёт
  «+00», которого нет в `ВариантыЧасовыхПоясов()`. Воспроизводится и на нетронутой базе 2.0.15.34 — к сборке E12.4
  отношения не имеет, но блокирует «все тесты пройдены» в чек-листе релиза.
- **Комплект в колонках движения считается одной позицией** (E12.4, ревью кода): склад при проведении разворачивает
  комплект в состав. Рабочее место комплекты в строках не вводит, в движении они появятся только из документа,
  введённого в своей форме.
- **Знаки движения покрыты тестом только на приходе материалов** (E12.4, ревью кода P3): расход материалов, приход
  и расход продукции проверяются глазами. Перепутанные колонка или знак в `ПарыДвиженияДокументов` сценариями
  не ловятся.
- **Итоги подвала «Документов» считает форма отдельным циклом** (E12.4, ревью кода P3): подсказка индикатора считает
  те же величины в модуле менеджера. Общая экспортная функция итогов — вместе с выносом движка строк.
- **`ОтображениеПодсказки = Кнопка` на колонке таблицы** (E12.4): прецедента в конфигурации нет, загрузка и смок
  проходят; где платформа рисует значок — у заголовка или в ячейках — проверяется только глазами.
- **Интерактивный мастер первого запуска не покрыт тестами** (2.0.15.31): headless-заполнение идёт через обработку
  `НачальноеЗаполнение` мимо формы, поэтому ошибка «Object field is not writable (Параметры)» в `ФормаПервогоЗапуска` с
  2.0.14.11 дожила до ручного запуска у владельца (правило tiny1C bsl-014).
- **Уровень 4 не покрывает закладку «Продукция»**: окончание редактирования, выбор черновика, удаление с вопросом, вставка
  строки в начало (Ins, F9, Esc), выбор заказа основания по отметке и панель выделенных строк (2.0.15.31) — ручная
  приёмка; сервер (`Обработки.АРМПроизводство`) покрыт сценарием уровня 3.

## Найдено в S1 волны переработки (2026-09-19, способ изготовления и процент потерь)

- **Обязательные гейты этапа прошли на субагентах Claude, а не на Codex.** `ai-review-plan`,
  `ai-review` и `ai-database` обязательны для этапа (новый объект метаданных, реструктуризация
  регистра с историей, обработчик обновления), все три вернули `You've hit your usage limit`.
  Решение владельца 2026-09-19 — не перепрогонять после выхода из лимита. Сборка 2.0.15.39 проверена
  той же моделью, что писала код; независимого ревью у неё нет.
- **Обновление даёт версию истории данных на каждую технологическую карту.** У документа
  `<dataHistory>Use</dataHistory>`, обработчик перезаписывает все карты базы, а режим загрузки
  версионирование не подавляет. Автор версий — пользователь, под которым шло обновление. Плюс
  переиндексация полнотекстового поиска из-за `fullTextSearch` у новых ресурсов. На больших базах
  стоит померить прирост `_DataHistory*` до обновления клиента.
- **Верхней границы у процента потерь нет** — решение владельца 2026-09-19: 100 % и больше остаются
  ответственностью пользователя. Запрещён только отрицательный. Если арифметика S5 окажется
  чувствительной к этому, вопрос поднимается заново там, а не в техдолге.
- **`ТехнологическиеКарты.СвойстваИзготовленияОтличаются` экспортная, а рабочих потребителей нет**:
  карточка номенклатуры перешла на `ВерсияКартыУстарела`, которая читает версию один раз. Функция
  оставлена осознанно — она отвечает на более узкий вопрос «изменились ли только способ и процент», и
  именно это разделение проверяет сценарий. Если S2–S9 потребителя ей не найдут, удалить вместе с
  веткой сценария.
- **Способ изготовления на закладке «Материалы» пуст у строки без версии карты.** Колонка берёт способ
  из версии, на которую ссылается строка товаров (`Техкарта`); материалы, введённые руками без карты,
  остаются с пустым способом. Для показывающей колонки это честное «способ неизвестен».
- **Строки состава с пустой номенклатурой способа не получают** ни в документе, ни в движениях —
  правило общее с маркерной записью отмены карты. Такие строки всё равно отсекает проверка
  заполнения, но если когда-нибудь появится законная строка без материала, правило придётся
  пересмотреть в обоих местах: `ТехнологическаяКарта.ПередЗаписью` и
  `ОбновлениеПрограммыОбработчики.ЗаполнитьСпособыИзготовленияТехкарт`.
- **Базовая линия линта пересоздана** на состоянии 2.0.15.39 (решение владельца 2026-09-19): приняты
  169 замечаний, накопившихся с 2026-08-26. Прежняя — `build/bsl-json-baseline-20260826.json.bak`.
  Если часть принятого стоило чинить, разбирать по этой копии.
