# Автоматизированные тесты maERP

Тесты гоняются на живой базе (по умолчанию — тестовая база сборки `build/ib`,
она же drissotex-тест). Реестр тестов с покрытием — `docs/TESTS.xlsx`.

## Состав тестов

| Уровень | Что проверяет | Чем | Разрушает данные |
|---|---|---|---|
| 0. Статический анализ | Стиль и типовые ошибки BSL | `scripts/lint.sh` (BSL LS) | нет |
| 0.5. Сверка ссылок | Обращения к объектам, которых в конфигурации нет | `scripts/refcheck.py` | нет |
| 0.7. Проверка динамических списков | Колонки форм против произвольного запроса списка: поля, которых в запросе нет, и запрос не из той таблицы | `scripts/listcheck.py` | нет |
| 1. Проверка конфигурации | Ссылочную целостность метаданных, компилируемость модулей (тонкий клиент, сервер) | `scripts/smoke.sh`, шаг CheckConfig | нет |
| 2. Смок «открыть все формы» и «напечатать всё» | `ПриСозданииНаСервере` каждой формы — на новом объекте и на образце из базы; формирование каждой печатной формы | `scripts/smoke.sh`, шаг СмокТест | нет |
| 2.5. Headless-первый запуск | Пустая база из CF заполняется из JSON (организация, пользователи, служебный администратор), повтор ничего не создаёт, визард не откроется, смок под созданным администратором проходит | `scripts/initial-fill-test.sh <файл.cf>` | да — на своей базе `build/ib-initial`, которую пересоздаёт |
| 3. Жизненный цикл документов | Создание, запись и проведение ключевых документов, движения по регистрам: закупка, продажа с оплатой, возврат, выпуск продукции, переработка, зарплата, безналичные деньги, дополнительные реквизиты, отрасль | `scripts/doctests.sh`, обработка `ТестыДокументов` — каждый сценарий в транзакции с откатом | нет (откат) |
| 4. Сценарные UI-тесты | Клиентскую логику, обработчики, подборы | план: тест-клиент платформы / Vanessa | да — только на копии |

Уровни 0–3 реализованы. Уровень 4 добавляется, когда появится клиентская логика,
которая им реально ломается; решение фиксируется здесь и в `docs/TESTS.xlsx`.

Уровни 0, 0.5 и 0.7 работают на исходниках и не требуют базы — их гоняю сразу
после правки модулей и форм. Уровни 1–3 работают на базе `build/ib`, а её обновляет из EDT
владелец: без этого проверяется код прошлой сборки.

## Сверка ссылок (уровень 0.5)

```bash
scripts/refcheck.py
```

`Константы.Х`, `Перечисления.Х`, `РегистрыСведений.Х`, `БиблиотекаКартинок.Х`,
типы `Тип("ДокументСсылка.Х")`, полные имена в строках (`"ОбщаяФорма.Х"`,
`"Документ.Х.Макет.Y"`) и вызовы `ОбщийМодуль.Метод()` сверяются с деревом
метаданных `src/cf/src`. Всё это компилируется и проходит CheckConfig, но падает
в рантайме — а смок видит только то, что лежит на пути открытия формы.

Там же сверяются стандартные команды форм документов со свойством «Проведение»: у
проводимого документа нет `WriteAndClose`, у непроводимого — `Post`, `PostAndClose`,
`UndoPosting`. Такая кнопка не даёт загрузить конфигурацию в базу («Неверное имя
команды элемента формы»), а EDT по исходникам её не видит (2.0.15.16: форма заказа
клиента после включения проведения в E6).

Имена приводятся к NFC: macOS хранит имена каталогов в разложенном виде, и без
нормализации любое имя с «ё» или «й» выглядит несуществующим.

Принятые расхождения (стандартные картинки платформы, наследие бухгалтерского
контура) лежат в `scripts/refcheck-known.txt`; обновить файл целиком —
`scripts/refcheck.py --update-known`. Новое расхождение — ошибка: разбирается до
сдачи работы, а в файл попадает только вместе с записью причины в
`docs/TECHDEBT.md`.

## Проверка динамических списков (уровень 0.7)

```bash
scripts/listcheck.py
```

Для каждого динамического списка с произвольным запросом (`customQuery`)
сверяются две вещи: поле каждой колонки таблицы есть среди полей верхнего
уровня запроса, и `mainTable` встречается в тексте запроса. Стандартные поля
списка (`Ссылка`/`Ref`, `Код`, `Наименование`, `ПометкаУдаления` и родня)
платформа отдаёт без запроса — они не проверяются.

Дыра, ради которой проверка появилась: запрос списка можно подменить целиком, и
ни один машинный контроль этого не заметит. Такой запрос компилируется,
CheckConfig и EDT его принимают, а смок открывает форму через
`ПолучитьФорму()` — то есть выполняет только `ПриСозданииНаСервере`, при
котором данные списка не читаются. Ошибка достаётся пользователю при первой
отрисовке таблицы (инцидент 2026-09-09: список поступления товаров и услуг
выбирал заказы поставщику, тринадцать колонок формы остались без полей).

Принятые расхождения — `scripts/listcheck-known.txt`, обновить файл целиком —
`scripts/listcheck.py --update-known`. Правило то же, что у сверки ссылок:
новое расхождение разбирается до сдачи работы, а в файл попадает только вместе
с записью причины в `docs/TECHDEBT.md`.

Клиентская половина той же дыры проверкой не ловится: обработчик строки, который
читает `ТекущиеДанные.Поле`, требует колонки в таблице, а таблица может
строиться программно. Это заявка на уровень 4 — `docs/ROADMAP.md`, блок
«Покрытие тестами».

## Как устроен смок-тест (уровень 2)

Обработка `СмокТест` (подсистема «Автотесты», см. ниже):

1. Клиент запускается с параметром `/C "СмокТест;<путь-к-отчёту>"`. Хук в
   `ПриНачалеРаботыСистемы` модуля приложения видит параметр, открывает форму
   обработки и пропускает обычную инициализацию клиента.
2. Первым делом в отчёт пишется строка `СТАРТ;<дата>` — по ней скрипт понимает,
   что клиент поднялся и прогон пошёл.
3. **Проход 1, формы.** Сервер собирает полные имена всех форм конфигурации
   (справочники, документы, журналы, отчёты, обработки, планы, регистры,
   критерии отбора, общие формы); формы только для мобильного устройства
   пропускаются. Клиент по каждому имени вызывает `ПолучитьФорму()` — форма не
   показывается, но её `ПриСозданииНаСервере` выполняется полностью.
4. **Проход 2, формы объектов на образцах.** Те же формы объектов, но открытые
   с параметром `Ключ` по образцу из базы: основная форма объекта каждого
   справочника, документа, плана видов характеристик и плана счетов, а у объектов
   с группами — ещё и основная форма группы. Образец берётся тот же, что и для
   печати (`ОбразецОбъекта`): последний проведённый документ, первый непомеченный
   элемент справочника, для формы группы — первая непомеченная группа. Если
   подходящего объекта в базе нет, форма даёт строку `ПРОПУСК` — заводить образцы
   умеет `scripts/fixtures.sh`. Проход добирает и те формы объектов, которые в
   проходе 1 стоят в пропусках: создание нового там запрещено бизнес-логикой или
   правами, а открыть существующий объект можно.
5. **Проход 3, печатные формы.** По каждому объекту из
   `ПечатныеФормы.ОбъектыСКомандамиПечати()` берётся один образец из базы
   (последний проведённый документ; для лидов — заказ покупателя по лиду) и
   формируется каждая печатная форма объекта через
   `ПечатныеФормы.СформироватьПечатныеФормы`. Проверяется, что модуль менеджера
   вернул табличный документ; пустой документ пишется строкой `ПУСТО` и ошибкой
   не считается. Печатных форм в конфигурации больше полусотни, и ни одна из них
   не открывается проходом по формам — до этого прохода они не проверялись ничем.
   Дополнительно в тексте сформированной формы ищется номер документа-образца:
   если его там нет, пишется строка `ПОДОЗРЕНИЕ` — форма сформировалась, но шапка
   может быть пустой (см. «Чего прогон не видит»). Это не ошибка прогона: список
   таких строк смотрится глазами, а печатные формы, где номера заведомо нет,
   заносятся в `ПечатныеФормыБезНомера()` вместе с причиной здесь. Сейчас там три формы:
   чек возврата от покупателя (обычного и налогового) печатает номер документа-основания,
   а не свой собственный, а Z-отчёт кассовой смены номера не печатает вовсе — в макете
   дата, касса, организация, операция и сумма.
6. В конце пишется отчёт и клиент завершает работу. При интерактивном открытии
   формы обработки (без параметра запуска) итог выводится в окно сообщений.

Все три прохода только читают данные, поэтому прогон безопасен и на рабочей базе
клиента — например, как приёмочный контроль после обновления.

### Чего прогон не видит

Зелёный прогон означает «форма открылась» и «печать не упала», а не «напечатано
правильно». Пустое поле в шапке для прогона неотличимо от заполненного: платформа
исключения не выдаёт. Отсюда проверка номера документа (строка `ПОДОЗРЕНИЕ`) — она
ловит самый частый случай, шапку, собранную из итоговой строки запроса без
агрегатных функций (`docs/TECHDEBT.md`). Всё остальное содержимое печати —
суммы, реквизиты организации, скидки — проверяется только глазами, по одной
готовой форме каждого вида перед сдачей релиза.

Пустая форма объекта была вторым слепым блоком: `ПолучитьФорму(Имя)` без параметров
выполняет `ПриСозданииНаСервере` на новом объекте, и ветки «только для записанного
объекта» проверены не были. Так мимо прогона прошла ошибка в
`Справочники.Номенклатура.КомплектИспользованВДокументах` (2026-08-27): для пустой
ссылки функция выходит по первой строке, а на существующем товаре падал запрос.
Закрыто проходом 2 — тем же формам передаётся `Ключ` по образцу из базы.
**Неосновные** формы объекта, которые подставляет `ОбработкаПолученияФормы`
(`ФормаЭлементаECommerce` номенклатуры и контрагентов, `ФормаДокументаECommerce`
заказа, `ФормаЭлементаЕдиница` и `ФормаЭлементаУпаковка`), проход 2 открывает по
явному списку `ДополнительныеФормыОбъектов()` в модуле менеджера: во время
выполнения отличить форму объекта от формы списка нельзя, поэтому список ведётся
руками — новая подстановка в `ОбработкаПолученияФормы` означает строку в нём.
Образец для форм единиц измерения подбирается по виду измеряемой величины
(`ОбразецПоУсловию`).

Смок не проверяет, что документ проводится и что движения правильные, — это
уровень 3 (см. «Тесты документов» ниже).

## Что лежит в конфигурации: подсистема «Автотесты»

Всё, что относится к тестам, собрано в отдельной подсистеме `Автотесты`:

- `Обработка.СмокТест` — прогон. В **модуле менеджера** серверная логика всех
  проходов: `ПроверяемыеФормы()` (состав форм и список пропусков),
  `ФормыОбъектовСОбразцами()` (основные формы объекта и группы с образцами к ним) и
  `ПрогнатьПечатныеФормы()` (образцы объектов, формирование, пропуски печати).
  В **модуле формы** — только клиентский цикл прогона и запись отчёта.
- `Обработка.ТестовыеДанные` — заполнение базы образцами объектов печати (модуль
  менеджера, своей формы нет; см. раздел про образцы ниже). ЗАПИСЫВАЕТ данные.
  Экспортирует `ПервыйЭлемент(ИмяСправочника)` — им пользуются и тесты документов.
- `Обработка.ТестыДокументов` — уровень 3. В **модуле менеджера** сценарии
  (`ИменаСценариев()`, `ВыполнитьСценарии()`, по функции `Сценарий<Имя>` на сценарий),
  данные сценариев, помощники создания документов и чтения регистров. В **модуле
  формы** — только запуск и отчёт. Данных не оставляет: транзакция каждого сценария
  откатывается.
- `ОбщийМодуль.АвтотестыКлиент` — `ОбработатьЗапускТеста()`: разбирает параметр
  запуска (`СмокТест;<отчёт>`, `ТестовыеДанные;<отчёт>` или `ТестыДокументов;<отчёт>`)
  и открывает форму прогона. Модуль управляемого приложения зовёт его одной строкой
  и о тестах больше ничего не знает.

Тем же приёмом (параметр `/C`, маркеры `СТАРТ;`/`ИТОГ;`, ожидание в `run-lib.sh`) устроен
headless-первый запуск — обработка `НачальноеЗаполнение` подсистемы «Обновление
программы», но это не тест, а механизм развёртывания (`docs/DEPLOY.md`); тест к нему —
`scripts/initial-fill-test.sh`.

Подсистема **не входит в командный интерфейс**: показывать пользователю нечего,
прогон запускают скрипты, а вручную обработка открывается через «Все функции».
Понадобится пункт меню — достаточно поставить `includeInCommandInterface` в
`Автотесты.mdo` и решить, каким ролям он виден.

Скрипты (`lint.sh`, `refcheck.py`, `listcheck.py`, `smoke.sh`, `fixtures.sh`, `doctests.sh`) живут в `scripts/` рядом с
остальной сборочной обвязкой — в конфигурацию они не входят. Ожидание клиента и отчёта
при headless-запуске 1С — общая часть `scripts/run-lib.sh`.

## Алгоритм запуска и сбора ошибок

```bash
scripts/smoke.sh [каталог-базы]      # по умолчанию build/ib
```

База с пользователями: логин и пароль — только через переменные окружения
`SMOKE_USER` / `SMOKE_PWD` в момент запуска, в файлы и скрипты они не попадают.
Таймаут шага — `SMOKE_TIMEOUT` (по умолчанию 900 с).

Шаги скрипта и артефакты:

1. **CheckConfig** — `DESIGNER /CheckConfig -ConfigLogIntegrity
   -IncorrectReferences -ThinClient -Server`. Замечания — в
   `build/check-config.log`; непустой лог = провал шага. Если среди замечаний
   есть ошибка компиляции (позиция вида `{ОбщийМодуль.Имя.Модуль(859,37)}`),
   шаг 2 не запускается вовсе: клиент с несобирающимся модулем всё равно не
   поднимется, а прогон только съест таймаут.
2. **СмокТест** — `ENTERPRISE /C "СмокТест;build/smoke-report.txt"`. Формат
   отчёта, по строке на событие:
   - `СТАРТ;<дата>` — клиент дошёл до прогона;
   - `ОШИБКА;<что проверялось>;<описание в одну строку>` — форма не открылась
     или печатная форма не сформировалась;
   - `ПУСТО;<печатная форма>` — сформирована, но табличный документ пустой;
   - `ПРОПУСК;<что>;<причина>` — проверять не на чем (в базе нет объекта) или
     проверка исключена списком;
   - `ИТОГ;Всего=N;Ошибок=M;ПечатныхФорм=K;ОшибокПечати=L` — маркер завершения
     прогона (по нему скрипт понимает, что клиент отработал).
   Строки `СТАРТ` скрипт ждёт `SMOKE_START_TIMEOUT` секунд (по умолчанию 120):
   не дождался — снимает клиент и валит прогон, не тратя общий таймаут.
   Клиентский лог платформы — `build/smoke-client.log` (смотреть, если отчёт
   не появился: не найдена база, не прошла аутентификация и т.п.).

Скрипт не полагается на коды возврата `1cv8` (на macOS процесс-обёртка может
падать, пока работу делает дочерний процесс): он ждёт завершения процессов по
паттерну командной строки и признаков готовности в файлах. Итог — `ТЕСТЫ
ПРОЙДЕНЫ` / `ТЕСТЫ НЕ ПРОЙДЕНЫ` и код возврата 0/1.

### Разбор ошибок

- Каждая строка `ОШИБКА;` разбирается до причины: либо это дефект (чинится до
  релиза), либо форма легитимно не открывается без параметров — тогда она
  вносится в `ПропускаемыеФормы()` модуля формы обработки `СмокТест`
  с комментарием-причиной, и причина дублируется в реестре `docs/TESTS.xlsx`.
- Печатная форма, которую нельзя сформировать без параметров команды, вносится
  так же — в `ПропускаемыеПечатныеФормы()`. Строки `ПУСТО;` разбираются глазами:
  у образца могло не быть строк, но пустая форма у документа с данными — дефект.
- Строки `ПРОПУСК;Печать.<объект>;в базе нет ни одного объекта` — не ошибка, но
  и не покрытие: если объект важен, в базу заводится образец.
- Замечания CheckConfig разбираются все; допустимых «известных» замечаний нет —
  если такие появятся, для них заводится явный фильтр в `smoke.sh` с причиной.

## Кто что делает

**Машина (Claude) может сама:**

- править тесты и обработку `СмокТест`, гонять `scripts/lint.sh`,
  `scripts/refcheck.py` и `scripts/listcheck.py`;
- запускать `scripts/smoke.sh` и `scripts/doctests.sh` на `build/ib` и разбирать
  `smoke-report.txt` / `check-config.log` / `doctests-report.txt` до причин;
- чинить найденные дефекты и пополнять список пропусков;
- вести реестр `docs/TESTS.xlsx` и этот документ.

**Человек (владелец):**

- Refresh и импорт в EDT, проверка конфигурации, обновление `build/ib` из EDT —
  без этого база не содержит свежих правок, и смок проверяет старый код;
- закрыть базу в EDT перед прогоном (агент EDT блокирует `build/ib`);
- запуск на базах клиентов (доступы — только у владельца);
- приёмка: ручной регресс по чек-листам `docs/drissotex-analysis/wave*-check.md`
  и решение «релизим/не релизим»;
- проверка обновления на копии базы предыдущей версии: механизм
  `ОбновлениеПрограммы` при первом входе администратора поднимает версию в
  регистре `ВерсииПодсистем`, ранее выполненные заполнения данных не
  повторяются, показывается «Что нового», в журнале регистрации по событию
  «Обновление программы» ошибок нет (шаг 7 чек-листа релиза).

## Образцы объектов для печати: обработка ТестовыеДанные

Проход печати проверяет печатную форму только там, где в базе есть хотя бы один
объект (то же и у прохода форм на образцах). На тестовой базе (копия drissotex) образцов не было у 22 объектов из 25 — их печать
не проверялась ничем, и в отчёте они писались строкой
`ПРОПУСК;Печать.<объект>;в базе нет ни одного объекта`.

Образцы заводит обработка `ТестовыеДанные` подсистемы «Автотесты»:

```bash
scripts/fixtures.sh [каталог-базы]      # по умолчанию build/ib
```

Скрипт **записывает данные** — документы остаются в базе. Запускать только на тестовой базе.

Как заполняется образец:

1. Объект, у которого образец уже есть, пропускается — обработка идемпотентна, повторный
   запуск ничего не создаёт.
2. Реквизиты-ссылки заполняются по метаданным: первый непомеченный элемент справочника
   (группы не берутся). Контрагент берётся вместе со своим договором — договор выбирается
   первым, поэтому пара заведомо согласована.
3. Особенности видов документов — хозяйственная операция, строки табличных частей, документ-
   основание — заданы явно в `ДовестиТоварныйОбразец`, `ДовестиДенежныйОбразец` и
   `ДовестиПрочийОбразец`. Новый документ с печатью — новое правило туда же.
4. Документ пробуется провести; если проведение не проходит (в тестовой базе нет остатков,
   не хватает связанных данных), он остаётся записанным: печати этого достаточно.
   В отчёте это видно — `проведен` или `записан без проведения`.
5. Комментарий образца — «Образец автотеста»: по нему видно, что документ заведён прогоном,
   и его можно найти отбором.

Отчёт `build/fixtures-report.txt`:

```
СОЗДАН;Документ.ОприходованиеТоваров;000000012;проведен
ОШИБКА;Документ.ВыплатаЗарплаты;<описание>
ИТОГ;Создано=N;Ошибок=M;Было=K
```

Обработка `ТестовыеДанные` своей формы не имеет: клиентским запускателем служит форма
`СмокТест` — она открывается с параметром `ТолькоТестовыеДанные`, потому что серверную
функцию модуля менеджера с клиента иначе не позвать.

Заполнение — разовый шаг: после него база держит образцы, и прогон печати закрывает все
печатные формы конфигурации. Пересоздали базу — прогнать `fixtures.sh` заново.

## Headless-первый запуск: тест initial-fill (уровень 2.5)

Базу клиента из хаба tinycio поднимает скрипт на сервере, без визарда первого запуска:
обработка `НачальноеЗаполнение` (подсистема «Обновление программы») заводит организацию,
пользователей и служебного администратора из JSON. Контракт — `docs/DEPLOY.md`.

```bash
scripts/dist-build.sh 2.0.14.11            # CF из обновлённой build/ib
scripts/initial-fill-test.sh build/dist/maERP-2.0.14.11.cf
```

Что делает тест (база `build/ib-initial` пересоздаётся, `build/ib` не трогается):

1. Пустая база из CF (`scripts/ib-create.sh`).
2. `scripts/initial-fill.sh` с образцом `scripts/initial-fill.example.json`, пароли
   подставляются случайные — только во временный payload с правами 600, удаляется на выходе.
   Проверка result-файла: `success`, `organization_created`, все `users[].created`,
   `support_admin_created` = `true`; `organizations_total` и `users_total` > 0 — это
   отрицание условия визарда «ноль организаций и ноль пользователей»; слова `password`
   в result нет.
3. Тот же payload второй раз под созданным администратором: успех, всё `created: false`
   (идемпотентность — раннер хаба вправе повторить задание).
4. `scripts/smoke.sh` на этой базе под созданным администратором — итог
   `ТЕСТЫ ПРОЙДЕНЫ`. `INITIAL_FILL_NO_SMOKE=1` пропускает шаг для быстрой проверки.

Образец payload задаёт отрасль «Строительство», поэтому тест сверяет и
`industry_attributes_created`: 4 после первого заполнения (реквизиты объекта
недвижимости у номенклатуры заведены), 0 после повтора.

Проверка визарда глазами остаётся за человеком: на пустой базе визард открывается,
на последнем шаге показывает пароль `tinycio-1c` один раз; на заполненной — не открывается.

## Тесты документов (уровень 3)

Обработка `ТестыДокументов` подсистемы «Автотесты» проводит ключевые документы и
сверяет движения по регистрам — то, чего смок не видит (форма открылась, а проводится
ли документ и правильно ли, он не знает).

```bash
scripts/doctests.sh [каталог-базы]      # по умолчанию build/ib
```

Запуск тот же, что у смока: `/C "ТестыДокументов;<отчёт>"`, маркеры `СТАРТ;` и
`ИТОГ;`, ожидание в `run-lib.sh`, доступ через `SMOKE_USER` / `SMOKE_PWD`. Отчёт
`build/doctests-report.txt`, по строке на сценарий:

- `СТАРТ;Сценариев=N` — клиент поднялся;
- `ОК;<сценарий>;<что проверено>`;
- `ОШИБКА;<сценарий>;<описание>` — не прошла проверка заполнения, проведение или
  сверка движений; в описании — текст исключения и сообщения пользователю из
  `ОбработкаПроведения` (причина отказа);
- `ПРОПУСК;<сценарий>;<причина>` — в базе нет условий для сценария;
- `ИТОГ;Сценариев=N;Ошибок=M;Пропущено=K`.

Как устроен сценарий:

1. **Данные заводятся заново** внутри транзакции сценария: контрагент (покупатель,
   поставщик и субподрядчик) с договорами обоих видов, склад, касса, банковский счёт,
   сотрудник, единица измерения, номенклатура трёх видов — товар, сырьё и продукция с
   технологической картой. Из базы берутся только первая организация, валюта
   регламентированного учёта и предопределённые элементы (ставка «Без НДС», типы
   оплаты, статьи ДДС). Свежие номенклатура и склад дают нулевые остатки, поэтому
   ожидаемые значения не зависят от содержимого базы, а по объекту расчётов сверяется
   долг только по своим документам. Наименования и комментарии помечены «Тест уровня 3».
2. **Документы** датируются на 100 секунд раньше текущего момента с шагом в секунду —
   порядок на оси времени регистров совпадает с порядком создания. Перед записью
   вызывается `ПроверитьЗаполнение()` — как это делает форма; затем запись с
   проведением. Количества и цены — в одном месте, `ЧислаСценариев()`, ожидаемые
   остатки считаются из них.
3. **Сверки** — запросы к остаткам и оборотам регистров (`ТоварыВНаличии`,
   `ТоварыВПереработке`, `ВзаиморасчётыСПокупателями`, `ВзаиморасчётыСПоставщиками`,
   `Продажи`, `ДенежныеСредстваОрганизаций`, `Зарплата`). Долг покупателя —
   положительный остаток, долг поставщику — отрицательный: так их читают
   `ПолучитьЗадолженностьКлиентовПоКлассификации` и
   `ПолучитьЗадолженностьПоставшиковПоКлассификации`, и так их гасят оплаты.
4. **Откат всегда**: и после успеха, и после ошибки. Прогон безопасен на копии базы
   клиента.

Сценарии первой очереди:

| Сценарий | Документы | Что сверяется |
|---|---|---|
| Закупка | Поступление товаров | остаток и себестоимость на складе, долг поставщику по документу |
| Продажа | Заказ покупателя → реализация по заказу → ПКО по реализации | основание и сумма реализации, остаток, количество/выручка/себестоимость в `Продажи`, долг покупателя до и после оплаты, деньги в кассе |
| ВозвратОтПокупателя | Реализация → возврат заполнением по реализации | товар вернулся той же партией (количество и себестоимость), продажи сторнированы, долг покупателя закрыт |
| ВыпускПродукции | Поступление сырья → выпуск по техкарте | списание сырья по карте, приход продукции по себестоимости сырья; ПРОПУСК при выключенной опции «Производство» |
| ТехнологическиеКарты | Две версии рецептуры → правка в тот же день → отмена | границы дат, удаление материала без подмешивания из старой версии, отсутствие дублей, сравнение состава, заполнение выпуска с учётом опции упаковки, единичные веса при отсутствии карты, маркер отмены |
| ПереносТехнологическихКарт | Старые табличные части → обработчик обновления → повтор | дата создания и запасная дата 01.01.2000, ЕИ → упаковка, пустая характеристика, свёртка повторяющихся материалов, сохранение исходной ТЧ, отсутствие дублей, защита будущих карт, отмен и черновиков |
| СебестоимостьТехкарт | Перенос → мультивыпуск → новая рецептура → перепроведение | заполнение материалов по двум картам, распределение себестоимости 50/20 при весах 5/2, сохранение старой себестоимости после новой версии; ПРОПУСК без ФО «Производство» |
| ДатыТехкарты | Новая версия → повторная запись дня → попытка изменить прошлую версию | начало дня для новых и существующих карт, отсутствие дублей, подбор допустимой даты и серверный запрет даты раньше последней версии, сохранение старого состава при отказе, учёт будущей версии |
| ПоказателиТехкарты | Поступления и продажи → оценка состава | последние закупочная и продажная цены, себестоимость, ограничивающий материал, общий остаток для дублей, усечение дробного выпуска, раздельные характеристики, отрицательная маржа, отсутствие цены и пустой состав |
| СпособыПополненияЗапасов | Номенклатура → обработчик обновления → повтор | умолчания шести типов, серверная страховка пустого значения, сохранение «Оба», заполнение существующего элемента, пропуск группы и идемпотентность повторного запуска |
| КоммерческоеПредложение | Предложение → печать → заказ покупателя на основании | Черновик и ручной статус, перенос описания в номенклатуру, данные и строки заказа, связь через основание, статус «Выигран», отсутствие смены статуса при ошибке заполнения заказа |
| ВидОперацииЗаказа | Заказ покупателя: заполнение, запись, заказы по предложениям → обработчик обновления → повтор | значение заполнения «Продажа», заполнение из отбора списка (`fillFromFillingValue`), страховка пустого вида в `ПередЗаписью`, смена вида у записанного заказа, «Производство» по предложению с продукцией и «Оба» при включённой опции, представление заказа на производство и стандартное представление продажи (эталон — предложение), заполнение существующих заказов и идемпотентность |
| МатериалыЗаказа | Заказ клиента на производство: заполнение по техкартам → запись → заказ по КП → продажа | уникальные коды строк при заполнении, состав, количество за единицу и итог, версия карты и упаковка по опции, позиция без карты, строка с повторным кодом, пересчёт при записи, один состав продукции (в том числе при другой характеристике), строка без материалов, смена номенклатуры и удаление строки, материалы по КП сразу, очистка у продажи, режим загрузки, отказ настоящей записи; ПРОПУСК без ФО «Производство» |
| ПроведениеЗаказа | Заказ клиента на производство: проведение → перепроведение → правка карты технологом → заказ с составом карты → новый состав → отказы → отмена проведения → продажа | регистрация версии карты при отличии состава с датой проведения и её действующий статус, отсутствие перезаписи карты при неизменном составе (по версии данных), неизменность правки технолога при перепроведении, совпадение с действующей картой, перезапись карты дня новым составом, сохранение упаковки карты, свёртка и перенос упаковки в обеих ветках опции, отказы проверки проведения (продукция без материалов, пустой материал, материал = продукция, разные упаковки, будущая карта), права ролей на карты, запрет смены вида проведённого заказа, проверка заполнения при записи из формы, заголовок формы, отказ проведения продажи; ПРОПУСК без ФО «Производство» |
| ПотребностиПроизводства | Заказ клиента на производство → закупки без потребности → заказы поставщику → поступления → возвраты поставщику → отчёт → перепроведение и отмена → отказы | «Потребность» по продукции в базовых единицах и перепроведение без дублей; заказ поставщику под продажу, под непроведённый заказ и строка из предварительного заказа без движений; ввод по дефициту с основанием и продукцией, явная продукция раньше распределения, излишек на последнюю продукцию; «Пришло» по коду строки и по «Заказано» заказа поставщику; ввод поступления по заказу клиента (заказано − пришло); сторно возвратов по своему поступлению в обратном порядке; отчёт и флаг «только дефицит»; перепроведение каждого документа, отмена в обратном порядке и повторное проведение; отказ записи проведённого заказа с изменённой потребностью; отказ ввода без потребности последним шагом; ПРОПУСК без ФО «Производство» |
| СтадииЗаказа | Заказ клиента на производство → заказ поставщику → поступления → выпуски → реализации → возврат → оплаты → отмены проведения; продажа | стадия и индикатор расчёта и последней записи истории на каждом шаге; черновик и перепроведение без новых записей истории; откат стадии отменой поступления, возврата, оплаты и проведения заказа; выпуск и отгрузка частями; оплата реализации, аванс «на основании заказа» с зачётом в реализации, аванс по строке оснований; поздняя веха (отгрузка без закупок и выпуска); заказ без материалов; продажа и смена вида операции без стадий; ПРОПУСК без ФО «Производство» |
| АвтовыпускПриОтгрузке | Заказ клиента на производство → закупка под заказ → реализации с автовыпуском → ручной выпуск → перепроведение, отмена, пометка → запреты → реализации в одну секунду → серия → отказ | расчёт без записи: продукция, материалы с партией под заказ, продукция по техкарте, «ИзОстатка»; один автовыпуск на секунду раньше реализации; себестоимость автовыпуска и продажи по ФИФО; общая партия материала не тронута; к выпуску за вычетом свободного ручного выпуска; «ИзОстатка» в движениях и в доле поступления стадии; перепроведение без дублей; отмена проведения, перенос и снятие пометки удаления; запрет ручной правки автовыпуска и основания-реализации у ручного выпуска; выключенная константа и продажа без автовыпуска; граница по моменту времени; две реализации в одну секунду; серия продукции; отказ по нехватке материала последним шагом; ПРОПУСК без ФО «Производство» |
| Переработка | Поступление сырья → передача в переработку → поступление из переработки заполнением по передаче | остатки на складе и у переработчика, себестоимость продукции с услугами, долг переработчику |
| Зарплата | Начисление → ведомость заполнением по начислению → РКО по ведомости | остаток по сотруднику после начисления и выплаты, сумма ведомости, деньги в кассе; ПРОПУСК, если дата начала расчёта зарплаты позже даты теста |
| БезналичныеДеньги | Поступление и реализация → поступление безналичных по реализации → списание поставщику по поступлению | оба долга закрыты, остаток на счёте |
| ДополнительныеРеквизиты | `УправлениеСвойствами` | реквизит и сведение заводятся идемпотентно, значения читаются по имени из табличной части и регистра |
| Отрасль | `НачальноеЗаполнениеВызовСервера.ПрименитьОтрасль` | «Строительство» заводит четыре реквизита и ставит константу, повтор — 0, «Торговля» — 0 |
| НастройкиПрограммы | `Обработки.НастройкиПрограммы` (панель настроек) | запись константы модулем менеджера и чтение назад, признак «расположение ФО» для Бухгалтерии, имя константы по пути данных, список часовых поясов содержит текущий, заголовок окна включает заголовок системы |

**Новый документооборот — новый сценарий.** Разработка документа не считается
законченной без сценария здесь: имя в `ИменаСценариев()`, ветка в `СценарийПоИмени`,
функция `Сценарий<Имя>` и строка в таблице выше и в `docs/TESTS.xlsx`. Помощники
для этого уже есть: `НовыйДокумент`, `ДобавитьСтрокуТоваров`, `Провести`,
`ДокументЗакупки`, `ДокументРеализации`, `ОстатокТовара`, `ПроверитьРавенство`.

Чего уровень 3 не видит: клиентскую логику форм (обработчики `ПриИзменении`,
подборы, автозаполнение цен) — документы заполняются кодом. Это уровень 4.

## Регламент

1. **Перед сборкой релиза прокатываются все тесты**: `scripts/lint.sh` без
   новых замечаний против baseline, `scripts/refcheck.py` без новых расхождений,
   `scripts/smoke.sh` и `scripts/doctests.sh` на обновлённой `build/ib` с итогом
   `ТЕСТЫ ПРОЙДЕНЫ` и `scripts/initial-fill-test.sh` на собранном CF с итогом
   `ТЕСТ ПРОЙДЕН`. Это обязательный шаг чек-листа релиза (`docs/RELEASING.md`),
   релиз с падающими тестами не собирается.
2. **Новый функционал расширяет тесты.** Разработка формы, документа или
   команды не считается законченной, пока: новые формы проходят смок (они
   попадают в перебор автоматически — но прогон надо выполнить; неосновная форма
   объекта — строка в `ДополнительныеФормыОбъектов()`); для нового документооборота
   добавляется сценарий в `ТестыДокументов` (создание, запись, проведение, движения);
   изменения состава тестов фиксируются в `docs/TESTS.xlsx`.
3. После обновления базы клиента смок-прогон и тесты документов на ней —
   рекомендуемый приёмочный контроль (уровни 1–3 данные не меняют).

### Проверка E1 (2.0.15.6)

Пять сценариев техкарт описаны выше; всего в обработке 15 сценариев.
Прогон — после Refresh в EDT и обновления `build/ib` до 2.0.15.6.
Сценарии переноса вызывают полный обработчик обновления
внутри транзакции с откатом, поэтому проверяют также перенос старых карт,
имеющихся в тестовой базе. Записи итога в журнале регистрации могут сохраниться
после отката; документы и движения сценария не остаются. Ошибки старых данных
нужно разобрать по номенклатуре, а не считать успешным переносом.

Ручная приёмка переноса и история проверки —
`docs/plans/production-2mzl-e1-check.md`. Пока свежая конфигурация не загружена
в базу, зелёный отчёт прежнего прогона не подтверждает E1.

### Проверка E2 (2.0.15.7)

В обработке 16 сценариев. Новый сценарий `СпособыПополненияЗапасов` выполняется
в собственной транзакции с откатом и проверяет:

- умолчания «Производство» для продукции и полуфабриката и «Закупка» для товара,
  сырья, комплекта и услуги;
- серверное заполнение пустого значения в `ПередЗаписью`;
- сохранение явно выбранного значения «Оба»;
- заполнение пустого значения существующего элемента монопольным обработчиком
  обновления и пропуск групп;
- идемпотентность повторного запуска обработчика.

Прогон 2026-09-12 на обновлённой `build/ib` 2.0.15.7: 16 сценариев,
ошибок 0, пропусков 0. Смок: 396 форм, 110 проверок на образцах и 53 печатные
формы, ошибок 0.
Ручная проверка карточки: поле видно при включённом производстве и скрыто при
выключенном; смена типа устанавливает умолчание; ручной выбор «Оба» сохраняется
после записи, закрытия и повторного открытия.

### Проверка E3 (2.0.15.8)

В обработке 17 сценариев. Новый сценарий `КоммерческоеПредложение` выполняется
в собственной транзакции с откатом и проверяет:

- создание и запись предложения, статус «Черновик» и сохранение ручного статуса;
- перенос описания строки в карточку соответствующей номенклатуры;
- формирование печатной формы Devis A4;
- заполнение заказа покупателя на основании предложения, перенос шапки и строки;
- связь заказа с предложением и автоматический статус «Выигран» после записи;
- сохранение исходного статуса, когда заказ не прошёл проверку заполнения.

Смок и сценарии запускаются после Refresh проекта в EDT и обновления `build/ib`
до 2.0.15.8.

### Проверка E4 (2.0.15.9)

В обработке 18 сценариев. Новый сценарий `ВидОперацииЗаказа` выполняется
в собственной транзакции с откатом и проверяет:

- значение заполнения «Продажа» у нового заказа и заполнение «Производство» из
  структуры отбора списка — путь общей команды «Заказы клиентов на производство»;
- заполнение пустого вида операции в `ПередЗаписью` у заказа, созданного кодом без
  заполнения (как в кабинете партнёра и лидах), и сохранение смены вида у записанного
  заказа;
- вид операции заказа по коммерческому предложению: закупаемый товар — «Продажа»,
  продукция и способ пополнения «Оба» — «Производство» при включённой опции
  «Производство», «Продажа» при выключенной;
- представление заказа на производство и неизменное стандартное представление
  продажи: формат платформы берётся с коммерческого предложения, у которого
  обработчиков представления нет;
- заполнение существующего заказа монопольным обработчиком обновления, отсутствие
  заказов без вида операции и идемпотентность повторного запуска.

Уровень 3 клиентскую часть не видит. Руками после обновления: команда в разделе
«Производство» открывает список только заказов на производство, первая страница
без пропусков при числе продаж больше размера страницы, новый заказ из списка —
«Производство»; тумблер «Операция» в общем списке; заголовок формы при смене вида;
представление основания в реализации, введённой по заказу; при выключенной опции
«Производство» нет поля, колонки, тумблера и команды. Раздел «Производство» —
глазами под ролями `ОператорПроизводства` и `Кладовщик`, которым выдана команда.

Смок и сценарии запускаются после Refresh проекта в EDT и **полного** обновления
`build/ib` до 2.0.15.9: инкрементальное обновление падает на новом объекте
верхнего уровня.

### Проверка E5 (2.0.15.13)

В обработке 19 сценариев. Новый сценарий `МатериалыЗаказа` выполняется в собственной
транзакции с откатом и проверяет:

- заполнение всех строк по действующим картам: нулевые коды строк товаров получают
  уникальные коды, материал, количество за единицу и итог, версия карты в колонке
  «Техкарта», упаковка только при опции «Использовать упаковку», закупаемый товар без
  карты не сообщается;
- заполнение одной строки: позиция без карты сообщается и остаётся без материалов,
  строка с повторным кодом получает новый код и собственные материалы;
- запись: строки одной продукции с разным количеством и одним составом, пересчёт
  материалов по новому количеству, отказ при разном составе одной продукции (в том числе
  при другой характеристике), строка без материалов не мешает, смена номенклатуры и
  удаление строки убирают материалы, нулевой код строки без материалов не меняется;
- заказ по коммерческому предложению с продукцией получает материалы сразу, у продажи
  материалы и версии карт очищаются, режим загрузки материалы не трогает, коды строк
  продажи не меняются;
- настоящая запись с разным составом отклоняется — последним шагом: отказ записи
  оставляет транзакцию пригодной только для отката, поэтому промежуточные проверки
  отказа идут через экспортную `СогласоватьМатериалы` объекта без записи.

Прогон 2026-09-13 на копии обновлённой `build/ib` 2.0.15.13 (после исправлений по ревью):
19 сценариев, ошибок 0, пропусков 0; CheckConfig без замечаний; смок — 398 форм, 110 проверок на
образцах и 53 печатные формы, ошибок 0. Шаг 1б смока ложно сработал на испаноязычных строках
расширенной проверки (TECHDEBT 2026-09-13), шаг 2 запускался отдельно.

Уровень 3 клиентскую часть формы не видит. Руками после обновления: таблица материалов
видна только у заказа на производство и скрывается при смене вида без записи; отбор по
текущей строке и «Все материалы»; обе кнопки заполнения и вопрос перед перезаполнением
всех строк; копирование строки товаров копирует материалы, отмена копирования (Esc) не
оставляет лишних; смена номенклатуры вводом и автоподбором очищает материалы; пересчёт
при изменении количества и упаковки продукции, подбор и штрихкод; колонка «Техкарта»
через «Ещё → Изменить форму»; при выключенной опции «Производство» форма заказа
открывается без таблицы материалов.

### Проверка E6 (2.0.15.14)

В обработке 20 сценариев. Новый сценарий `ПроведениеЗаказа` выполняется в собственной транзакции
с откатом и проверяет:

- проведение заказа на производство с материалами, отличными от карты: версия карты в колонке
  «Техкарта» строки продукции записана в базу, её состав равен составу заказа, дата — день
  проведения, версия становится действующей картой продукции; у закупаемого товара без материалов
  версии нет;
- повторное проведение без изменений карту не перезаписывает — сверяется версия данных документа
  карты: карта дня общая, ссылка и число документов лишнюю регистрацию не показывают;
- карту поправили после проведения, а материалы заказа (разбитые на две одинаковые строки) не
  менялись — перепроведение правку не откатывает;
- заказ с материалами, равными действующей карте, получает её версию без новой регистрации;
- новый состав перезаписывает карту дня, упаковка карты сохраняется;
- свёртка составов, очистка и перенос упаковки — в обеих ветках опции «Использовать упаковку»
  параметром, независимо от константы базы;
- отказы через экспортную `ПроверитьПроведениеЗаказа` без записи: продукция «Производство» без
  материалов, пустой материал, материал = продукция, разные упаковки одного материала, будущая карта
  продукции (текст ошибки даты); закупаемый товар без материалов проводится;
- права ролей: у `Оператор` нет права менять карты, у `ОператорПроизводства` есть;
- смена вида проведённого заказа отклоняется при записи и перепроведении; настоящая отмена проведения
  вместе со сменой вида в одной записи проходит и карту не трогает;
- запись проведённого заказа в режиме «Запись» с изменёнными материалами отклоняется
  (`ПроверитьМатериалыПроведенногоЗаказа`), без изменений — нет, при перепроведении — нет;
- предусловия: у пользователя прогона есть право менять карты; `ЗаказПроводится` — Истина у
  производства и Ложь у продажи; в тексте результата — какая ветка упаковки выполнялась;
- `НужнаПроверкаЗаполненияПриЗаписи`: только запись из формы в режиме «Запись», не пометка удаления;
- заголовок формы: продажа — «Enregistré», непроведённый заказ на производство — «Non reporté»;
- последним шагом настоящая запись продажи с проведением отклоняется.

Уровень 3 клиентскую часть не видит. Руками после обновления: у продажи в форме нет «Провести»,
«Провести и закрыть», «Отмена проведения», главная кнопка — «Записать и закрыть», заголовок
«Enregistré»; у заказа на производство — кнопки проведения, «Провести и закрыть» главная, после
проведения поле вида операции только для просмотра, «Отмена проведения» видна; в форме нет
задвоенных кнопок (явные кнопки в автозаполняемой панели); список «Заказы клиентов на производство»
и общий список с тумблером «Производство» — кнопки проведения и пункты контекстного меню есть, в
общем списке без отбора — нет; пиктограмма проведённого заказа; форма выбора заказа — без
проведения; ECommerce-форма — заголовок «Enregistré», проверка заполнения при записи работает;
запись продажи с пустым контрагентом из формы отклоняется (к сообщению поля добавляется платформенное
«Не удалось записать…»).
Под пользователем только с ролью `Оператор`: проведение заказа на производство с составом, отличным от
карты, отклоняется сообщением «…права менять карты нет», версия данных карты не меняется; с составом,
равным карте, заказ проводится. Двумя сеансами одновременно: запись карты в карточке номенклатуры и
проведение заказа той же продукции — одна запись ждёт другую, документ карты за день один.

### Проверка E7 (2.0.15.15)

В обработке 21 сценарий. Новый сценарий `ПотребностиПроизводства` выполняется в собственной транзакции с откатом;
ожидаемые количества — план `.ai/plans/production-2mzl-e7.md`, п. 12.7: продукция P1 (M1 ×2, M2 ×1) ×10, P2
(M1 ×3) ×5. Проверяет:

- проведение заказа на производство: «Потребность» M1/P1 20, M2/P1 10, M1/P2 15; перепроведение без дублей;
- заказ поставщику по продаже (основание не ставится, состав товаров), под непроведённый заказ на производство и
  строка из предварительного заказа — движений нет;
- ввод заказа поставщику на основании заказа на производство: основание и строки дефицита с продукцией; строки с
  явной продукцией распределяются раньше строк без неё; `ДефицитЗаказа` после первого заказа; заполнение по
  потребности, заказ сверх дефицита и излишек на последнюю продукцию;
- поступление по заказу поставщику: продукция по коду строки и по «Заказано» этого заказа поставщику; ввод
  поступления на основании заказа клиента (только заполнение) — «заказано − пришло»;
- два возврата поставщику по одному поступлению — сторно «Пришло» этого поступления в обратном порядке продукции,
  второй возврат учитывает первый;
- отчёт «Потребности производства» компонуется, флаг «Только дефицит» отбрасывает строки без дефицита;
- перепроведение каждого документа не меняет остатки; отмена проведения в обратном порядке обнуляет их, повторное
  проведение в прямом порядке восстанавливает;
- проведённый заказ с другим количеством продукции в режиме «Запись» отклоняется через экспортную
  `ПроверитьМатериалыПроведенногоЗаказа` без записи;
- последним шагом ввод заказа поставщику на основании непроведённого заказа на производство — исключение с текстом.

Смок и сценарии запускаются после Refresh проекта в EDT и **полного** обновления `build/ib` до 2.0.15.15: новый
регистр верхнего уровня, реквизит табличной части заказа поставщику и тип основания поступления.

Прогон 2026-09-14 на копии полностью обновлённой `build/ib` 2.0.15.16: CheckConfig без замечаний; шаг 1б смока ложно
сработал (96 строк, все в базе известных замечаний без учёта BOM), шаг 2 — 398 форм, 110 проверок на образцах и 53
печатные формы, ошибок 0; `doctests` — 21 сценарий, 19 ОК, 2 ошибки: `ПроведениеЗаказа` (переменная `Менеджер` в
модуле объекта заказа совпала с реквизитом, урок tiny1C bsl-010) и `ПотребностиПроизводства` (зарезервированное слово
`Есть` псевдонимом запроса). Исправлено в 2.0.15.17, повторный прогон ждёт обновления базы.

Повтор 2026-09-14 на 2.0.15.17: 21 сценарий, 20 ОК; `ПроведениеЗаказа` и все шаги движений `ПотребностиПроизводства`
прошли, упала проверка отчёта — детальные записи с автовыбором без измерений компоновка свернула в 2 строки из 3.
В 2.0.15.18 поля отчёта выводятся явно, сверяются сами строки.

Повтор на 2.0.15.18: три строки отчёта совпали, лишней оказалась строка общего итога (45/52/29) — вывод в коллекцию
значений добавляет её в конец (в прошлом прогоне «2 строки» — свёрнутая детальная и итог). В 2.0.15.19 общий итог в
проверке отключён.

**Итог 2026-09-14 на копии обновлённой `build/ib` 2.0.15.19: 21 сценарий, ошибок 0, пропусков 0** — в том числе
`ПроведениеЗаказа` (E6) и `ПотребностиПроизводства` (E7). Смок на 2.0.15.16 (последняя сборка с изменением форм):
CheckConfig без замечаний, 398 форм, 110 проверок на образцах, 53 печатные формы, ошибок 0. Ручная приёмка форм —
по списку выше.

Уровень 3 клиентскую часть не видит. Руками после обновления: в заказе поставщику поле «Заказ клиента» и кнопка
«Заполнить по потребности» (вопрос перед очисткой непустой таблицы, сообщение без заказа клиента и у непроведённого
заказа), колонка «Продукция» через «Ещё → Изменить форму»; без опции «Производство» поля, кнопки и колонки нет;
«Создать на основании» из заказа клиента на производство — заказ поставщику и поступление, у продажи поступление
отклоняется; колонка «Заказ клиента» в списке поступлений и открытие списка под ролью `Логист` (колонки нет, список
открывается); отчёт «Потребности производства» в разделах «Производство» и «Закупки», флаг «Только дефицит»;
ввод заказа поставщику по заказу на производство при выключенной опции «Производство» — прежний состав товаров.

### Проверка E8 (2.0.15.20)

В обработке 22 сценария. Новый сценарий `СтадииЗаказа` выполняется в собственной транзакции с откатом; ожидаемые значения —
по коду `ПроизводственныеЗаказы.РассчитатьСтадию` (план `.ai/plans/production-2mzl-e8.md`, раздел 6): заказ O1 — товар 3 и
продукция 10 по цене продажи 100, материал — сырьё 2 на единицу (потребность 20). На каждом шаге сверяются стадия и
индикатор расчёта и последней записи истории:

- запись без проведения — «Черновик» `○○○○○`, повторная запись записей истории не добавляет; проведение — «Заказ
  поставщику», перепроведение без новой записи;
- заказ поставщику на всю потребность — «Восполнение потребности» `●○○○○`; поступления 12 (доля 0,6, `●◐○○○`) и 8 — «В
  производстве»; отмена второго поступления возвращает «Восполнение», повторное проведение — «В производстве»;
- выпуски 4 (доля 0,4, `●●◐○○`) и 6 — «Ожидает отгрузки»;
- реализация товара и продукции 4 — «Ожидает отгрузки» `●●●◐○`, долг 700; реализация продукции 6 — «Ожидает оплаты», долг
  1300; возврат одной единицы — «Ожидает отгрузки», долг 1200; отмена возврата — «Ожидает оплаты»;
- ПКО по первой реализации — `●●●●◐`, доля оплаты 0,5385; ПКО «на основании заказа» (аванс на самом ПКО) историю не меняет;
  зачёт этого аванса во второй реализации — «Выполнен» `●●●●●`; отмена первой оплаты — «Ожидает оплаты»;
- поздняя веха: заказ без закупок и выпуска, отгруженный со склада, — «Ожидает оплаты» `○○○●○`; заказ без материалов —
  «Формируется потребность», аванс по строке оснований — `○○○○◐`, долг −100;
- отмена проведения O1 со связанными документами — «Черновик» `○○●●◐`, движений потребности нет;
- продажа — «Новый», расчёта стадии нет, реализация — «Собран»; черновик на производство, переведённый в продажу, и
  проведённый заказ, переведённый в продажу вместе с отменой проведения, — «Новый».

Смок и сценарии запускаются после Refresh проекта в EDT и **полного** обновления `build/ib` до 2.0.15.20: новые значения
перечисления, реквизиты регистра истории статусов, индексы `Основание` у реализации, выпуска и возврата от покупателя.

**Итог 2026-09-14 на копии полностью обновлённой `build/ib` 2.0.15.20: 22 сценария, ошибок 0, пропусков 0** — с
первого прогона. Смок: CheckConfig без замечаний; шаг 1б ложно сработал (96 строк, после нормализации BOM и Unicode все
в базе известных), шаг 2 отдельно — 398 форм, 110 проверок на образцах, 53 печатные формы, ошибок 0.

Уровень 3 клиентскую часть не видит и идёт под администратором. Руками после обновления: в шапке заказа на производство —
стадия и жирный индикатор, подсказка с легендой, обновление после проведения и отмены проведения; у продажи — прежний
статус; в списке заказов колонка «Индикатор», цвет «Черновик» и «Выполнен», особый цвет «Ожидает оплаты», картинки
отгрузки и оплаты у заказов на производство; панель «Детали» — строка «Стадия»; дашборд производства — карточка «Заказов в
производстве» и диаграмма статусов; АРМ производства — колонка «Индикатор»; список реализаций, закладка «Заказы к
отгрузке» — выполненных заказов нет. Под пользователем с ролью `PDV` или `Казначей`: проведение ПКО по реализации заказа на
производство проходит и двигает стадию; под `Кладовщик` — отмена проведения реализации по такому заказу.

### Проверка E9 (2.0.15.21)

В обработке 23 сценария. Новый сценарий `АвтовыпускПриОтгрузке` выполняется в собственной транзакции с откатом и сам
включает константу `ИспользоватьВыпускПродукцииПриПродаже`; `СтадииЗаказа` её явно выключает. Ожидаемые значения — по коду
`Производство` (план `.ai/plans/production-2mzl-e9.md`, раздел 9): остатки товар 3 по 50, M1 50 по 40, M2 10 по 30; заказ
на производство — товар 3, P1 10 (M1 2 на единицу), P2 3 без материалов с техкартой M2 1; под заказ пришло M1 12 по 10.

- реализация товар 3, P1 6, P2 3: расчёт — продукция «P1 6, P2 3», материалы «M1 12 под заказ, M2 3», «ИзОстатка» пуст;
  один проведённый автовыпуск на секунду раньше; себестоимость автовыпуска 210, продажи 360; M1 на складе 50 на 2000;
  стадия «Восполнение потребности» `●◐◐◐○`;
- ручной выпуск P1 2 и реализация P1 4: к выпуску 2, M1 4 из общей очереди, «ИзОстатка» 4, себестоимость продажи 320, M1 42;
  «Ожидает оплаты» `●◐●●○`, доля поступления 0,8; перепроведение — тот же автовыпуск, «ИзОстатка» 4, M1 42;
- отмена проведения второй реализации распроводит автовыпуск и снимает «ИзОстатка», стадия `●◐◐◐○`; пометка удаления
  реализации переходит на автовыпуск и снимается; повторное проведение — тот же автовыпуск;
- без записи: правка автовыпуска и ручной выпуск с основанием-реализацией отклоняются; расчёт первой реализации не учитывает
  более поздние выпуск и реализацию; выключенная константа и реализация по продаже — автовыпуск не активен;
- две реализации одного заказа в одну секунду (6 и 4) выпускают 6 и 4, перепроведение обеих — то же;
- продукция с серией в строке реализации выпускается с той же серией;
- последним: заказ P3 5 (M3 2 на единицу) при остатке M3 3 — реализация не проводится, в сообщении названо M3.

Смок и сценарии запускаются после Refresh проекта в EDT и **полного** обновления `build/ib` до 2.0.15.21: тип реквизита
`Основание` выпуска стал составным, у выпуска появились движения потребностей производства.

Уровень 3 клиентскую часть не видит и идёт под администратором. Руками после обновления: настройки «Производство» —
переключатель «Автоматический выпуск при продаже» (недоступен при выключенном разделе); реализация по заказу на
производство — после проведения в списке выпусков новый выпуск с основанием-реализацией на секунду раньше, себестоимость в
отчёте по продажам; форма автовыпуска — только просмотр, надпись-ссылка открывает реализацию, у ручного выпуска поле
«Основание» предлагает только заказ; панель «Детали» списка выпусков — «Создан автоматически при проведении реализации»;
заказ — стадия и индикатор после реализации, отчёт «Потребности производства» — колонка «Из остатка»; нехватка материалов —
сообщение с перечнем. Под пользователем с ролью `PDV` или `Казначей`: проведение реализации по заказу на производство с
автовыпуском проходит.
