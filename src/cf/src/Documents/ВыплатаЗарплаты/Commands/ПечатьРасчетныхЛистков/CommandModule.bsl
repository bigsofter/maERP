
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Для Каждого Док Из ПараметрКоманды Цикл
		Таб = Новый ТабличныйДокумент;
		Сформировать(Док, Таб);
		ВыводПечатныхФормКлиент.ОткрытьПредварительныйПросмотр(Таб);
	КонецЦикла;	
КонецПроцедуры

&НаСервере
Процедура Сформировать(Док, Таб) Экспорт
	Документы.ВыплатаЗарплаты.СформироватьРасчетныеЛистки(Док, Таб);
КонецПроцедуры
