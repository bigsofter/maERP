// Блок настроек «Производство»: включение раздела и автоматический выпуск при продаже.
// Константа записывается сразу при изменении поля.
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	УстановитьДоступностьАвтовыпуска();

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

&НаКлиенте
Процедура НастройкаПриИзменении(Элемент)

	ОбновлятьИнтерфейс = ЗаписатьНастройкуНаСервере(Элемент.Имя);
	Модифицированность = Ложь;
	ОбновитьПовторноИспользуемыеЗначения();
	Если ОбновлятьИнтерфейс Тогда
		ОбновитьИнтерфейс();
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Функция ЗаписатьНастройкуНаСервере(Знач ИмяЭлемента)

	ОбновлятьИнтерфейс = Обработки.НастройкиПрограммы.ЗаписатьНастройкуФормы(ЭтотОбъект, ИмяЭлемента);
	УстановитьДоступностьАвтовыпуска();
	Возврат ОбновлятьИнтерфейс;

КонецФункции

// Автоматический выпуск при продаже работает только при включённом разделе «Производство» (E9).
//
&НаСервере
Процедура УстановитьДоступностьАвтовыпуска()

	Элементы.ИспользоватьВыпускПродукцииПриПродаже.Доступность = НаборКонстант.ИспользоватьПроизводство;

КонецПроцедуры

#КонецОбласти
