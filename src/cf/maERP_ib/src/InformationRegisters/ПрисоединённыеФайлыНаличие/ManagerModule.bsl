
Процедура ОбновитьНаличиеФайлов(Объект) Экспорт
	МЗ = РегистрыСведений.ПрисоединённыеФайлыНаличие.СоздатьМенеджерЗаписи();
	МЗ.Объект = Объект;	
	МЗ.ЕстьФайлы = РегистрыСведений.ПрисоединённыеФайлы.ЕстьПрисоединенныеФайлы(Объект);	
	МЗ.Записать(Истина);
КонецПроцедуры
