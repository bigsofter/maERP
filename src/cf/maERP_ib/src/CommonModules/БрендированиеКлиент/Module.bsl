
Процедура ВызовПервогоЗапуска() экспорт
	
	РезультатОрганизации = БрендированиеВызовСервера.ПолучитьКоличествоОрганизаций();
	
	РезультатПользователи = БрендированиеВызовСервера.ПолучитьКоличествоПользователей();  
	
	Если РезультатОрганизации = 0 и РезультатПользователи = 0 Тогда
		
		ОткрытьФорму("ОбщаяФорма.ФормаПервогоЗапуска",,ЭтотОбъект,,,,,РежимОткрытияОкнаФормы.БлокироватьВесьИнтерфейс);
		
	КонецЕсли;
	
КонецПроцедуры
